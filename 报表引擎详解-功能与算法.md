# 报表引擎详解 —— 功能与算法

> 范围：`print-server` 的**非线性报表引擎**（`print-server/src/report/`）。
> 写法约定：**每条算法结论后面跟 `文件:行号`**，对不上源码的就是我写错了。
> 行号取自本文写作时的 `HEAD`（`3759813`），代码一变就要重新核。
>
> ⚠️ **行号已经漂了（2026-09-26 实测，逐提交核过）**：`3759813` 之后有 **7 个提交**
> （②①③④ 修复 + 缺陷探针 + 分级通道 + 界面接线）。实测漂移：
> `engine.rs` **5094 → 6085 行（+991）**、`mod.rs` **7753 → 7900 行（+147）**。
> （上一版这里写的是 **+984 / 6078** —— 那是**改到一半时量的**，对不上任何一个提交；
> 逐提交量出来才发现。**「量过一次」不等于「现在还是这个数」。**）
> 所以本文里的 `engine.rs:NNNN` **不要当精确定位用** ——
> 中段引用大致要 **+105**，但插入点前后的偏移量不同，**统一加常数是错的**。
> **可靠做法：按引号里的代码片段或函数名去搜**（例：「`layout_columns` 让每个列展开组
> 从自己的 `tpl_col` 起占列」→ 搜 `let mut cursor = self.insts[group[0]].tpl_col`）。
> 行号整体重核是一件独立的机械工作，**还没做** —— 别以为文中行号已经跟上了。
>
> 验证状态：本文所有断言都在本机跑过（`cargo test` **523 通过 / 0 失败 / 14 忽略**；
> `scripts/check-all.sh` **7 道闸全绿**：`ts-test` 239 条、`ts-test-designer` 136 条）。
> 那 14 条 `#[ignore]` 的构成（`cargo test -- --list --ignored` 实测）：
> **13 条性能基准**（`engine::scale::bench_*`，debug 下跑没意义）
> ＋ **1 条条码外部 oracle 的 dump**（`barcode::tests::dump_matrices_for_external_oracle`）。
> **缺陷探针一条都不剩了** —— ①~④ 全部修完并转成常驻回归测试（见 §14.6）。
> 也就是说：**默认闸里没有红测试被藏起来**，`#[ignore]` 里只有基准和一条 oracle dump。
> 凡是我**没能验证**的（例如「真 Word 能打开导出的 docx」），文中显式标注为未验证，
> 不当成结论写。

---

## 0. 一句话

这是一个**只读的、模板驱动的非线性报表引擎**：输入「模板 + 数据行」，输出「展开后的二维网格」，
再由此派生 HTML / xlsx / CSV / docx 四种导出物。

它的内核是四件事：

1. **主格树**（哪一格跟着哪一格展开）—— 决定数据怎么铺开；
2. **层次坐标求值**（`D3[B3:+0].sum()`）—— 决定跨行跨列的聚合怎么算；
3. **不动点迭代**（求值 ↔ 过滤互相依赖）—— 决定「小计为 0 的分组不显示」这类需求怎么收敛；
4. **布局**（行分组 + 列递归分列）—— 决定算出来的东西落在第几行第几列。

外加一层**自研的零依赖编码器**（二维码 / Code128 / PNG / ZIP / OOXML），
让条码、图表、Word 导出都不依赖外部 crate 或本机程序。

---

## 1. 定位：三个「表格」不是一回事

这是本项目最容易出沟通事故的地方，先分清（细节见项目长期记忆）：

| 模型 | 结构 | 坐标 | 本质 | 代码位置 |
| --- | --- | --- | --- | --- |
| **自由画布** | `AnyControl[]` 控件树 | mm 绝对定位 | 打印版面 | `designer-react/src/canvas/` |
| **表格控件** | `TableCell[][]` 固定行列 | 挂靠控件 | 单据明细 | `openprint/src/types/control.ts` |
| **非线性报表** | `CellTpl[][]` 行列可扩展 | **运行期布局** | 分组 / 交叉 | `print-server/src/report/` ← **本文只讲这个** |

前两者是「画出来的版面」，第三个是「算出来的版面」。
非线性报表的每一格最终落在第几行第几列，**模板里查不到**，只能由引擎按数据算出来。

---

## 2. 代码地图与数据流

### 2.1 文件与职责

`print-server/src/report/` 共 **22 113 行**，15 个模块（`wc -l` 实测）：

| 文件 | 行数 | 职责 |
| --- | --- | --- |
| `mod.rs` | 7 753 | 渲染管线（`render`）、分页、HTML、参数绑定、样例模板、**172 条测试** |
| `engine.rs` | 5 094 | **展开引擎本体**（生产代码 3 702 行 + 1 392 行测试） |
| `model.rs` | 1 736 | 全部数据模型（模板 / 实例 / 输出格 / 页面设置 / 纸张表） |
| `barcode.rs` | 1 662 | 自研二维码 + Code128 编码器（**44 条测试**，全仓最密） |
| `xlsx.rs` | 1 453 | xlsx 导出（列宽、样式、原生图表、位图嵌入） |
| `expr.rs` | 738 | 表达式词法 + 语法分析（纯语法层，不碰引擎状态） |
| `store.rs` | 679 | `ReportDef` 持久化（列表 / 存 / 读 / 删） |
| `chart_svg.rs` | 625 | 图表的**内联 SVG** 渲染（HTML 侧） |
| `docx.rs` | 572 | 手写 OOXML 的 Word 导出 |
| `chart.rs` | 465 | 图表声明 → 模板坐标反查 → 数值 |
| `import.rs` | 397 | 把 `.xlsx` 读成模板（文本即语义通道） |
| `png.rs` | 314 | 自研最小 PNG 编码器（**非压缩 DEFLATE**） |
| `barcode_svg.rs` | 284 | 条码位矩阵 → 内联 SVG |
| `zip.rs` | 196 | 最小 ZIP 写出器（**只写 method 0**） |
| `csv.rs` | 145 | CSV 导出（RFC 4180 + BOM + CRLF） |

另有 `ticket/`（`esc` / `tspl` / `zpl`）负责票据指令，不属本报告范围。

### 2.2 数据流

```
HTTP 请求 (RenderRequest)
  │
  ├─ 数据源现查（render_with_sources → db::query_rows）      mod.rs:474
  │
  └─ render()                                                 mod.rs:112
       │
       ├─ prepare_dataset   多数据集挑选 + UNNEST 嵌套数组      mod.rs:551 / 506
       ├─ loop_groups       循环变量 → 一个取值一张 sheet       mod.rs:266
       ├─ PageConfig::resolve_setup  纸张/方向/边距/页码校验    model.rs:929
       │
       └─ Engine::expand_sheet()  ← ★ 核心，八个阶段           engine.rs:957
            │
            ├─ paginate()     按行切页 + 重复表头表尾           mod.rs:412
            │
            └─ 导出端
                 ├─ to_html()      @page + 内联 SVG            mod.rs:803
                 ├─ to_xlsx()      原生图表 + 位图条码          xlsx.rs:443
                 ├─ to_csv()       RFC 4180                    csv.rs:37
                 └─ to_docx()      手写 OOXML + stored zip      docx.rs:316
```

**关键结构选择**：`Engine` 一次只服务**一张 sheet**，`render()` 每张 sheet 新建一个实例
（`mod.rs:152`）。这保证了「变量不跨 sheet」——`vars` 的清空只是防御性的，
真正的隔离来自「一个新 Engine」（`engine.rs:965-968` 的注释明确写了这一点，并有探针证实）。

---

## 3. 数据模型：三层 + 一个范式

### 3.1 三层

```
① 声明层（模板里存的东西，字段全是 Option）
   ReportTemplate { sheets: Vec<SheetTpl>, datasets }        model.rs:1054
     └─ SheetTpl { name, rows: Vec<RowTpl>, page, loop_field } model.rs:1039
          └─ RowTpl { cells: Vec<CellTpl> }                    model.rs:729
               └─ CellTpl { pos, value, model: Option<CellModel>,
                            merge_across, merge_down, merge_to_end,
                            image, chart, barcode }             model.rs:701

② 实例层（展开过程的中间态，只在 Engine 内部）
   CellInst  42 个 pub 字段：父子链 / rows 视图 / 求值状态 / 布局结果  model.rs:1092

③ 输出层（跨语言契约，设计器与导出端都读它）
   GridCell { text, pos, rowspan, colspan, raw_number,
              num_format, formula, style, image, chart, barcode } model.rs:1249
   RenderedSheet { name, rows, page_setup }                      model.rs:1293
```

### 3.2 核心范式：「声明 vs 结果」

引擎里有一组**成对**的类型，把「作者写了什么」和「算出来是什么」严格分开：

| 声明（模板） | 结果（输出） | 区别 |
| --- | --- | --- |
| `CellChart` `model.rs:630` | `ResolvedChart` `model.rs:672` | 前者写模板坐标 `["A3"]`，后者是**已展开的真实数值** |
| `CellBarcode` `model.rs:489` | `ResolvedBarcode` `model.rs:522` | 前者写原文，后者是**已编码的位矩阵** |
| `PageConfig` `model.rs:818` | `ResolvedPageSetup` `model.rs:852` | 前者全是 `Option`，后者**校验过、方向已换算** |
| `CellConditional` `model.rs:259` | `CondRule` `engine.rs:614` | 前者 `when` 是字符串，后者是**能直接比的 `(op, a, b, style)`** |
| `CellModel` 里的声明 | `GridCell` | 前者可能是错的，后者一定可用 |

**为什么值得这么做**：`ResolvedChart` 存的是**算好的数**而不是一个坐标引用
（`model.rs:1275-1279` 的注释）——预览、HTML、xlsx 三边拿到同一份数据，
不会各自再解析一遍坐标。`ResolvedBarcode` 同理（`model.rs:1282-1287`）。
「各自再解析一遍」的下场就是三边慢慢分叉，而分叉是静默的。

### 3.3 位置名（pos）与坐标

```rust
fn col_name(idx) -> String          // 0 → "A"，25 → "Z"，26 → "AA"   model.rs:1305
pub fn cell_pos(row, col)           // (0,2) → "A3"（1 基行号）        model.rs:1319
```

`GridCell.pos` **保留的是模板坐标**，不是输出坐标（`model.rs:608-612`）。
这是图表能在服务端成立的前提：只有保留模板坐标，「`A3` 展开成了哪几个输出格」才反查得回来。

---

## 4. 核心算法：一次 `expand_sheet` 的八个阶段

`expand_sheet` 是引擎的唯一入口（`engine.rs:957`），它按固定顺序跑八个阶段。
**顺序本身是语义的一部分**——后面每一阶段都依赖前面阶段的产出。

```
阶段 0    模板级父格解析（含缺省推断 + 规则 3 认领）   engine.rs:980-981
阶段 0.5  编译条件格式规则（模板级，只编译一次）        engine.rs:988
阶段 1    按模板顺序建实例（父格必然先于子格）          engine.rs:992-1139
阶段 2    惰性求值 + 依赖传播 + 不动点迭代              engine.rs:1151
阶段 2.5  展示文本（formatExpr > dict > NumFmt）        engine.rs:1157-1161
阶段 3    行布局（递归 place）                          engine.rs:1165
阶段 3.5  列布局（col_parent 递归分列）                 engine.rs:1169
阶段 4    填网格 + 图表反查                             engine.rs:1179-1314
```

### 4.1 阶段 0：父格解析

#### 三态 `ParentDecl`

```rust
enum ParentDecl {
    Ref(String),      // 声明了父格
    ExplicitNone,     // 显式写了 A0 = 「明确不要父格」
    Unset,            // 没写
}
```
`engine.rs:47-51`

**为什么必须三态而不是两态**：上游 NopReport 的 `getRowParent` 开头就是
`if (rowParent == CellPosition.NONE) return null`——写了 `A0` 就是**明确不要父格**，
**不再套用缺省推断**。早期实现用 `filter(|p| !is_root_ref(p))` 把 `A0` 当成「没写」，
于是它落进缺省推断，与注释和上游都不符（`engine.rs:36-40` 的注释记录了这次修正）。

`A0` 对应 NopReport 的 `CellPosition.NONE`，`is_root_ref` 大小写不敏感（`engine.rs:21`）。

#### 缺省行父格推断：三步，命中即停

```
① 向左扫同行，扫到**纵向扩展格** → 它就是父格
② 扫到的格子不是扩展格、但它**已经有**父格 → 跟随到那个父格
③ 都扫不到 → 取本行最左格（col 0）的父格
```
`default_row_parent` `engine.rs:83-110`

三个细节：

- **第 ② 步读的是「解析后」的父格**，含推断出来的。上游 `initParentChildren` 会把推断结果
  **回写**进 model（`if (xptModel.getRowParent() == null) setRowParent(...)`），
  所以后处理的格子往左扫时，读到的是左边格子**解析后**的值（`engine.rs:42-45`）。
  这就是 `resolve_parents` 必须**按行优先序一次性预扫**的原因（`engine.rs:161-170`）。
- **自引用保护**：跟随结果等于本格位置时返回 `None` 而不是自己（`engine.rs:99`、`engine.rs:107`）。
- **`ExplicitNone` 是「停并返回无父格」**，不是继续往左扫（`engine.rs:101`）。

列父格是它的镜像（`default_col_parent` `engine.rs:113-139`）：向上扫 → 扫不到取**第一行**同列。

#### `resolve_parents`：为什么抽成独立的 Pass 0

`engine.rs:171-201`。抽出来的理由（注释里写了）：

> 规则 3（`addDefaultRowParents`）要先知道「谁是谁的子格」才能算展开范围，
> 而展开范围又必须在建实例**之前**就定下来。原先边建实例边推断，拿不到这个全局视图。

**语义等价性**：解析只读模板和**行优先序更早**的格子（向左扫同行前面的列、向上扫同列前面的行），
所以一次性预扫出的 map 与逐格边扫边填的结果一致（`engine.rs:167-169`）。

**两处必须用同一个判据**——`is_placeholder`（`engine.rs:153-159`）同时被阶段 0 和阶段 1 使用：

```rust
fn is_placeholder(cell: &CellTpl) -> bool {
    cell.value.is_none() && cell.model.is_none()
        && cell.image.is_none() && cell.chart.is_none() && cell.barcode.is_none()
}
```

判据一旦不一致，后果是**静默的**：某格在一处算「存在」、在另一处算「不存在」，
于是主格树缺节点、子格退回挂根——表照样出得来，只是数据悄悄变多（`engine.rs:143-146`）。

三个「必须算有内容」的反例（都是实测踩过的）：
- **图片格**：只放一张 logo、既不写 `value` 也不写 `model` 是很自然的写法，
  漏掉它那个格子会**整格消失**（`engine.rs:148-149`）。
- **图表格**：它**天生就没有 `value`**（数据来自别的格子），漏判的话 100% 消失（`engine.rs:150-151`）。
- **条码格**：一个固定二维码（扫码关注）也不写 `value`（`engine.rs:152`）。

### 4.2 规则 3：认领孤儿格

**问题**：手写模板里，展开块「影子」下的格子扫不到任何父格，会挂根只渲染一次——
表现就是**分组明细里突然冒出一个总计**（`engine.rs:318-319`）。

**规则**：顶层展开格把它展开范围内的、**没有父格**且**不与它自己的跨度重叠**的格子收作子格
（`add_default_parents` `engine.rs:330-414`）。

先自底向上算出每个格子的展开范围（`span_of` `engine.rs:257-295`）：

```
叶子：  offset = 0, span = 自身跨度
非叶子：span 覆盖 自身跨度 ∪ ⋃(子格号 + 子 offset, + 子 span)
```

跨度定义：行轴 `merge_down + 1`（`tpl_row_span` `engine.rs:206`）；
列轴 `merge_across + 1`，但 `merge_to_end` 只能按「本行列数 − 起始列」估**下界**
（`tpl_col_span` `engine.rs:213-224`）——偏窄只会少认领，不会认错。

`collect_spans` 必须**覆盖全部格子**（含没有子格的叶子），不能只遍历 `children` 的键——
顶层展开格若一个子格都没有就不在 `children` 里，规则 3 会查不到它的范围（`engine.rs:299-300`）。

**三个容易写错的点**（`engine.rs:321-329` 逐条列出）：

1. **门槛是 `rowParent == null`**，即从未声明、也没推断出父格。
   显式写 `A0` 是 `CellPosition.NONE`——一个**非 null 的哨兵值**，所以**不受**规则 3 影响。
   别把 `ExplicitNone` 一起收编了。代码里就是 `matches!(resolved.get(..), Some(ParentDecl::Unset))`（`engine.rs:335`）。
2. **边认领边生效**：按行优先序处理，前面的格子认领完之后，后面的格子再判断「我有没有父格」时已经是「有」了。
   所以读的是**可变 map**，不是快照（`engine.rs:332`、`engine.rs:377`）。
3. `ranges` 只需要算一次——认领只作用在**顶层**格上，而顶层格不会成为别人的子格（`engine.rs:328-329`）。

**两个例外**：

- **列轴要放过显式声明了 `col_after` 的格子**（`engine.rs:381-398`）。
  `col_after` 是作者声明「我排在某个列展开格所占列区间**之后**」；
  规则 3 收编之后该格会变成那个列展开格的子格、被放进它的列区间**里面**——
  两条声明打架，结果是「金额合计」这种表头跟着月份重复一遍。
- **与本格自己跨度重叠的不收**（`engine.rs:407-410`），条件照抄上游。

递归深度上限 `MAX_LAYOUT_DEPTH = 256`（`engine.rs:18`）：显式父格允许指向任意格子（甚至成环），
上游遇到环抛异常，我们只求「别把栈打穿」，到顶就退回叶子形态（`engine.rs:255-256`）。

### 4.3 阶段 1：建实例

按**行升序、列升序**遍历模板格（`engine.rs:992-993`），保证父格必然先于子格创建。
这是主格树「下标严格递减的 DAG」的成因，**结构上不可能成环，无需环检测**（`engine.rs:1022-1023`）。

#### 先登记 pos，再找父格

```rust
self.by_pos.entry(pos.clone()).or_default();   // engine.rs:1018
```
**必须早于下面的父格循环**。目的：让「这个格存在、只是展开成 0 条」能和「这个格压根不存在」
区分开（`engine.rs:1013-1017`）。不登记的话，一个展开成 0 条的格在 `by_pos` 里等同于没写过，
它的子格就会被当成「父格声明错了」而退回挂根。

#### 父格查不到：两种，处理方式必须不同

```rust
let row_parents: Vec<Option<usize>> = match &row_ref {
    Some(p) => match self.by_pos.get(p).cloned() {
        Some(list) if !list.is_empty() => list.iter().map(|i| Some(*i)).collect(),
        Some(_) if *p != pos => Vec::new(),          // 父格在、但展开成 0 条 → 子格一条都不出
        None | Some(_) => {                           // 父格压根没建过 → 退回挂根 + 告警
            self.warnings.push(format!("{pos} 声明的 row_parent \"{p}\" 不存在——…"));
            vec![None]
        }
    },
    None => vec![None],
};
```
`engine.rs:1030-1045`

- **空表**：父格存在但展开成 0 条 → 子格一条都不出。润乾语义：主格没数据，跟随它的子格一并消失。
- **键不存在**：父格压根没建过，一定是模板写错（父格必须先于子格）→ **退回挂根保住数据不消失**，
  但展开结果会变少，**必须告警**。
- **自引用**（自己声明自己当主格）走 `None | Some(_)` 那条，和「父格不存在」同样处理。
  不能走上面那条——那会让整张表**悄悄渲染成空的**（`engine.rs:1035-1036`）。

#### 笛卡尔积 + 分桶

行主格 × 列主格做笛卡尔积，取数视图取两者**交集**（这就是交叉表的本质，`engine.rs:1046`）。

朴素的「每个 (行主格 × 列主格) 组合都克隆一遍 base 再二分过滤」是 O(R×M×M)。
现改为**一次分桶**（`engine.rs:1083-1096`）：

```rust
let row_to_cp = self.col_parent_index(&col_parents);   // 数据行 → 所属列主格，O(总行数)
let mut buckets: HashMap<Option<usize>, Vec<usize>> = HashMap::new();
if let Some(map) = &row_to_cp {
    for &r in &base { buckets.entry(map.get(&r).copied()).or_default().push(r); }
}
```
判别实验（`engine.rs:1086-1087`）：交叉表 640×320，这一项原本占 705 ms 里的 459 ms；
总量固定只变月份数 → 705→483→362→303 ms，与 M 成正比。

没有列主格（或列主格 rows 有重叠）时走原来的求交口径，用**二分**而不是线性扫
（`engine.rs:1098-1113`），并带 `debug_assert!` 钉住「`insts[..].rows` 必须严格升序」——
将来若有人改成非有序构造，这里会立刻炸，而不是静默少取数
（少取数是「合计悄悄变小」，比崩溃难查得多）。

#### `make_insts`：两条分支，字段必须两边都拷

`engine.rs:1446-1569`。展开格（行/列展开）与非展开格是**两条独立分支**，
每条都要把 `CellTpl` + `CellModel` 的字段拷进 `CellInst`。

**反复踩过的坑**：只拷一条分支 → 那条路上的东西**静默不出**。注释里逐条记录了：

- `image`（`engine.rs:1514-1516`）：挂在展开格上的产品图列（`field: photo` + `image.from: value`）走展开分支。
- `chart`（`engine.rs:1517-1519`）：挂在分组格上的图表走展开分支。只拷一条的话图**静默不出**。
  ⚠️ **但源码注释里那句「每组一张图」是不成立的** —— 见 §14.6 缺陷 ⑤。
- `barcode`（`engine.rs:1520-1523`）：一列订单号条码走展开分支。只拷一条的话**整列都不出**。
- `format_expr` / `dict`（`engine.rs:1525-1528`）：挂在分组格上的字典（编码 → 名称）会静默失效。

#### 展开集：三种来源

```rust
let mut groups = match &model.expand_expr {
    Some(src) => parse_expand_list(src) → group_by_list(..),   // ① 固定列表
    None => match &model.field {
        Some(f) => group_by_field(ds, view, f),                // ② 按字段分组
        None => view.iter().map(|r| (Null, vec![*r])).collect() // ③ 每行一个
    }
};
```
`engine.rs:1468-1480`

- **① `expand_expr`**（`["1月","2月","3月"]`）**优先于 `field`**。存在的两处理由（`model.rs:308-311`）：
  1. **顺序按字面量走**，不按数据出现顺序——科目顺序既不是字母序也不是数据序；
  2. 数据里**没有的项照样展开出来**（值格留空），也就是「月份补全」。
  只接受常量（展开期层次坐标尚未建立），写坏了进告警，不静默当空。
- **② `group_by_field`**：按字段分组去重，**保持首次出现顺序**（等价于 SQL `group by`）。
  用**哈希索引**定位已有分组，不是 `iter_mut().find(..)`——
  后者是「行数 × 组数」的平方级开销（16 000 个唯一值约 1.3e8 次字符串比较，
  而这段在每个展开格的展开路径上）。`engine.rs:3412-3418`。
- **分组键必须带类型前缀**（`value_key` `engine.rs:3420-3426`）：
  `Null` → `"\0null"`，字符串 → `"s:{s}"`，其余 → `"v:{other}"`。
  这样 `Null` 才能和字符串 `"null"` 区分开。

然后三道后处理（`engine.rs:1481-1494`）：`expand_max_count` 截断 → 空集时
`keep_expand_empty` 决定是否保留一格 → `expand_min_count` 补空值到 N 条。

#### 跨数据集：`join_on` 是唯一通路

```rust
let base = match parent {
    Some(p) => {
        let pds = self.insts[*p].ds.clone();
        if pds == cell_ds_name { self.insts[*p].rows.clone() }   // 同数据集：视图求交
        else { self.join_view(&cell_ds_name, *p, &model, &pos) } // 跨数据集：走 join_on
    }
    None => self.all_rows(&cell_ds_name),
};
```
`engine.rs:1071-1082`

`join_view`（`engine.rs:914-955`）**没有 `join_on` 就返回空并告警**：

> 跨数据集必须写 join_on 指定关联字段（取子数据集里与父行同值的行），否则本格取不到值

注释里写明了理由（`engine.rs:912-913`）：**按行号硬凑会产出「看着正常其实错」的数据，
那比出空危险得多**。这是本项目一贯的取舍——**不对称成本**：
少一行是可见的，错一行是不可见的。

#### 后代注册

```rust
fn register_descendants(&mut self, idx: usize) {
    let mut p = self.insts[idx].parent;
    while let Some(pi) = p { self.insts[pi].descendants.entry(pos).or_default().push(idx);
                             p = self.insts[pi].parent; }
    let mut cp = self.insts[idx].col_parent;
    while let Some(ci) = cp { self.insts[ci].descendants.entry(pos).or_default().push(idx);
                              cp = self.insts[ci].col_parent; }
}
```
`engine.rs:1576-1588`

**两条链都要登记**，这是「跨层小计」和「交叉表列向小计」的前提：
值单元格既属于某个行分组，也属于某个列分组，只有两条链都登记，
`B3[B2:+0].sum()` 这类横向层次坐标才能取到该列下的全部值（`engine.rs:1571-1575`）。

### 4.4 阶段 2：惰性求值 + 依赖传播 + 不动点

#### 为什么不能单遍扫描

早期实现按实例下标 `0..n` 单遍扫描，问题在于 `value_expr` 引用另一个 `value_expr` 格时
结果取决于创建顺序：**被引用格下标更大就会读到 Null**
（`engine.rs:1143-1145`）。既不报错也不崩溃，只在特定模板下**静默算错**。

#### `ensure_value`：递归 + 环检测

```rust
fn ensure_value(&mut self, i: usize) {
    if self.insts[i].evaluated { return; }
    if self.insts[i].evaluating {            // ← 环检测
        if !self.insts[i].cycle_warned {
            self.insts[i].cycle_warned = true;
            self.warnings.push(format!("{pos} 的表达式存在循环引用…"));
        }
        return;
    }
    self.insts[i].evaluating = true;
    if let Some(agg) = self.insts[i].agg { … self.aggregate(i, agg) … }
    else if let Some(expr) = self.insts[i].value_expr.clone() {
        match expr::parse(&expr) {
            Err(e) => self.warnings.push(format!("{pos} 的 value_expr 无法解析（{e}），已保留展开值")),
            Ok(ast) => { self.ensure_deps(&expr, &ast, i);
                         let v = self.eval_ast(&ast, i, i).into_json(self);
                         self.insts[i].value = v; }
        }
    }
    self.insts[i].evaluating = false;
    self.insts[i].evaluated = true;
}
```
`engine.rs:2037-2076`

**成环时必须告警**（`engine.rs:2034-2036`）：不挂不崩、但也不给任何提示，
用户看到的就是几个空格子，和自己写错了表达式完全对不上号——属于最难排查的那类静默失败。
同一格在一次展开里可能反复被撞上，所以 `cycle_warned` 只报一次。

解析失败**保留展开值**、不因一处坏表达式拖垮整张表，但**必须告警**——
否则用户只看到一个空单元格，无从下手（`engine.rs:2057-2058`）。

#### 不动点迭代

```rust
fn evaluate_to_fixpoint(&mut self, n: usize) {
    const MAX_ROUNDS: usize = 4;
    for round in 0..MAX_ROUNDS {
        self.pos_index.borrow_mut().clear();
        self.deps_done.borrow_mut().clear();
        self.resolve_cache.borrow_mut().clear();
        self.coord_nums_cache.borrow_mut().clear();
        self.coord_prefix_cache.borrow_mut().clear();
        for i in 0..n { self.ensure_value(i); }
        let changed = self.compute_tests(n, round == 0);
        if !changed { break; }
        if round >= 1 { self.warnings.push(format!("row/col_test 第 {} 轮仍在翻转…", round + 1)); }
        if round + 1 == MAX_ROUNDS { break; }
        for inst in self.insts.iter_mut() { inst.evaluated = false; inst.evaluating = false; }
    }
}
```
`engine.rs:1740-1781`

**为什么要求不动点**（`engine.rs:1147-1149`）：

> 求值必须和 row/col_test 一起迭代到稳定：测试可以引用聚合结果（「小计为 0 的分组不显示」），
> 而聚合又要跳过被测试删掉的格——两者互相依赖，单遍算完再删就会留下
> 「明细里没有、合计里还在」的**脏数**。

`MAX_ROUNDS = 4`。第 0 轮的 `changed` 只是「首次定下谁被删」，属正常流程；
第 1 轮起还在翻转，才说明条件依赖了会随删除而变化的聚合值，
此时结果取决于最后一轮，**值得提示**（`engine.rs:1764-1766`）。

**缓存的正确性完全依赖这条时序**（`engine.rs:1747-1751`）：
`hidden` 只在每轮末尾的 `compute_tests` 里变，而缓存每轮开头清空 →
「清空 → 求值整轮 → 改 hidden」之间结果集恒定。

#### `compute_tests`：沿主格链传递

```rust
let mut hidden = vec![false; n];
for i in 0..n {
    let own = !self.insts[i].row_test_passed || !self.insts[i].col_test_passed;
    let inherited = self.insts[i].parent.is_some_and(|p| hidden[p])
                 || self.insts[i].col_parent.is_some_and(|p| hidden[p]);
    hidden[i] = own || inherited;
}
let changed = self.insts.iter().zip(hidden.iter()).any(|(x, h)| x.hidden != *h);
```
`engine.rs:1795-1806`

主格被删 → 子格跟着删（整行 / 整列删除的语义）。父格下标恒小于子格，**单遍升序即可**。

`test_result`（`engine.rs:1594`）解析失败按**保留**处理并告警：
**静默删行比多留一行危险得多**——前者是丢数据且无从察觉，后者只是排版难看（`engine.rs:1592-1593`）。

### 4.5 阶段 2.5：展示文本三值

兜底顺序 `formatExpr` > `dict` > `NumFmt`（`compute_display` `engine.rs:2083-2116`）。

**只覆盖文本，不动 `value`**（`engine.rs:2080-2082`）：
xlsx 仍导出原始数值和数字格式串。所以字典把 1 显示成「是」时，Excel 里那个格子还是数字 1。
这是「展示值」语义的固有取舍，好处是导出后仍可再做透视 / 计算。

- `format_expr`：可用 `value` 指代本格的值，如 `IF(value >= 1000, "大额", "小额")`。
  它可能引用别的格，所以先 `expr_deps` + `ensure_value` 把依赖求值到位（`engine.rs:2093-2096`）。
  解析失败**回落**到 dict / 数字格式并告警。
- `dict`：键取**未套数字格式**的原始文本（`display(value, None)`）——
  配 `{"1": "是"}` 时不该被千分位 / 小数位干扰（`engine.rs:2110-2111`）。命中不了就回落。

### 4.6 阶段 3：行布局

```rust
let roots = self.roots.clone();
let total_rows = self.layout_group(&roots, 0).max(1);
```
`engine.rs:1164-1165`

#### `place`：递归，返回子树占用的物理行数

```rust
fn place(&mut self, idx: usize, offset: usize) -> usize {
    self.insts[idx].dropped = false;         // 落位即视为未被删除
    self.insts[idx].row_start = offset;
    if children.is_empty() || self.layout_depth >= MAX_LAYOUT_DEPTH {
        self.insts[idx].row_span = 1; return 1;
    }
    let parent_col = self.insts[idx].tpl_col;
    let first_row = children.iter().map(|c| self.insts[*c].tpl_row).min().unwrap_or(0);
    let collides = children.iter().any(|c| self.insts[*c].tpl_row == first_row
                                        && self.insts[*c].tpl_col == parent_col);
    let own = if collides { 1 } else { 0 };
    self.layout_depth += 1;
    let used = self.layout_group(&children, offset + own).max(1);
    self.layout_depth -= 1;
    self.insts[idx].row_span = if own == 1 { 1 } else { used };
    own + used
}
```
`engine.rs:1819-1857`

三个关键点：

1. **`merge_down` 不参与布局推进**（`engine.rs:1824-1826`）：它是显示属性（纵跨多级表头里
   已经存在的那几行），而那些行本来就是独立模板行，布局已经分开算过了。
   在这里再 `.max(merge_down+1)` 会给多级表头**凭空多出一行**。
   `row_span` 只用于输出 `rowspan`，不参与布局推进（`engine.rs:1854`）。
2. **父格锚在「第一组子格」那一行上**（`engine.rs:1833-1835`）：横向主从 `华东 | 37,900`
   就是这个行为——父格与子格不同模板行也照样同行。
3. **同格冲突时整组下移一行**（`engine.rs:1837-1839`）：如果第一组子格里**有跟父格同列的**，
   同格落位会把父格整个盖掉——上下堆叠的主从（`A1`=地区 / `A2`=城市，都在 A 列）正是这种形状，
   地区名会**静默消失**。这时 `own = 1`，把父格那一行让出来，同时父格不再纵跨子树
   （否则下面的子格会被盖进父格的合并区）。

#### `layout_group`：三条规则

```
不同模板行        → 顺序占行
同一模板行，不同列 → **共享**同一段行（小计行的 B4 与 D4 必须同行）
同一模板行同一列   → 顺序占行（B3 展开出的 上海 / 杭州 / 南京）
```
`engine.rs:1859-1913`

```rust
let items = items.iter().filter(|c| !self.insts[*c].hidden).collect();   // 读 hidden，不重新求值
// 按 tpl_row 分连续组 → 组内按 tpl_col 分连续组
let share_row = group.len() > 1
    && (self.insts[group[0]].col_expand || self.insts[group[0]].col_parent.is_some());
```
`engine.rs:1867`、`engine.rs:1894-1895`

**读的是 `hidden`（阶段 2 已算好并沿主格链传递），不重新求值**：
求值要用到最终 hidden 集合，布局必须跟它看到同一份结果（`engine.rs:1864-1866`）。

### 4.7 阶段 3.5：列布局

`layout_columns`（`engine.rs:1922-1985`）以 `col_parent` 为边递归分列，返回总列数。

```
无 col_parent 的实例 → 直接落在模板列上；其中列展开格按展开顺序依次占列
有 col_parent 的实例 → 落在主格的列区间内，按 (tpl_row, 行主格) 分组顺序排
不同 (tpl_row, 行主格) 的分组 → **共享**同一段列（交叉表的多个数值列并排）
声明了 col_after 的单元格 → 列号不能写死，统一延后到**第二遍**
```
`engine.rs:1915-1921`

**第一遍**：根实例按 `pos` 稳定分组（同一 pos 的实例在创建时必然连续），
`col_after` 的整组推入 `deferred`；其余从模板列起依次占列（`engine.rs:1938-1961`）。

**第二遍**：`col_after` 目标格此时已定列，取其列区间末尾作为起点（`engine.rs:1963-1983`）：

```rust
let target = self.insts[group[0]].col_after.clone().unwrap_or_default();
let mut after = self.insts[group[0]].tpl_col;
for i in 0..n {
    if self.insts[i].pos == target {
        after = after.max(self.insts[i].col_start + self.insts[i].col_span.max(1));
    }
}
```

用途（`model.rs:298-301`）：「行合计」这类列数随数据变化的单元格——它不能写死模板列号，
否则会被列展开出来的列覆盖。

**整列删除**：`hidden` 的实例标记 `dropped` 并递归带走列子格
（`mark_col_dropped` `engine.rs:1810-1816`），否则它们会以默认列号 0 冒出来。

#### `assign_col`：子格按 (tpl_row, 行主格) 分组

```rust
let mut index: HashMap<(usize, Option<usize>), usize> = HashMap::new();
for k in kids {
    let key = (self.insts[k].tpl_row, self.insts[k].parent);
    match index.get(&key) {
        Some(&p) => groups[p].push(k),
        None => { index.insert(key, groups.len()); groups.push(vec![k]); }
    }
}
```
`engine.rs:1988-2020`

**用哈希索引代替 `keys.iter().position(..)`**——列子格多时那是平方级（`engine.rs:1996`）。
跨行分支的同列单元格必须落在同一列，所以分组键里带 `tpl_row`。

### 4.8 阶段 4：填网格 + 图表反查

```rust
let ncols = total_cols.max(1);
let mut grid: Vec<Vec<Option<GridCell>>> = vec![vec![None; ncols]; total_rows];
for (i, inst) in self.insts.iter().enumerate() {
    if inst.dropped || inst.row_start >= total_rows || inst.col_start >= ncols { continue; }
    let (mut text, num) = display(inst.value.clone(), inst.format.as_ref());
    if let Some(Some(t)) = self.fmt_text.get(i) { text = t.clone(); }
    …
    // 占用检查：这一格已被先创建的实例写过 → 记账、丢弃本实例，不再静默覆盖
    if let Some(prev) = grid[inst.row_start][inst.col_start].as_ref() {
        collisions.push((inst.row_start, inst.col_start, prev.pos.clone(), inst.pos.clone()));
        collided.push(i);
        continue;
    }
    grid[inst.row_start][inst.col_start] = Some(GridCell { … });
}
```
`engine.rs:1179-1295`

填格时依次做：

1. **文本**：`display(value, format)` 出 `(text, raw_number)`，被 `fmt_text` 覆盖（阶段 2.5）。
2. **公式**：`export_formula` 为真时 `excel_formula(i)` 翻译成 Excel 公式；
   翻不出来**回落写值并告警**（`engine.rs:1189-1196`）——静态值不会算错，静默丢公式才难查。
3. **合并规则**（`engine.rs:1197-1203`）：
   ```
   colspan = merge_to_end ? (总列数 − 起始列) : max(列子树跨度, merge_across + 1)
   rowspan = max(行子树跨度, merge_down + 1)
   ```
4. **图片**：`from: value` 时拿本格算出来的值当 src；解析不了**把原因挂到 text 上**并告警——
   留白看不出是「没配」还是「配错」（`engine.rs:1204-1216`）。
5. **条码**：内容来自**本格自己**，所以能就地编码，不用像图表那样等整个网格填完
   （`engine.rs:1222-1263`）。
6. **样式 + 条件格式**（`engine.rs:1277-1288`）：
   ```rust
   style: apply_conditional(
       inst.style.clone(),
       conds.get(&(inst.tpl_row, inst.tpl_col)).map(|v| v.as_slice()).unwrap_or(&[]),
       as_number(inst.value.clone()),
   )
   ```
7. **图表留空**：`chart: None`，由末尾的 `resolve_charts` 补（`engine.rs:1290-1292`）。

末尾三件收尾（`engine.rs:1297-1355`）：
- **落位冲突上报**：`collided` 里的实例标 `dropped`，冲突逐条告警
  （上限 20 条，超出只报总数）—— 见 §14.6 缺陷 ①；
- `var_misses` → 告警「表达式引用了变量「{name}」，但它从没被 assign 赋值过」；
- `unsupported` → 告警「写法不受支持」（如 `ACCSUM` 接过滤表达式）。

`grid` 转成 `Vec<Vec<GridCell>>`（空洞填 `empty_cell`），再调 `resolve_charts`（`engine.rs:1308-1315`）。

---

## 5. 表达式语言与层次坐标

### 5.1 语法（`expr.rs`）

词法 + 语法分析是**纯语法层，不依赖引擎状态**（`expr.rs:1`）。手写的**无扫描器递归下降**解析器，
直接跑在字节游标上（`Parser { s: &[u8], i: usize }` `expr.rs:173-176`）。

```text
cmp     := add (('>=' | '<=' | '==' | '!=' | '>' | '<') add)*
add     := mul (('+' | '-') mul)*
mul     := unary (('*' | '/') unary)*
unary   := '-' unary | primary
primary := number | string | '(' cmp ')' | call | cellref
call    := IDENT '(' [cmp (',' cmp)*] ')'
cellref := IDENT [ '[' COORD ']' ] [ '.' ( FUNC '(' ')' | 'expandIndex' ) ]
```
`expr.rs:3-13`

**全部左结合**，无 `&&` / `||`，无幂运算。
注意 `^` / `>` **不是表达式语言的语法**——它们只出现在 `import.rs` 里标记行 / 列展开
（`=^ds1.city` / `=>ds1.city`，`import.rs:8-19`）。

`Coord` 解析（`Coord::parse` `expr.rs:41-55`）：

| 写法 | `position` | `relative` | `reverse` | 含义 |
| --- | --- | --- | --- | --- |
| `B3` | `None` | `false` | `false` | 当前主格的后代 |
| `B3:1` | `Some(1)` | `false` | `false` | 绝对第 1 个 |
| `B3:+0` | `Some(0)` | `true` | `false` | 相对当前（`+` 号强制 relative） |
| `B3:-1` | `Some(-1)` | `true` | `true` | 相对前一个（负数强制 relative + reverse） |

> 上游注释里那句「`:+0` 不可省略（省略会解析为空集）」在这里的体现是：
> 裸 `B3` 走「取 anchor 的全部后代」，而 `B3:+0` 走「相对偏移 0」——
> 两条分支结果常常相同，但语义路径不同（`engine.rs:3405-3406` vs `engine.rs:3387-3403`）。

### 5.2 AST：13 个变体

`Expr`（`expr.rs:78-119`）共 **13** 个变体：
`Num` / `Str` / `Cell` / `Var` / `Call` / `SelfValue` / `Binary` / `Cmp` / `Neg` /
`Filter` / `Dollar` / `Array` / `Lambda`。

其中两个需要解释：

- **`Filter`**：`POS[COORD]{条件}`。条件以**候选格**为上下文求值（润乾语义）：
  裸 `B2` 指候选格的 B2 主格。这是坐标定位不到时唯一能精确定位的手段，如「月份不连续时找去年同月」。
- **`Dollar`**：`$POS`，条件表达式里取**当前格**的主格（裸 `POS` 取的是候选格的主格）。
  润乾经典写法 `C2 - C2[A2:-1]{$B2==B2}` 就靠它区分两个上下文。

**格子 vs 变量靠长相区分**（`is_cell_name` `expr.rs:129-140`）：
**至少一个字母 + 至少一个数字，且只有这两段**（`A1` / `B3` / `AA10`）。
其余裸标识符一律当变量。这是取舍（`expr.rs:126-128`）：
宁可让少数变量名（如 `x1`）不可用，也不要让 `B3` 这种最常见的写法变得不确定。
Lambda 参数名也走同一判据，**解析期就报错**（`expr.rs:116-117`）。

### 5.3 `Val`：五态，`Set` 是设计精髓

```rust
enum Val {
    Num(f64), Str(String), Bool(bool), Null,
    Set  { cells: Rc<[usize]>, pick: Option<usize> },
    List(Rc<[Val]>),
}
```
`engine.rs:433-456`

**`Set` 是 NopReport 的设计精髓**（`engine.rs:429-431`）：

> 层次坐标返回的是**格集**，由使用场景决定当集合遍历（`SUM(D3)`）还是取首格的值
> （参与四则运算 `C4 / C4[B4:-1]`）。这样就不必引入润乾那套区分「单值 / 集合」的 `{}` 语法。

`Set` 的两个字段都有性能理由（`engine.rs:440-449`）：

- **`cells: Rc<[usize]>` 而不是 `Vec`**：结果集常常是**整列**，而每条明细行都要解析一次，
  逐行克隆一份 `Vec<usize>` 就是 O(n²)。实测 `B2 * 2` 在 16 000 行上 226 ms，
  且规模翻倍耗时翻四倍。无层次坐标时结果集与当前格无关，共享同一份即可。
  `cells` **恒为升序**（`by_pos` 与 `descendants` 都是按创建顺序 push），
  所以成员判断用**二分**而不是线性扫。
- **`pick`**：其中「对当前格可见」的那一个（同一主格下的兄弟格，或主格链上的祖格），
  折叠成标量时取它的值——这是 NopReport `getNamedCells` 的可见性语义：
  写 `B2 / B2[A2:-1]` 时，裸 `B2` 必须是**当前行**的 B2，而不是全局第一个 B2。
  **在构造处就算好**：留到 `scalar` 里再判成员，等于每行多扫一遍整列。

`Set` 与 `List` 的分工（`engine.rs:453-454`）：
**`Set` 是「模板里的那些格」，`List` 是「算出来的那些值」**。

折叠规则（`Val::scalar` `engine.rs:471-485`）：
`Set` 取 `pick` 的值（没有则 `Null`）；`List` 取**首元素**（列表参与四则运算没有公认语义，
真要聚合就显式写 `SUM(MAP(...))`）。

比较用的文本口径（`as_text` `engine.rs:509-519`）：
非数值比较走它——分组标签（`"1月"` / `"华东"`）之间比大小、
数字与数字字符串混比（`100` 与 `"100"`）都靠它统一。

### 5.4 层次坐标 → 实例集合

`resolve_raw`（`engine.rs:3338-3409`）是算法核心。**三分支**：

```rust
// ① 找 anchor：先认自己，再沿行主格链，最后走列主格链
if self.insts[cur].pos == cd.pos { anc = Some(cur); }        // ← 先认自己
… while 行链 … if anc.is_none() { … while 列链 … }

// ② position 且非相对 → 绝对第 n 个
if let Some(pos_n) = cd.position {
    if !cd.relative {
        let siblings = parent.map(|pp| insts[pp].descendants[cd.pos])
                              .unwrap_or(by_pos[cd.pos]);     // 从父实例取同级列表
        return siblings.get(pos_n - 1).map(|s| insts[s].descendants[target]);
    }
    // ③ position 且相对 → 用 expand_index 偏移
    if cd.reverse {
        let idx = insts[anchor].expand_index as i64 + pos_n as i64;
        return if idx >= 0 { siblings.get(idx).map(…) } else { Vec::new() };
    }
}
// 默认（含 :+0）取当前主格的后代
self.insts[anchor].descendants.get(target).cloned().unwrap_or_default()
```

**「先认自己」是后补的**（`engine.rs:3344-3346`）：分组格写 `C1[B1:+0] >= 0`
（小计为 0 的组不显示）时，`B1` 不在**自己**的祖先链上，
旧实现一律返回空集 → 条件恒假 → **整行被静默删光**。
自己就是自己最近的同名主格，认了才说得通。

`find_anchor`（`engine.rs:3317-3336`）抽出了同样的祖链逻辑，供 `resolve` 与
`resolve_for_excel` 共用。返回 `None` 表示找不到 coord 指定的同名主格。

**被删掉的格不出现在结果里**（`engine.rs:3241`）：它们既不出表，也不该进合计。

### 5.5 函数集（`eval_call` `engine.rs:2903-3237`）

| 函数 | 语义要点 |
| --- | --- |
| `SUM` `COUNT` `AVG` `AVERAGE` `MIN` `MAX` | 空集口径：COUNT=0、AVG=0、MIN/MAX=NaN（`agg_of_numbers` `engine.rs:591-600`） |
| `AVERAGE` | NopReport 官方名，内部叫 `AVG`。**两个都得认**，否则照官方文档写的模板会**静默出空**（`engine.rs:3009-3011`） |
| `COUNTA` | 数**非空**单元格，与 `COUNT`（只数数字）**不同**。`Numbers.non_null` 与 `count` 是两个字段（`engine.rs:570-572`） |
| `PRODUCT` | 空集为 1.0，与 `iter().product()` 一致 |
| `RANK` | 当前值在一组里的**降序**名次（1 起）。`sorted.partition_point(\|v\| *v <= x)` |
| `IF` / `NVL` | `IF` 只求值命中的那个分支（`engine.rs:2921-2925`） |
| `PROPORTION` | 当前行的值 / 同范围之和；`total == 0` 时返回 `Null` 而不是 Infinity（`engine.rs:3079-3082`） |
| `ACCSUM` | 累计汇总，从第一个实例累加到当前实例位置 |
| `ASSIGN` | `assign("name", 值)` 绑定命名变量，作用域**本 sheet**。**返回被赋的值**，所以能嵌进更大的表达式 |
| `MAP` / `FLATMAP` / `FILTER` / `REDUCE` | 集合函数 + Lambda。这是 NopReport 明确的取向：**引擎不内置数据集知识**，复杂口径由 `map/filter/reduce` 组合出来（`engine.rs:3155-3157`） |

**认不出的函数名 → 告警而不是静默**（`engine.rs:3225-3235`）：

> 出空可以，但**不能静默** —— 出表就是一格空白，作者只会以为「没数据」，
> 而不会想到「这个函数我不认」。

`ACCSUM` 接 `Expr::Filter` 时**不臆造语义**，而是记一笔明确的告警（`engine.rs:3134-3153`）：

> 累计需要「当前实例在集合里的位置」，而过滤后的集合不一定包含当前实例，基准无从确定。

`FILTER` 输入是格集时**保留格实例**（`engine.rs:3185-3200`），
这样 `FILTER(C1[A1:+0], x => x > 100).sum()` 还能接着用格集那套聚合口径。

---

## 6. 性能：六张缓存

这是本项目最「工程」的一层。所有缓存的**正确性依据是同一条时序**：
`hidden` 只在 `evaluate_to_fixpoint` 每轮末尾变，缓存每轮开头清空。

| 缓存 | 键 | 解决什么 | 代码 |
| --- | --- | --- | --- |
| `PosIndex` | 位置名 | 整列的 cells / 前缀和 / 主格索引 / 聚合 | `engine.rs:541`、`engine.rs:2312` |
| `Numbers` | （挂在 PosIndex 上） | SUM/COUNT/AVG/MIN/MAX/PRODUCT/COUNTA/RANK **整族** | `engine.rs:567` |
| `deps_done` | 表达式源码 | 不含层次坐标时依赖集与当前格无关，一轮确保一次就够 | `engine.rs:780` |
| `resolve_cache` | `(target, coord, anchor)` | 带坐标的 `resolve` 结果集 | `engine.rs:799` |
| `coord_nums_cache` | `(target, coord, anchor)` | 带坐标的聚合值（`Rc<Numbers>`） | `engine.rs:817` |
| `coord_prefix_cache` | `(target, coord, 坐标anchor)` | 带坐标的**前缀和** | `engine.rs:828` |
| `excel_resolve_cache` | `(target, coord, anchor)` | 专服务 phase 4（导出公式），顺带缓存引用串 | `engine.rs:786` |

几个值得单说的设计判断：

**① `Numbers` 为什么把 PRODUCT / COUNTA 也算进来**（`engine.rs:561-566`）：

> 它们和 SUM 是**同一族写法**，只是散在 `eval_call` 的不同分支里。缓存若只存 sum/min/max，
> 那两个分支就接不上，于是同一份结果集被每行各重扫一遍——实测 `RANK`/`PRODUCT`/`COUNTA`
> 在 8000 行分别是 301 / 336 / 37 ms（平方曲线），而 SUM 那一支已经是 2.5 ms。
> 与其为每个函数单开一份缓存（键、失效点都要各自维护，迟早走偏），
> 不如让**一个** `Numbers` 同时喂饱整族。

**② `coord_prefix_cache` 的第一版是错的**（`engine.rs:824-827`）：

> 第一版就是塞进了键（`(…, 坐标anchor, targetanchor)`）：值对了，但键逐行不同 →
> **永不命中** → 还是每行扫一遍集合，8000 行 149 ms，曲线照旧是平方。
> 现在每行只剩 `resolve`(O(1)) + 二分找位置(O(log)) + 一次取下标(O(1))。

正确做法是**把逐行的那个维度变成下标**，而不是塞进缓存键。

**③ 只缓存结果集还不够**（`engine.rs:802-806`）：

> 结果集缓存把 `resolve` 降到 O(1)，但拿到集合之后 `aggregate_cells` 仍要逐格克隆
> `JsonValue` 再转 f64 —— N 行共享同一 anchor 时同一份合计照样算了 N 遍
> （实测：只加 `resolve_cache`，8000 行 560 ms → 448 ms，**曲线一点没直**）。

**④ 顺序不能反的借用陷阱**（`engine.rs:2966-2968`、`engine.rs:2986`、`engine.rs:3118-3119`）：
`cached_numbers` 返回的是**活借用**，拿在手上再进 `anchor_number_of`（内部要借 `pos_index`）
会让 **RefCell panic**。所以必须「先取 `Rc`（O(1) 克隆，借用立刻释放），再 `eval_ast`」。

**⑤ 缓存必须排在朴素路径之前**（`engine.rs:3018-3020`）：

> 必须排在 `arg_numbers` 之前：后者本身就是那次 O(n) 的整列扫描，
> 先算它就等于没优化（第一版就踩了这个，1.3 s 只掉到 1.18 s）。

---

## 7. 数字格式与聚合

### 7.1 `NumFmt` → 文本 / Excel 格式串

`NumFmt`（`model.rs:43`）支持 `kind`（数字 / 货币 / 百分比等）、`digits`、`thousands`、`code`。
两条派生：

- `apply_format`（`engine.rs:3559`）→ 显示文本；
- `excel_num_format`（`engine.rs:3630`）→ Excel 数字格式串，写进 `GridCell.num_format`。

**「展示值」与「原值」分离**：`display()` 同时返回 `(text, Option<f64>)`
（`engine.rs:3545`），`raw_number` 保留原始数值。条件格式比的是 `raw_number` 而**不是** `text`——
后者已套过 `1,234.50` / `12%` 之类的显示格式，拿它反解会算错（`model.rs:376-377`）。

`empty_cell()`（`engine.rs:3683`）给网格空洞兜底。

### 7.2 聚合口径只有一份

`agg_of_numbers`（`engine.rs:591-600`）是 SUM/COUNT/AVG/MIN/MAX/PRODUCT 的**唯一**口径：
`cached_col_agg`、`cached_coord_agg` 和 `eval_call` 里的分支都走它，
避免三处各写一遍而慢慢走偏（`engine.rs:584-587`）。

> 注意 `"counta"` **不**走这里：它要的是 `non_null`，与 `count` 语义不同，
> 混进一个 match 里迟早有人把两者当同一个。

`Numbers` 的 `sorted` 是**升序副本**，`RANK` 的「比我大的有几个」退化成
`sorted.len() - sorted.partition_point(|v| *v <= x)`（`engine.rs:2976`）。

---

## 8. 非文本格子（图片 / 图表 / 条码）

### 8.1 优先级：收成**一个**判据

```rust
pub enum Graphic<'a> { Image(&'a str), Chart(&'a ResolvedChart), Barcode(&'a ResolvedBarcode), None }

impl GridCell {
    pub fn graphic(&self) -> Graphic<'_> {
        if let Some(src) = self.image.as_deref() { return Graphic::Image(src); }
        if let Some(ch) = self.chart.as_ref()      { return Graphic::Chart(ch); }
        if let Some(bc) = self.barcode.as_ref()    { return Graphic::Barcode(bc); }
        Graphic::None
    }
}
```
`model.rs:577-601`

**优先级：图片 > 图表 > 条码 > 文本。**

**为什么要有这个东西**（`model.rs:563-575`）：原先三个渲染端
（`to_html`、`decode_images`、`write_charts`）**各自写了一遍**优先级判断，于是必然分叉：

- `to_html` 写了「图片 > 图表 > 文本」，条码加进来时得再想一遍；
- `decode_images` 只看图片和条码，`write_charts` 只看图表 →
  **「图片 + 图表」的格子会在 Excel 里同时嵌一张图**和一张图表，两个东西叠在同一格上
  （这是加条码时才发现的，**之前一直存在**）；
- 告警里说的优先级是第三份。

本项目在「预览与导出两套口径」上吃过亏（表头行数那次，静默不一致），
所以这里把它收成**一个判据**：**引擎决定，渲染端只画**。

引擎侧还做了一件事（`engine.rs:1229-1242`）：**被盖住时连编都不编**——
省一次编码是次要的，主要是让「`GridCell.barcode` 有值 ⟹ 它就是要画的那个」这个不变量恒成立，
渲染端因此不用再判一遍优先级。

### 8.2 图表：模板坐标反查

**为什么数据来源写「模板位置名」而不是输出行列**（`model.rs:605-612`）：

> 图表要画的是**展开之后**的数据（3 个地区 → 3 根柱子），而作者写模板时只知道模板坐标
> （`A3` = 地区列）。`GridCell.pos` 保留了模板坐标，所以「`A3` 展开成了哪几个输出格」
> 是可以反查的——图表因此能在**服务端**解析出真实数据。这一条是整个特性成立的前提。

反查算法（`chart.rs`）：

1. `referenced_positions(decl)`（`chart.rs:76`）收集声明里提到的所有位置名
   （categories + 每条 series 的 `from`）；
2. 建 `ChartIndex`：位置名 → 按 `(行, 列)` 排序的输出格列表；
3. `resolve_chart`（`chart.rs:140`）把声明变成 `ResolvedChart`。

**取值顺序**（`chart.rs:55` `source_number`）：优先 `raw_number`，取不到再把 `text` 解析成数字。
两者都没有（空串 / `-` / 纯文字）就是**缺测**，结果是 `null`——**不补 0**
（`model.rs:655-659`）：

> 「空着」和「就是 0」在图上长得一样，在报表里是两回事。
> 交叉表里「某地区某月没有数据」是常态，所以缺测必须是一等公民而不是报错。

**三条硬约定**：

| 约定 | 理由 |
| --- | --- |
| **一个声明只画一份** | 图表格所在行会跟着别的格子展开，同一个 `pos` 在网格里出现 N 次。图片复制 N 份是对的（一列产品图），图表复制 N 份是错的——它读的是**整列**的数据，N 份内容一模一样，落到 Excel 就是 N 张图叠在同一格上。只认最上/最左那份（`engine.rs:1323-1326`）。⚠️ **代价是「每组一张图（数据各不相同）」做不到** —— 去重按 `pos`，所有分组实例共享同一个 `pos`，只剩第一份；数据索引也按 `pos` 收集，于是那一张图会把**所有组**的类目都收进去。见 §14.6 缺陷 ③ |
| **空值是空档不是 0** | 见上 |
| **图表是导出那一刻的快照** | 数据写死在图表里，改表体的数不会跟着变（`xlsx.rs:343`） |

失败**不中断渲染**：该格文本改成 `[图表: 原因]` 并进 `warnings`，表照常出
（`engine.rs:1319-1321`）。`resolve_chart` 的错误都**点名**：count 不匹配时报出两个数
（`chart.rs:356`）、未知位置名报出名字（`chart.rs:368`）、自引用有专门文案（`chart.rs:376`）、
未知类型被拒并点名（`chart.rs:384`）。

图表类型表 `KINDS = ["bar","line","pie"]`（`chart.rs:30`），
默认 `bar`（`chart.rs:33`），`pie` 只允许一条序列（`chart.rs:418`）。

### 8.3 条码：全自研，零依赖

`barcode.rs` **1 662 行、44 条测试**，全仓测试最密的模块。

```rust
pub const SYMBOLOGIES: [&str; 2] = ["qr", "code128"];   // barcode.rs:40
const DEFAULT_SYMBOLOGY: &str = "qr";                    // barcode.rs:46
const MAX_QR_BYTES: usize = 213;                         // barcode.rs:52
const MAX_CODE128_BYTES: usize = 48;                     // barcode.rs:59
pub const PX_PER_MODULE: usize = 3;                      // barcode.rs:69
const BAR_ROWS: usize = 25;                              // barcode.rs:76
const QR_QUIET: usize = 4;                               // barcode.rs:79
const BAR_QUIET: usize = 10;                             // barcode.rs:81
```

#### QR：字节模式 + 纠错 M + 版本 1~10

**213 字节的推导**（`barcode.rs:51-52`）：版本 10 的 346 个总码字 × 8 = 2 768 位，
减去 4 位模式指示符、16 位字符计数（版本 10 起是 16 位）剩 1 708 位 → **213 字节**。

**手写表 vs 计算**：

| 项 | 做法 | 位置 |
| --- | --- | --- |
| 纠错块结构 | 手写 `QR_M_BLOCKS: [(usize, &[(usize, usize)]); 10]` | `barcode.rs:224` |
| 总码字数 | 手写 `QR_TOTAL_CODEWORDS: [26,44,70,100,134,172,196,242,292,346]` | `barcode.rs:239` |
| 剩余位 | 手写 `QR_REMAINDER_BITS: [0,7,7,7,7,7,0,0,0,0]` | `barcode.rs:246` |
| 对齐图案中心 | 手写 `QR_ALIGN_POS` | `barcode.rs:249` |
| 版本信息 | 手写 `QR_VERSION_INFO: [0x07C94, 0x085BC, 0x09A99, 0x0A4D3]` | `barcode.rs:264` |
| 格式信息（M 级） | 手写 `QR_FORMAT_M: [u32; 8]` | `barcode.rs:269` |
| **GF(2^8) 乘法** | **计算**，本原多项式 `0x11D` | — |
| **RS 生成多项式** `g(x)=∏(x−a^i)` | **计算** | — |
| **格式信息 BCH(15,5)** | **计算**，`0x537 ⊕ 0x5412` | — |
| **版本信息 BCH(18,6)** | **计算**，`0x1F25` | — |
| **掩码选择 + 四条罚分规则** | **计算** | `barcode.rs:1230`（罚分测试） |

版本 11+ **明确不做**（`barcode.rs:30`）：213 字节已经远超单据 / 标签的实际载荷；
版本一多，手写表的体积和维护成本都不划算。

#### Code128：**不自动切换码集**

支持全表 107 个符号，码集 B / C，需要时用 A；GS1-128 是**显式开关**。

**不自动切换码集是刻意的**：切换是个**启发式**，而错误的启发式意味着
**条码扫不出来**——比多占几毫米坏得多。

`MAX_CODE128_BYTES = 48` 是**版面**上限而不是编码上限（`barcode.rs:57-59`）：

> 48 字节在码集 B 下约 500 个模块，按每模块 3 像素算将近 1500 像素宽，
> 再长就宽到排不进报表列了。

超限**明确报错并点名实际字节数与上限**（`barcode.rs:911-914`）。

**GS1 用显式开关而不是靠内容前缀猜**（`model.rs:502-503`）：

> GS1 的载荷里看不出来「我要不要 FNC1」，猜错了是**条码能扫但内容不对**，最难查。

#### 位矩阵的表示

`ResolvedBarcode.rows: Vec<String>`，每行一个字符串，`'1'` = 黑，**静区已含在内**，
一维码已拉伸成面（`model.rs:525-529`）。

**用字符串而不是 `Vec<Vec<bool>>`**：后者每格序列化出来是 `true,` 五个字符，
而报表**每一行**都可能带条码，一张 65×65 的二维码就是 **5 倍体积差**。
附带好处是 JSON 里肉眼能看出这是个二维码。

#### 输出

- **HTML**：内联 SVG（矢量，缩放 / 打印都清晰）——`barcode_svg::render`（`mod.rs:910`）。
  空 SVG（矩阵为空）时回落显示 `text`，免得整格空白（`mod.rs:911-914`）。
- **xlsx**：**1 位灰度位图**，由自研 PNG 编码器产出（`png.rs`）。

### 8.4 PNG 编码器：非压缩 DEFLATE

`png.rs` 314 行。关键选择（`png.rs:75`、`png.rs:91`）：

- **zlib 头 `0x78 0x01`**（`CM=8, CINFO=7`，最小编码窗口）；
- **DEFLATE 全部用 stored（非压缩）块**（`btYPE=00`）——省掉整个 Huffman 编码器；
- **Adler-32** 校验和；
- **1 位灰度**（`bit depth 1`），条码天然是黑白两色。

跨块的大载荷有测试（`png.rs:278-283`）：`zlib_stored` 处理超过一个 stored 块的载荷，
解回来必须逐字节一致。

### 8.5 图片

`CellImage`（`model.rs:405`）：`from`（`literal` / `value`）+ `src`。

**只收 data URI，故意不做文件路径**（`model.rs:391-393`）：

> 服务端按模板里的字符串读本地文件 = 模板变成一个**任意文件读取原语**
> （`../../.ssh/id_rsa` 这种），而模板是可以被导入 / 分享的。少一条通路少一类洞。

`parse_image_data_uri`（`model.rs:433`）的校验规则（都有测试）：

| 规则 | 理由 |
| --- | --- |
| 只认 png / jpeg / jpg / gif / bmp | 与 `rust_xlsxwriter` 的 `XlsxImageType` 一致（`model.rs:1327-1329`） |
| 元信息大小写 / 前后空格都认 | 前端拼 data URI 时大小写不统一（`model.rs:1345-1350`） |
| 不是 data URI 一律报错，**错误里带原文片段** | 作者贴了个本地路径进来时，得一眼看出是「路径」而不是「图坏了」（`model.rs:1352-1358`） |
| 只收 base64 | 静默当 base64 解会得到一堆乱字节，写进 xlsx 就是「图裂」（`model.rs:1361-1366`） |
| **webp / svg 明确拒绝并点名** | `rust_xlsxwriter` 会**静默**把 webp 转成 PNG，且完全不支持 svg（`model.rs:1369-1376`） |

---

## 9. 条件格式

**语义**（`model.rs:362-384`）：

- **自上而下，第一条命中的生效**（后面的不再看）。顺序是语义的一部分：
  `[{gt 1000 → 红}, {gt 100 → 黄}]` 与反过来写的结果完全不同。设计器里因此显示序号并支持上下移动。
- **只认数值**：比的是**格式化之前的原始数值**（`raw_number`）。
  因此**空值 / 文本 / 布尔格一条规则都不会命中**——它们压根没有数值可比。
  这是刻意选的语义（`model.rs:378-380`）：

  > 「把文本硬解析成数字」会引入「`-` 当 0 用」这类静默错误，
  > 而报表里空着和就是 0 是两回事。

- 只放在 `CellModel` 上（不像 `image` / `chart` / `barcode` 那样两个槽都认）：
  它比的是**本格算出来的数值**，而「算出来的值」只可能来自 `model`（`model.rs:363-367`）。

**编译**（`compile_conditionals` `engine.rs:633-695`）：

- **编译只做一次**（每个模板格一次），而不是每个展开实例一次——
  否则一条坏规则会在告警里重复 N 遍（N = 该格展开出来的行数），**作者根本看不出是同一件事**（`engine.rs:611-612`）。
- 告警里的序号**按人眼从 1 数**，要能直接对上设计器里那一行（`engine.rs:637-638`）。
- **坏规则丢进告警而不是让整表失败**：服务端的失败粒度是**一格**
  （与图片 / 图表 / 条码一致）。但**绝不静默跳过**（`engine.rs:626-628`）。
- **空样式的规则要丢掉**（`engine.rs:630-632`）：

  > 判定是「第一条命中的生效」，留一条命中却什么也不改的规则会把**后面**真正想生效的规则
  > 挡掉。那比报错难查得多，所以宁可丢 + 告警。

**应用**（`apply_conditional` `engine.rs:733`）：逐字段覆盖在作者样式之上，
结果放进 `GridCell.style`（`engine.rs:1284-1288`）。

**样式是唯一的样式通道**：条件格式**不另开渲染路径**，只是在填格那一步决定把哪份样式放进槽里
（`engine.rs:1281-1283`）。`CellStyle` 支持字色 / 底色 / 粗斜 / 对齐 / 字号；
**刻意不给边框**（Univer 不渲染边框，见项目长期记忆）。
颜色只认 `#RRGGBB`，认不出**两端点都报 400**。

`html_style_attr`（`mod.rs:693`）把 `CellStyle` 渲成 `<td style="…">`；
**无样式时输出逐字节不变**（`mod.rs:921-923`）。

---

## 10. 导出四端

### 10.1 HTML（`to_html` `mod.rs:803-937`）

这是**预览与导出共用**的一路，也是唯一能直接看的。

#### `@page`：只吐作者明写的项

```rust
if setup.paper.is_some() { decls.push(format!("size:{:.2}mm {:.2}mm", w, h)); }
else if setup.landscape { decls.push("size:landscape".to_string()); }
if let Some(m) = setup.margin_mm { decls.push(format!("margin:…")); }
```
`mod.rs:813-829`

三条约定：

- **`@page` 是文档级规则**（不像 xlsx 那样每 sheet 一份），所以整篇只能有一份纸张设置。
  多 sheet 纸张不一致时取第一份并在 `render()` 里告警（`check_paper_consistency` `mod.rs:765`）。
- **没有任何 sheet 配页面设置时一个字都不吐**——输出与加这个功能之前**逐字节一致**（`mod.rs:808`）。
- **只吐作者明写的项**：`size` 要写了纸张才吐；只写了方向时吐 CSS 的裸 `landscape` 关键字
  （**纸交给打印机定**），**不替作者把纸钉成 A4**；`margin` 要写了页边距才吐。
  否则「只配了个页码」的模板会连纸张和页边距一起被钉死，而作者从没要求过（`mod.rs:810-812`）。

#### 页码：服务端烘焙

```rust
let body_mm = (s.height_mm - m.top - m.bottom).max(1.0);
out.push_str(&format!("<div style=\"min-height:{body_mm:.2}mm;display:flex;flex-direction:column\">\n"));
out.push_str(&format!("<div style=\"margin-top:auto;text-align:center;font-size:10pt\">{}</div>\n", escape(text)));
```
`mod.rs:839-853`

**为什么要在服务端烤**：PDF 走 Chrome CLI（`--print-to-pdf`），它**不支持 `@page` 的 margin box**
（`@bottom-center` 那套），吐不出页眉页脚。
所以做法是「纸高容器 + `margin-top:auto` 把页脚顶到底」。

作者没写页边距时按 `DEFAULT_MARGINS` **估**——这里只是**算落点**，
不是替作者声明页边距（那件事由 `@page` 的 `margin` 决定）（`mod.rs:842-844`）。

**「服务端知不知道共几页」是一个显式信号**（`mod.rs:236-239`）：

```rust
// 没真正分页时不传页码 —— `page_no` 就是「服务端知道共几页」这个信号，
// 传 `None` 等于承认不知道，于是 `to_html` 不印页脚。
let pno = if *effective { Some((*i, *n)) } else { None };
```

**多 sheet 时页码各算各的**（`mod.rs:213-215`）：`all_pages` 是把所有 sheet 的页拍平在一起的，
用扁平下标当页码会把 B 表的第一页印成第 4 页。

#### 表格与合并

```rust
let mut covered = vec![vec![false; ncols]; nrows];
for (r, row) in sheet.rows.iter().enumerate() {
    for (c, cell) in row.iter().enumerate() {
        if covered[r][c] { continue; }
        // 标记被本格 rowspan/colspan 覆盖的格子
        …
    }
}
```
`mod.rs:866-883`

**被合并覆盖的格子必须从 HTML 里省略**：`rowspan`/`colspan` 只写在起始格上，
其余格子若照常输出 `<td>` 会让每一行的列数偏移
（Univer / Excel 用 `mergeData` 表达，没有这个问题）。

#### 内联 SVG

```rust
let inner = match cell.graphic() {
    Graphic::Image(src)   => format!("<img src=\"{}\" alt=\"{}\" …>", escape(src), escape(&alt)),
    Graphic::Chart(ch)    => crate::report::chart_svg::render(ch),
    Graphic::Barcode(bc)  => { let svg = barcode_svg::render(bc); if svg.is_empty() { escape(&cell.text) } else { svg } }
    Graphic::None         => escape(&cell.text),
};
```
`mod.rs:895-920`

**服务端画 SVG 而不是指望前端**：`to_html` 产出的是**自包含文档**，
里面没有 JS，也没法回头调设计器（`mod.rs:888-889`）。
图表 SVG 默认 `viewBox` 宽 480（`chart_svg.rs:29`）。

### 10.2 xlsx（`to_xlsx` `xlsx.rs:443`）

#### 列宽：单位是**字符**，不是 mm

```rust
const MAX_DIGIT_WIDTH: f64 = 7.0;      // xlsx.rs:89
const CELL_PADDING: u32 = 5;           // xlsx.rs:90
fn col_pixels(chars: u16) -> u32 {     // xlsx.rs:92
    (f64::from(chars) * MAX_DIGIT_WIDTH).round() as u32 + CELL_PADDING
}
fn chars_from_pixels(px: f64) -> f64 { (px - 5.5) / MAX_DIGIT_WIDTH }   // xlsx.rs:103
```

**没有 mm ↔ 列宽的换算**（mm 只用于页边距，`MM_PER_INCH = 25.4` `model.rs:758`）。
列宽由内容宽度决定（`display_width` `xlsx.rs:779`），
夹在 `MIN_COL_WIDTH = 8`（≈61px，`xlsx.rs:718`）与 `MAX_COL_WIDTH = 60`（`xlsx.rs:731`）之间
——**图表列有额外下限**，否则一张 600px 宽的图表缩成 61px，「功能看着就是坏的」（`xlsx.rs:251`）。

行高是另一套：`row_pixels(pts) = round(pts × 4/3)`（`xlsx.rs:108-109`）。

#### 原生图表 + 隐藏辅助列

**为什么数据要写到表格下方**（`xlsx.rs:333-338`）：

> 图表的来源写的是**模板坐标**（`A3`），它展开出来的输出格**可能不连续**——
> 纵向分组报表里同一个 `B3` 会出现在第 0、3、6 行。而 Excel 原生图表的一条序列
> 只能是**一个连续区域**，所以：把**解析好的数**写到表格下方的隐藏列里，再让图表引用那个块。

```rust
let helper_col: u16 = col_count + 1;              // xlsx.rs:375
ws.write_string(cursor, helper_col + 1 + s, &series.name)   // 序列名（图例从这里取字）
ws.write_string(cursor + 1 + i, helper_col, cat)             // 类目
…
ws.set_column_hidden(helper_col + k)              // xlsx.rs:430
```
`xlsx.rs:375-430`

图表锚在**作者放图表格的那一格**上（图表浮在网格之上，这是 Excel 图表的固有行为，`xlsx.rs:421`）。

**为什么用原生图表而不是嵌位图**（`xlsx.rs:349-350`）：

> 那样图表在 Excel 里就是一张**死图**：改不了数据、换不了图型、缩放会糊。
> 原生图表是矢量的、可编辑的，这也是选它的全部理由。

代价是**图表是快照**（`xlsx.rs:343`）：在 Excel 里改表体的数字，图不会跟着变。

#### 重复表头与打印设置

`repeat_rows` 由 `xlsx_header_rows` 决定（`mod.rs:1038-1044`）：

```rust
tpl.sheets.first().and_then(|s| s.page.as_ref())
    .filter(|p| p.rows_per_page > 0)                                  // ← 真开分页了吗
    .map_or_else(|| tpl.header_row_count().max(1), |p| p.repeat_header_rows.max(1))
```

**`.filter(|p| p.rows_per_page > 0)` 这一句是关键**：`page.is_some()` **不等于**开了分页
（页面设置也挂在同一个 `PageConfig` 上）。只配纸张时若走 `repeat_header_rows` 分支，
会把「2 行表头」静默变成「1 行」。

`header_row_count`（`model.rs:1071-1087`）与 TS 侧 `headerRowCount`
（`openprint/src/report/grid-report.ts`）是**同一套判据**：
从第一行起，连续「既没有纵向展开格、也没有主格」的行（`row_parent: ""` 也算没主格）。
典型值：生成器模板第一行是标题、第二行才是列头 → **2**；双指标交叉表 → **3**。

#### 页面设置

每 sheet 的 `pageSetup` / `pageMargins` / `oddFooter` 由 `ResolvedPageSetup` 驱动。
页码转 Excel 页脚码（`page_number_excel_footer` `model.rs:884-890`）：

```rust
tpl.replace('&', "&&").replace("{page}", "&P").replace("{pages}", "&N")
```

**顺序不能反**（`model.rs:881-883`）：

> **字面量 `&` 必须先翻倍成 `&&`**（Excel 的约定：`&&` 才印出一个 `&`）。
> 顺序不能反——先替换再转义的话，刚写进去的 `&P` 会被翻成 `&&P`，
> 于是页脚印出字面量「&P」而不是页码，而且**一点报错都没有**。

**B5 = JIS 182×257**（Excel 纸张码 13，`model.rs:772-781` 的 `PAPERS` 表）。

### 10.3 CSV（`to_csv` `csv.rs:37`）

- **RFC 4180** + **UTF-8 BOM** + **CRLF**（Excel 打开中文不乱码的前提）；
- **写 `text` 不写 `formula`**；
- **CSV 注入（`=cmd|`）刻意不改写**——加 `'` 会改掉 `+86` 手机号。

### 10.4 docx（`to_docx` `docx.rs:316`）

**手写 OOXML**，配**只写 method 0（存储，不压缩）**的最小 ZIP（`zip.rs:52-66`）。
只有 4~5 个很小的 XML part，压缩收益几乎为零，而不压缩就不需要 DEFLATE 编码器
（`zip.rs:7`）。CRC-32 与 PNG 共用。

**为什么「失败是全有全无」**：Word 遇到结构性错误是**整个文件打不开**，
不是「少一段」——所以本机没装 Word / WPS 时，「Word 能打开」这件事**验不了**，
只能用四条**可判定不变量**替代（见 §15）。

---

## 11. 分页（`paginate` `mod.rs:412-471`）

### 11.1 三层配置

```rust
pub struct PageConfig {
    pub rows_per_page: usize,        // 每页容纳的**数据**行数（不含重复表头/表尾）
    pub repeat_header_rows: usize,   // 每页顶部重复的模板行数
    pub repeat_footer_rows: usize,   // 每页底部重复的模板行数
    … 页面设置五个字段
}
```
`model.rs:818-844`

### 11.2 是否真分页：`is_effective`

```rust
pub fn is_effective(&self, total_rows: usize) -> bool {
    self.rows_per_page > 0 && total_rows > self.repeat_header_rows + self.repeat_footer_rows
}
```
`model.rs:895-897`

**为什么单独一个判据**（`model.rs:899-902`）：`is_effective()` 只回答「要不要分页」；
只配了纸张、没配 `rows_per_page` 时，HTML 依然要吐 `@page`（那是 `has_setup()` 的事）。

### 11.3 不切在合并格中间

```rust
pub fn merge_blocked_boundaries(rows: &[Vec<GridCell>]) -> Vec<bool> {
    let mut span = vec![0usize; n - 1];
    for (r, row) in rows.iter().enumerate() {
        for cell in row.iter() {
            let k = cell.rowspan.max(1);
            if k <= 1 { continue; }
            let last = (r + k - 1).min(n - 1);
            for b in r..last { span[b] += 1; }        // 用覆盖次数累加，多个合并格重叠时不用逐个判断
        }
    }
    span.iter().map(|c| *c > 0).collect()
}
```
`mod.rs:377-398`

**关键洞察**（项目长期记忆）：**主格展开出来就是合并格** →
「不在合并格中间切页」**等价于**「组内不跨页」。一个机制同时解决了两件事。

落点算法（`mod.rs:429-469`）：

```
剩下的已经够放一页 → 不再切（否则会为了「凑满」硬拆出一张小页）
否则：理想落点 want = start + rows_per_page - 1
     被合并格跨过时**往两边找**（先退后进），页大小贴着 rows_per_page 走，不会一个劲儿往后胀
```

**代价**：页大小不再严格等于 `rows_per_page`，**只会偏多不会偏少**——
一个组本身超过一页时，那一页就是会超，没法两全（`mod.rs:410-411`）。

**边界保护**（`mod.rs:450-459`）：`head_n + j` 必须还是个**存在的行边界**（`<= total-2`）：
表尾为 0 时，最后一行数据就是全表最后一行，它后面没有边界可切，越过去会 **panic**。

### 11.4 多页时公式回落写值

```rust
if n > 1 && grids.iter().any(|g| g.iter().any(|r| r.iter().any(|c| c.formula.is_some()))) {
    warnings.push(format!("[{}] 分页导出时公式坐标按整表生成、与逐页复制后的行号不一致，已回落写值", sheet_name));
}
```
`mod.rs:172-181`

公式是按**整表**的行列位置生成的，逐页复制后行号就对不上了——
第 2 页的 `SUM(C2:C5)` 只会算到本页那几行，跟同一格显示的静态值不一致。
**半对不对的公式比静态值危险**（`mod.rs:169-171`）。

### 11.5 配了页码但没真分页 → 必须说清为什么

```rust
if !effective && page_setup.as_ref().is_some_and(|p| p.page_number.is_some()) {
    warnings.push(format!(
        "[{}] 配了页码但这份模板没真正分页（rows_per_page = {}）：HTML 里印不出页码；\
         导出 xlsx 不受影响（那是 Excel 原生页脚）", sheet_name, cfg.rows_per_page));
}
```
`mod.rs:184-190`

**静默不印的话，作者只会看到「配置明明写了、输出里就是没有」**。

---

## 12. 数据源、参数与循环变量

### 12.1 数据集准备：UNNEST

`prepare_dataset`（`mod.rs:551`）+ `unnest`（`mod.rs:506-549`）。

**把每行某字段上的数组摊成多行**：父行字段 + 数组元素字段合并成一行。
摊平后引擎看到的就是一张**普通扁表**，展开、层次坐标、行父链全走既有逻辑，
**不用给引擎加「实例 ↔ 嵌套路径」这一层**（`mod.rs:500-501`）。

| 元素类型 | 处理 |
| --- | --- |
| 对象 | 字段直接并进这一行 |
| 标量 | 挂到 `field` 这个名字上 |
| **数组为空** | **保留父行**（去掉数组字段），否则该实体会整个消失（`mod.rs:505`） |

两个嵌套数组同时出现时**告警而不是做笛卡尔积**（测试 `two_nested_arrays_warn_instead_of_cartesian` `mod.rs:3436`）。

### 12.2 循环变量

`loop_groups`（`mod.rs:266`）：按 `field` 的**不同取值**把数据集分组，一个取值渲染一张 sheet。

- 这是 NopReport「循环变量出 N 个 sheet」那一层，用来做「一个客户一张表 / 一个部门一张表」；
- 生成的 sheet 名是 `{name} - {取值}`，取值按**首次出现顺序**排列（`model.rs:1047`）；
- **多数据集时**：循环变量在**主数据集**上取值，其余数据集按同名同值跟着筛；
  某个数据集里压根没这个字段就**不筛它**（整份透传），并告警说明（`mod.rs:261-262`）；
- 字段在数据集中不存在时**告警并退回单张表**（不静默出空表，`model.rs:1048`）。

### 12.3 数据源引擎

| 引擎 | 状态 |
| --- | --- |
| sqlite | ✅ |
| postgres | ✅ |
| odbc | ✅（可选 feature，**默认不编**） |
| MySQL / MariaDB / SQL Server / Oracle | **明确报错**（`unsupported_engine_name()`） |

**MySQL 归一成 sqlite，但 UI 下拉只有三项**（`admin.html:407`）——**手改配置才会踩**。
注意 `ServerConfig::load()` **不调 `validate()`**（只有保存 / 试连调），
手改配置写 mysql 会报**误导性的**「sqlite 文件不存在」。

### 12.4 报表参数：两条通道

| 通道 | 位置 | 绑定方式 |
| --- | --- | --- |
| `ReportDef.params` + `RunRequest.values` | 新 | **按名绑** |
| `RunRequest.params`（数据集名 → 位置数组） | 老 | 按位置 |

`resolve_params`（`mod.rs:1337`）/ `bind_params`（`mod.rs:1389`）。

**错误处理**：未知 / 必填缺失 / 引用解析不出值**一律报错**，
且**未知参数检查必须先于必填检查**——否则 typo 会被报成「region 必填」。

---

## 13. HTTP API 面

`main.rs` 共 **33 条路由**（`main.rs:184-224`）：

| 分组 | 路由 |
| --- | --- |
| 页面 / 健康 | `/`、`/admin`、`/health`、`/printers` |
| 打印 | `POST /print` |
| 字体 | `GET /api/fonts`、`/api/fonts/data` |
| 配置 | `GET/PUT /api/config`、`POST /api/config/test`、`GET /api/config/fs` |
| 数据源 | `/api/data/databases`、`/tables`、`/columns`、`/rows`（GET + POST） |
| **报表渲染** | `POST /api/report/render`、`/xlsx`、`/docx`、`/csv`、`/import` |
| 样例 | `GET /api/report/sample`、`sample.xlsx`、`sample.csv`、`sample-template`、`cross-tab-template`、`cross-tab-two-metrics`、`cross-tab-multi-level` |
| **报表文件** | `GET /api/reports`、`PUT /api/reports/save`、`GET/DELETE /api/reports/:id`、`POST /api/reports/:id/run`、`/xlsx` |

### 13.1 `ReportDef`：报表文件

存 `print-server/reports/<id>.json`（**配置文件同级**）。顶层 `format` … `options` 共 10 个字段。
**存的是「模板 + 数据源声明 + 渲染选项」，不是数据快照**（`mod.rs:1072-1077`）。

- **`id` 白名单 `[A-Za-z0-9_-]` ≤ 80** 是安全边界（直接拼文件名）；
- 打开一律 `setMode('free')`；
- 模板**存原样**，`options` 单独存；`withExportFormula` / `withExpandControl` **渲染前才套**
  （自由模板**刻意不套**后者）；
- 样例：`print-server/reports/sales-by-region.json`。

### 13.2 两套错误方言，没有错误码

| 方言 | 形态 | 来源 |
| --- | --- | --- |
| 中文 `text/plain` | `(StatusCode, String)` | 本项目手写的校验 |
| 英文 serde prose + 422 | axum 内建 | 请求体反序列化失败 |

**唯一的例外是 `PUT /api/reports/save` 的 400 / 409** —— 那两个数字不是装饰，
是**契约**：UI 靠它分辨「弹错误」还是「弹覆盖确认框」（见 §13.6）。
其它端点的状态码仍然不可依赖，别照着推广。

`/api/reports` 的 `updatedAt` 是 **UTC**，`/health` 的 `time` 是**本地时间**。

### 13.3 内外层形状不一致

| 端点 | 形状 |
| --- | --- |
| `/api/report/sample-template` | 内层 `template` |
| `/api/reports/:id`、`PUT /api/reports/save` | 外层 `ReportDef`（10 键） |

**`ReportDef` 没有 JSON Schema**（这是架构体检里记下的一条缺口）。

### 13.4 三条「静默失败」红线

1. **报表目录跟着 cwd 走**：`store::reports_dir(config_path)` = 配置文件同级的 `reports/`，
   默认配置路径是**相对**的 `print-server.json`。从仓库根启动 → `/api/reports` 返回 `[]`，
   **无任何报错**。刻意设计，只做披露（横幅 / `/health` / 响应头）。
2. **预览与导出是两套口径**：`buildRenderRequest` 前端算 `headerRows`；
   xlsx 导出在 Rust 侧另算（`ReportTemplate::header_row_count()`）。**不一致是静默的**。
   改一侧要同步另一侧。
3. **`ReportSource` 是 `rename_all = "camelCase"`** → JSON 里是 **`connId`**。
   写成 `conn_id` 被 serde 忽略、**静默落到第一个连接**。

### 13.5 响应里的诊断：`warnings` + `issues`（2026-09-26 起）

§13.2 说 HTTP 层的错误「没有错误码」—— 这一点**仍然成立**（`text/plain` 中文文案）。
但**渲染结果内部**的诊断现在有码了，两者不是一回事：

| 通道 | 形态 | 用途 |
| --- | --- | --- |
| `warnings` | `string[]` | **向后兼容视图**，内容逐字与老版本一致（含 `[sheet] ` 前缀） |
| `issues` | `RenderIssue[]` | 结构化：`level` / `code` / `sheet` / `pos` / `message` |

```jsonc
{
  "sheets": [...], "html": "...",
  "warnings": ["[冲突表] 布局冲突：A1 与 B1 都落在第 1 行第 2 列，…"],
  "issues": [
    { "level": "error", "code": "layout_collision", "sheet": "冲突表",
      "pos": "B1", "message": "布局冲突：…" }
  ]
}
```

**`warnings` 是 `issues` 的派生视图**（`level >= warning`，`[sheet] ` 前缀由 `sheet` 字段补上）——
不是两份各自维护的列表。所以两者不可能漂移，有专门的测试逐条对账。

**为什么前端不要用 `message.contains(..)` 判断严重程度**：文案会改（改得更清楚、
加标点、换措辞），那种判据一改就**静默失效**。用 `code`（词表在 `issue.rs` 有钉子测试，
TS 侧镜像 `IssueCode`）。细节见 §14.7。

### 13.6 覆盖已有报表：两道闸，查的不是同一个事实（2026-09-27 起）

`PUT /api/reports/save` 是**覆盖写**。它自己的文档注释一直写着「报表是用户的资产，
静默覆盖同名文件会丢东西，所以调用方（UI）要先经列表确认」——
而**那句话从来没实现过**：`saveReport` 直接 PUT，一次确认都没有，
手上明明就有 `savedReports` 也没拿它判任何东西。手打一个已存在的 id 点保存，
那份报表就无声无息被换掉（界面上只多一句「已保存模板」）。

现在有两道闸。**它们是互补，不是重复** —— 因为查的**不是同一个事实**：

| | 判据来源 | 挡得住 | 挡不住 |
| --- | --- | --- | --- |
| UI 预判（`saveReport`） | 打开弹窗那一刻的**列表快照** | 手打一个列表里已有的 id | 之后别处新建的同名 |
| 服务端 `store::save_new` | **文件系统**（权威） | 任何调用方，含 curl / 别的客户端 | —— |

契约：

- **目标已存在且请求没带 `?force=1` → `409`**（不是 400，见 §13.2）。
  拒绝是**零副作用**：目标文件逐字节不变、也不留 `.bak` ——
  只断言状态码的话，「先覆盖了再返回 409」这种实现也能过，那比不报错更坏。
- `?force=1` 才穿透。**只认 `1` / `true` / `yes`**（大小写不敏感、忽略两侧空白）；
  `?force=0`、`?force=flase`、`?force`（空值）**一律当没带**。
  语义是「**证明**你知道要覆盖」，不是「提过这事」——
  判错方向的代价是静默覆盖，所以一律往「多问一次」那边倒。
- `force` 走**查询串**，不走请求体：请求体就是 `ReportDef` 本体、要**原样落盘**，
  往里塞 `force` 得靠 `#[serde(skip)]` + TS 侧同步忽略 ——
  那是往「用户资产的文件格式」上开一个只为本次请求服务的口子，迟早会被谁序列化进去。
- **UI 收到 409 必须弹确认框，不是弹错误**：`doSave` 里 409 分支**必须排在
  通用 `!res.ok` 之前**。落进通用分支的症状是——用户看到「报表 x 已存在」，
  然后**没有任何办法继续保存**，明明点一下「覆盖」就能存。
- **「存我自己正在编辑的那份」要带 `force`**：`id === lastSavedId` 时带。
  不带的话第二次保存会被 409 顶回来，用户看到「存自己刚存过的那份也要确认」——
  那是把闸做成了绊脚石。
- 非法 id 仍是 **400**（先校验 id 再查存在性），别和 409 混。

**仍未做（诚实标注）**：`force` 是「我声明我有权写这个 id」，**不是乐观锁**。
所以「我打开它 → 别人改了它 → 我保存」仍然会覆盖掉别人的改动。
真要防那个得用 `updatedAt` 做 `If-Match` 式的比对，这次没做。

---

## 14. 已知边界与取舍

**诚实清单**——这些是**设计取舍**或**已知局限**，不是「忘了做」：

### 14.1 架构取舍

| 项 | 说明 |
| --- | --- |
| **只读，不回写** | 三个引擎都只读。对外口径必须是「**按只读设计，不支持回写**」，**不能说「暂未实现」** |
| **分页不认分组** | `paginate()` 按固定行数切**拍平网格**，不知道哪几行同组 → 一组明细跨页时第二页只有重复表头，**补不出主格**。「组内不跨页」靠合并格边界实现 |
| **不做带区模型** | 不引入润乾那套 9 类带区模型——需要给模板加带区类型，前后端模型都得改（`mod.rs:402-403`） |
| **不做子报表** | 明确不做 |
| **图片只收 data URI** | 不做文件路径（任意文件读取原语） |
| **不做 CORS 后端代理** | URL 取数走**前端**，后端不代理（SSRF 理由）。代价：CORS 失败只表现为 `TypeError: Failed to fetch` |
| **覆盖用 `force` 而不是乐观锁** | 服务端能挡住「没声明就覆盖」（409），但挡不住「我打开它 → 别人改了它 → 我保存」。`force` 是**授权声明**，不是版本比对。要真防得用 `updatedAt` 做 `If-Match` 式比对（见 §13.6） |

### 14.2 能力边界

| 能力 | 状态 |
| --- | --- |
| 图片 / 条码 / 二维码 | ✅（均自研） |
| 图表 | ✅（HTML 内联 SVG + xlsx 原生图表） |
| 条件格式 | ✅（**无新渲染代码**，走样式通道） |
| 页面设置（纸张/方向/边距/页码/居中） | ✅ |
| Word 导出 | ✅（手写 OOXML） |
| **背景 / 水印** | ❌ **没做** |
| 超链接 / 下钻 | ❌ 判定「打印用不上」 |
| 子报表 | ❌ 不做 |
| 填报回写 / 大屏 / AI / 权限分享 / 移动端 | ❌ 明确不做（做深度，不做广度） |

### 14.3 `GridCell` 字段很少

`text / pos / rowspan / colspan / raw_number / num_format / formula` + 后补的
`style / image / chart / barcode`。

**设计器里的彩色是语义高亮**——标的是「这格什么角色」不是「长什么样」。
xlsx 基础样靠导出器写死。

### 14.4 未验证项（必须一直说）

- **「真 Word 能打开导出的 docx」在本机验不了**（没装 Word / WPS），
  只能用四条可判定不变量替代（见 §15）；
- **URL 取数路径受 CORS 限制**；
- **`ReportDef` 没有 JSON Schema**；
- **xlsx 的三条探针用例无法在 `ts-test.sh` 里跑**。

### 14.5 跨语言闸：形状全覆盖，语义覆盖了 8 条

`mirror-check.py` 从 **2026-09-27** 起是**两层**：

| 层 | 覆盖 | 数量 |
| --- | --- | --- |
| A. 形状层 | 字段名（Rust 类型 ↔ TS 类型） | 24 组 |
| B. 语义层 | 规则类事实（白名单 / 上限 / 别名 / 词表） | **8 条** |

**已进语义层的 8 条**（每条自带方向，见 §15.7）：

| 事实 | 方向 | Rust | TS |
| --- | --- | --- | --- |
| 纸张名清单 | `equal` | `model.rs` `PAPERS` | `grid-report.ts:677` `PAPER_NAMES` |
| 图表类型 | `equal` | `chart.rs:30` `KINDS` | `grid-report.ts:301` `CHART_KINDS` |
| 码制白名单 | `equal` | `barcode.rs:40` `SYMBOLOGIES` | `grid-report.ts:188` `BARCODE_SYMBOLOGIES` |
| 字节上限（213 / 48） | `equal` | `barcode.rs:52,59` | `grid-report.ts:197` `BARCODE_MAX_BYTES` |
| 条件运算符 8 个 | `equal` | `model.rs:163` `CondOp::NAMES` | `grid-report.ts:417` `CONDITION_OPS` |
| 码制别名 | **`ts_subset`** | `barcode.rs:186` `normalise_symbology` | `grid-report.ts:211` `normaliseSymbology` |
| 页脚 255 上限 | `equal` | `model.rs:1028` `escaped_len + 2 > 255` | `grid-report.ts:748` `escaped + 2 > 255` |
| 诊断 code 词表 | `set_equal` | `issue.rs:139-149` `CODE_*` | `grid-report.ts:799` `IssueCode` |

**仍未覆盖的**（明写，别当成已覆盖）：

- **表头行数判据**（`model.rs:1071` ↔ `grid-report.ts:1543`）—— 它不是「一张表」而是一段**判据**，
  抽成事实要跑两边的代码，正则做不到。它仍只有 Rust 侧的内容钉子
  （`sample_template_has_two_header_rows` 等），**只改 TS 一侧仍然无人拦**。
- **样式颜色 `#RRGGBB`** —— 两边形态不同（Rust 在 `html_style_attr` 里校验，TS 是一组正则），
  且是「两端点都报 400」那种，抽成「清单」不自然。
- **语义层只覆盖「声明」，不覆盖「用法」** —— 闸能证明两边**声明了同一张表**，
  不能证明两边**用同一套逻辑去用它**（判据顺序、trim / 大小写敏感度）。
  这一层仍然靠人工核对结论（见 §14.4 与 `架构体检-不足与改进方案.md` §6）。

Rust 侧另有**内容钉子**（`chart.rs` 的 `kinds_table_is_pinned_and_names_the_ts_mirror`、
`barcode.rs` 的对应条、`model.rs:1517` 的 `paper_table_is_pinned`）——
但**它们只把「Rust 改了」变红**，TS 侧单独改要靠上面这张表。

### 14.6 已复现的缺陷（2026-09-26 补，由外部评审驱动）

> 这一节是**对本文初稿的更正**。初稿准确描述了「代码在做什么」，
> 但对**这些机制意味着什么**写得太客气：下面几条都是**静默产出错数据**。
>
> **修复进度（2026-09-26）**：**② → ① → 结构化错误 → ③④ 全部落地**，
> 五个探针里四个已从 `#[ignore]` 转成**常驻回归测试**
> （`nonconvergence_tests::forced_exit_recomputes_values_against_final_visibility`、
> `layout_collision_probe::collision_is_reported_and_first_instance_wins`、
> `cross_ds_col_parent_probe::*` 两条、`join_view_first_row_probe::*` 两条）。
> 剩下的 `#[ignore]` 只有基准（13 条）与一条条码 oracle dump。
>
> **编号约定**：本节 ①~⑤ 与 `架构评审核验-逐条复现.md` §3 一致 ——
> ③ 是「跨数据集列主格按裸行号」，④ 是「`join_view` 用首行代表整组」，
> ⑤ 是「图表每组一张不成立」。**③④ 是同一族**（多数据集关联只处理了部分路径）。

#### 缺陷 ① 二维落位冲突：后写静默覆盖前写 —— ✅ 已修

`grid[inst.row_start][inst.col_start] = Some(..)`（原 `engine.rs:1269`）**没有任何占用检查**。

复现（原 `layout_collision_probe`）：同一模板行放**两个**列展开格（模板列 0 与 1）——

```
实例数=6 落位数=4 冲突数=2
  冲突 (0, 1) ← ["A1", "B1"]
  冲突 (0, 2) ← ["A1", "B1"]
网格 1x4
  行0: "1月"@A1 | "Q1"@B1 | "Q2"@B1 | "Q3"@B1
```

3 个月 × 3 个季度的 6 个数据格，渲染成 `1月 Q1 Q2 Q3` —— **2月 / 3月 连同它们的值一起消失**，
**零告警**。根因：`layout_columns`（`engine.rs:1945-1960`）让每个列展开组从**自己的
`tpl_col`** 起占列，两组区间重叠时不检测。

**修法**：在写入点加占用检查 ——

- 冲突时**保留先创建的**实例（实例序号小 = 模板位置靠前，结果稳定可预测），
  后到者标 `dropped`（与「没被 place 过」同义 → 图表解析等后续环节自动跳过它）；
- 冲突**逐条告警**，点出落位坐标与两个 `pos`，并给出可执行的建议
  （让其中一个成为另一个的列主格，或把两者放到不同的模板行）；上限 20 条，超出只报总数。

**刻意不做的事**：不去「修好布局」。两个**独立的**列展开本来就没有合法的二维布局
（没有父子关系就没有列对齐依据），硬排只会得到一个**看起来像正常交叉表**的结果
——旧的 `1月 Q1 Q2 Q3` 正是这种（每列都有值，完全不像出错），比报错更危险。
现在的结果是 `1月 2月 3月 Q3`，**一眼能看出是坏的**，再配合告警才不会被误信。

**代价**：输出仍然是坏的，只是不再**静默**。真正「正确」的行为是拒绝该模板
（那属于**分级诊断**：现在这条已经是 `Error` 级 —— 调用方据此就能拦住导出，
见 §14.7）。

**本次没覆盖的相邻形态（已知边界，别当成已覆盖）**：检查只比「落位点是否相同」，
**不比 colspan / rowspan 的覆盖范围**。所以「A1 在第 0 列、`colspan = 3`，
B1 落在第 2 列」这种**起点不同但区间相交**的重叠仍然抓不到（B1 会写进 A1 的合并区里）。
补它要做矩形相交检测，而 `merge_to_end`（铺到行尾）让矩形在算之前就依赖别的格子的列区间，
成本不低 —— 所以**这次刻意没做**，而不是「顺手做了」。

**顺带证明的事实**：用临时 `panic!` 在冲突点拦截、跑完整套件 ——
**513 条既有测试里一条都不触发冲突**，只有本模块的模板会。所以这次检测是**纯增量**，
没有改变任何既有模板的落位。

#### 缺陷 ② 强制退出时，数值口径落后布局一轮 —— ✅ 已修

`evaluate_to_fixpoint`（原 `engine.rs:1740-1781`）在 `round + 1 == MAX_ROUNDS` 时 `break`，
**不重置 `evaluated`**。于是最后一轮的 `changed` 为真时：

| 项 | 用的是哪一轮的 `hidden` |
| --- | --- |
| `insts[..].value` | 第 3 轮（**上一轮**） |
| `insts[..].hidden`（布局按它过滤） | 第 4 轮（最新） |

复现（原 `nonconvergence_probe`，行测试 `COUNTA(B1) <= 1` 造成震荡）：

```
未丢弃 A1 实例数 = 3
C1 的值 = ["0.0", "0.0", "0.0"]     ← 按「全被删」那轮算的
网格 3 行                            ← 但最终 3 行都可见
```

**汇总显示 0，而明细明明有 3 行。** 附带缺陷：那句告警
「已按最后一轮结果出表」（原 `engine.rs:1769`）**与事实相反** —— 数值恰恰不是最后一轮的。

**修法**：未收敛时的收尾改为「清轮缓存 → 重置 `evaluated` → 按**最终**（已冻结的）
`hidden` 重算全部值」，抽出 `clear_round_caches()` / `reset_evaluated()` 两个 helper
（`cycle_warned` 刻意**不重置** —— 它是「同一个环只报一次」的闸）。
告警文案换成「…该条件在最终数据上仍不成立 —— 这张表的结果**不稳定**，
不要当作可发布的结果」，删掉那句说反的。

#### 缺陷 ③ 跨数据集列主格按**裸行号**关联 —— ✅ 已修

行父格跨数据集时会强制要求 `join_on`（`join_view`），但**列父格这条路没有同等检查**：
`col_parent_index` 把列主格的 `rows` 当**裸行号**索引，与当前格的 `base` 直接求交。
列主格的 `rows` 是**它自己那份数据集**里的行号 —— 跨数据集时两套行号没有任何共同含义。

复现（原 `cross_ds_col_parent_probe`，ds1 两行 / ds2 三行）：

```
网格 1 行 x 2 列
  行0: "111" | "222"
告警数 = 0
```

ds2 的第三行（333）**静默消失，零告警**。

**修法：拒绝 + 报 `Error`**（本格一格都不建）。行父格有 `join_view` + 强制 `join_on`
兜底；列父格**没有**对应机制（列轴上的「反向关联」语义完全不同，不是加个字段能覆盖的），
所以只能拒绝。判据是**数据集名**（`self.insts[cp].ds != cell_ds_name`），
并且过滤必须发生在建 `row_to_cp` **之前** —— 那个索引是直接拿列主格的 `rows` 建的，
混进跨数据集的实例就等于把两套行号混进同一张表。

**为什么这条不是「① 的重复」**（重要，别删）：① 只在**同模板行**撞列时才响。
把数值格放到列头的**下一行**（交叉表的正常形状）就不会撞列，① 完全不响 ——
而裸行号求交照样在按行号硬对齐。这个形状**最危险**：两份数据集**等长**时输出

```
行0: "1月" | "2月" | "3月"
行1: "111" | "222" | "333"          ← 零诊断，看着完全正确
```

纯属行数巧合。回归测试里把子表顺序倒过来（333/222/111）让「按行号对齐」立刻露馅，
并**显式断言这条形状里没有 `layout_collision`** —— 那一句就是「③ 独立于 ①」的判据。

#### 缺陷 ④ `join_view` 用**父实例首行**的关联键代表整组 —— ✅ 已修

`join_view` 原注释写：

> 父行当前的键值：父实例可能覆盖多行，取第一行（**分组格下它们同键**）

**括号里那个前提没有任何校验。** 只要**分组字段 ≠ 关联字段**，组内各行的关联键就不同，
「首行代表整组」立刻少取数。复现（原 `join_view_first_row_probe`，父按 `region` 分组、
`join_on: "cust"`）：

```
网格 1 行
  行0: "华东" | "100"
告警数 = 0
```

华东组内有 `cust=1` / `cust=2` 两条子表行（100 / 200），**正确合计 300，实际 100**，
200 **静默丢失，零告警**。评审说这一点「也需要验证，不能默认成立」——**验证结果：它不成立。**

**修法：取组内全部键值求并集**（「组关联」语义：本组覆盖的每个键，对应的子表行都算进来）。

- 组内**同键**时（绝大多数真实模板：分组字段 == 关联字段，或父格就是明细行）
  并集退化成单值 → 走**原来那条直接比较**的分支，O(|子表|)、无额外分配，
  行为与改动前**逐字节一致**。这一点由对照组
  `same_key_group_join_is_unchanged_and_silent` 单独钉住（含「一条诊断都不该多」）。
- 组内**多键**时用 `HashSet` 求并集（不是线性 `contains` —— 父格这一组可能很大，
  线性扫会退化成 O(|子表| × 组内键数)）。
- 多键是**语义变化**，作者会看到一个与改动前**不同的数** → 必须说出来，
  所以报一条 `Warning`（`join_key_not_grouped`）。**不报就是新的静默行为变化。**

**已知行为差异（诚实披露）**：旧代码取 `rows.first()`，若**首行恰好没有**关联字段
而后面某行有，旧行为是「告警 + 空」；新行为会取到后面那行的值。
这在行结构同质的数据集里不会出现，但确实是个差异 —— 新行为更接近「本组有哪些键」的本意。

#### 缺陷 ⑤ 图表「每组一张图」不成立

`resolve_charts`（`engine.rs:1356-1357`）按 `pos` 去重：

```rust
let mut seen: BTreeSet<String> = BTreeSet::new();
decls.retain(|(_, _, pos, _, _)| seen.insert(pos.clone()));
```

所有分组实例共享同一个 `pos` → 只剩第一份。而 `ChartIndex` 也按 `pos` 收集
（`engine.rs:1365-1377`）→ 那一张图会把**所有组**的类目收进去。

**结论**：`make_insts` 里那句注释「挂在分组格上的『每组一张图』走的就是这条展开分支」
（`engine.rs:1517-1518`）**是误导性的** —— 声明确实被拷到了每个实例，但随后全被丢掉。
「每组一张、数据各不相同的图」**当前做不到**。（本文初稿沿用了那句注释，已在 §8.2 更正。）

### 14.7 分级诊断通道（`Issue`）—— ✅ 已落地；③④ 也已修

| 项 | 现状 | 正确行为 |
| --- | --- | --- |
| **分级诊断通道** | ✅ **已实现**（`issue.rs`，2026-09-26） | — |
| 缺陷 ③ 跨数据集列主格 | ✅ **已修**（拒绝 + `Error`） | 拒绝该模板并报错，不按裸行号求交 |
| 缺陷 ④ `join_view` 首行假设 | ✅ **已修**（组内键求并集 + `Warning`） | 取组内键的并集，并把语义变化说出来 |
| ⑤ 图表「每组一张图」 | **不打算按原注释实现** | 已把注释与文档改成事实描述 |

③④ 的正确修法都是「**拒绝 / 改口径 + 报错**」，所以**必须先有这条分级通道** ——
这也正是用户把「结构化错误」排在 ③④ **之前**的原因。通道建好之后，
③④ 的落地就是「加一个 `code` + 用 `fail_at` 而不是 `warn`」。

#### 为什么需要分级

原来只有一条通道：`Engine.warnings: Vec<String>`。它的问题不是「信息不够」，
而是**分不出严重程度** —— 两类消息混在同一个 `Vec<String>` 里：

| 类别 | 例子 | 调用方该做什么 |
| --- | --- | --- |
| **提示** | `export_formula` 翻不成 Excel 公式，已回落写值 | 展示即可，结果是对的 |
| **结果不可信** | 落位冲突（整格数据被丢掉）、非收敛（汇总与明细口径不一致） | **拦住导出**，别让作者当成品发出去 |

混在一起时前端只有两个选择：全弹（提示把真问题淹掉）或全不弹（真问题看不见）。
评审那句「让错误结果不能被当成成功结果」，落到代码上就是这条分级。

#### 落地形态（`print-server/src/report/issue.rs`）

```rust
enum IssueLevel { Info, Warning, Error }   // Ord 是有意的：level >= Warning 就是要进 warnings

struct Issue {
    level: IssueLevel,
    code: String,          // 稳定标识；前端据此分类，**不要**用 message.contains(..)
    sheet: Option<String>, // 归属哪张 sheet（多 sheet / loop_field 展开时用得上）
    pos: Option<String>,   // 格子模板坐标（如 "B3"）；整表级的不带
    message: String,
}
```

- `Engine` 的私有字段从 `warnings: Vec<String>` 换成 `issues: Vec<Issue>`；
  `warnings()` 保留为**派生视图**（`level >= Warning` 的 message），
  `issues()` 给需要分级的调用方。**升级级别不会让任何消息从 `warnings` 里消失。**
- `RenderResponse` 增量加 `issues` 字段，`warnings` **保留**（前端零改动）。
- `[sheet] ` 前缀**不再拼进消息字符串**，改由 `sheet` 字段在末尾统一派生 ——
  这样「某条告警只进了 `warnings`、没进 `issues`」在结构上不可能发生。
- `dump=true` 时按级分节打印（`!! 结果不可信` / `!! 告警` / `!! 诊断`）。
  `Info` 级有**两个**消费方：这个 dump，以及设计器界面（见下面「界面接线」）。

#### 三个「记诊断」的入口，和一个借用陷阱

`warn` / `warn_at` / `fail_at` / `info` 四个 helper 让「绕过分级通道」编译不过
（字段换成 `Vec<Issue>` 后 `self.warnings.push(..)` 直接是类型错误）。

但 `Engine::warn(&mut self, ..)` 借的是**整个 `*self`**，与 `expand_sheet` 里
`for inst in self.insts.iter()` 那个仍然活着的不可变借用冲突（实测 **6 处 E0502**）。
所以另有一个字段级包装 `IssueSink(&mut self.issues)`（`issue.rs`），
在循环体里用**临时值**形式调用 —— 借用只活在那一条语句里。
这个包装的存在理由是**借用粒度**，不是抽象。

#### 哪些是 `Error`

| code | 级别 | 触发 |
| --- | --- | --- |
| `layout_collision` | **Error** | 两个实例落在同一格（§14.6 缺陷 ①） |
| `nonconvergent` | **Error** | `row_test`/`col_test` 在 4 轮内没稳定（§14.6 缺陷 ②） |
| `cross_ds_col_parent` | **Error** | 列主格跨数据集（§14.6 缺陷 ③）—— 本格拒绝出数 |
| `join_key_not_grouped` | Warning | `join_on` 字段 ≠ 父格分组字段、组内多键（§14.6 缺陷 ④）—— 已按并集关联 |
| `fixpoint_rounds` | Info | 收敛了但花了 ≥3 轮 —— 条件之间互相依赖，改模板要小心 |
| `generic` | Warning | 还没细分的旧告警站点（会长期存在，别当成错误） |

**`Info` 有真生产者**是刻意的：本项目不接受「加了级别却没有生产者」的死枚举。
要么给 `Info` 找个真用处（这里是「3 轮才稳定」这个诊断），要么就别加这一级。
同理，词表里**每个 `code` 都有生产者** —— `issue.rs` 的钉子测试盯着这张表，
改了要同步 TS 的 `IssueCode`。

#### 验证

- **临时 `panic!` 量影响半径**（通道 + ③④ 一起量）：在 ③ 的拒绝分支和 ④ 的多键分支
  各插一个 `panic!` 跑完整套件 → **零触发**。即「没有任何既有模板会走到这两条新分支」，
  改动是**纯增量**。（比「套件是绿的」强：绿只证明「没碰坏」，不证明「没走到」。）
- 三条故障注入（都实测「注入 → 红且报出预期文案，还原 → 绿」）：
  `fail_at` 降级成 Warning → **3 条测试红**；`warnings` 派生去掉 `[sheet] ` 前缀 →
  **3 条红**（含两条**既有**测试）；`is_warning_or_worse` 恒真 → **2 条红**。
- ③④ 各一条故障注入：
  - ③ 把数据集判据改成恒真（= 恢复旧行为）→ **2 条红**，
    其中一条报 `实际第 0 列 = Some(333.0)`（`1月` 配到 `333`，正是裸行号对齐）。
  - ④ 把键收集改回 `take(1)` → **1 条红**，报 `Some(100.0)` vs 期望 `Some(300.0)`。
- **对照组**（证明「同键时行为没变」，比证明「多键时取并集」更关键）：
  `same_key_group_join_is_unchanged_and_silent`。再给单键分支**注入一条多余的告警**
  → 这条对照组**和一条既有测试**（`cross_dataset_child_expands_over_joined_rows_only`
  里那句「写了 join_on 就不该告警」）同时红 —— 说明「不该多报」这件事本来就有闸守着。
- 端到端对账：`graded_issues_reach_the_response_and_stay_in_sync_with_warnings`
  逐条比 `warnings` 与 `issues` 的派生关系 —— 这是防「两边各写一份」的那条闸。

#### 界面接线（2026-09-26 补上，`GridReportModal`）

改之前界面**只读 `warnings`**，而 `Info` 按定义不进 `warnings` → 那一级在界面上
**根本不存在**（要看它只能用 `dump=true`）。现在三条渲染路径都
`setIssues(data.issues ?? [])`，界面按 `issues` 分级渲染：

| 级别 | 文案 | 界面表现 |
| --- | --- | --- |
| `error` | 结果不可信 | 红色 Alert + **禁用导出按钮** + 拦住导出时另给一条 `blockNotice` |
| `warning` | 告警 | 黄色 Alert，不拦导出 |
| `info` | 提示 | 灰色 Alert，不拦导出 |

- **级别 → 文案的映射只有一处**（`ISSUE_LEVEL_LABEL` / `ISSUE_TEXT_TYPE` 两个模块级表）。
  散落进 JSX 的话，改文案会漏掉一处，而漏掉的那处**不会报错**。
- **`warnings` 回退分支保留**：服务端是**独立进程**，用户自己起 ——
  「新界面 + 旧服务端」是真会出现的组合，那种响应里只有 `warnings`。
  条件是 `issues.length === 0 && warnings.length > 0`，即两个 Alert 不会同时出现。
- **导出有两道闸，条件是同一个**（`blockingIssues.length > 0`）：按钮 `disabled`，
  以及 `doExport` 开头那句守卫。
  ⚠️ **诚实披露：第二道闸当前从界面走不到** —— `grep doExport` 只有一处调用点，
  就是那个按钮，而按钮恰好在同一条件下禁用。它是留给「将来多一个调用点
  （菜单 / 快捷键 / 批量导出）」的保险，**不是**当下可测的路径。
  它有没有牙齿只能靠注入证明：把按钮的 `disabled` 拆掉（模拟「多了一个调用点」），
  守卫照样拦得住。

#### 验证（界面这一层）

- 4 条用例（`designer-react/src/modals/grid-report-issues.spec.tsx`）：
  `error` 级禁用+定位到格、**`info` 级看得见**、`warning` 级不拦、旧服务端回退。
  第二条是本次改动的**唯一决定性用例** —— 它用的响应 `warnings` 是**空的**，
  所以在「界面只读 `warnings`」的旧实现下**必然红**。
- 5 条故障注入（`scripts/fault-inject-issues-ui.py`，每条都要求**指定的那条用例**
  红在**指定的断言文案**上，只看退出码不算数）：

  | 注入 | 期望红在哪 |
  | --- | --- |
  | 预览路径不接 `issues` | `info 级`，等诊断 Alert 超时 |
  | `info` 进 state 前被过滤掉 | `info 级`，同上 |
  | `error` 判据写成 `warning` | `error 级`，红在「已拦住导出」上 |
  | 按钮不再禁用（守卫还在） | `error 级`，红在 `disabled` 上 —— **且没有 xlsx 发出去** |
  | 按钮禁用 + 守卫一起拆 | `error 级`，红在「结果不可信却把 xlsx 发出去了」上 |

- ⚠️ **用例里的断言顺序是有意的**：先断言**契约**（一个 xlsx 字节都没发出去），
  再断言**机制**（按钮 `disabled`）。反过来写会**短路** —— 实测注入「只拆
  `disabled`」时用例红在 `disabled` 那一行，后面的断言根本没执行，
  于是「守卫到底拦不拦得住」**永远验不到**。这是第 4、5 条注入区分开的前提。
- **这三条用例的跑器不是 `check-all.sh`**：它第 3 道闸 `ts-test.sh` 只覆盖
  `openprint/src/report/*.ts`（node 环境、无 DOM），而这份 spec 要 jsdom + antd +
  React。跑法见 §15。

**仍未覆盖**：HTTP 层错误（连接不上、404）**没有 code**，分级只覆盖渲染结果里的诊断；
`issues` 的 `code` 只用于展示与定位，界面**没有**按 code 分支（比如按 code 给「怎么改」
的建议）—— 那是另一件事。

---

## 15. 验证设施

这是本项目**最值得说的资产**。

### 15.1 三层验证

| 层 | 数量 | 位置 |
| --- | --- | --- |
| **Rust 单测** | **523 通过 / 0 失败 / 14 忽略**（共 537 条） | `print-server/src/` |
| 其中 `report/` | **433 条** | `print-server/src/report/` |
| **TS 类型闸** | 0 错 | `scripts/ts-check.sh` / `ts-project-check.sh` |
| **TS 纯函数单测** | **239 通过**（3 文件） | `scripts/ts-test.sh` → `openprint/src/report/*.ts` |
| **设计器 UI 单测** | `grid-report-` 过滤下 **144 通过**（12 文件）；全量 **383 通过**（44 文件） | `scripts/ts-test-designer.sh` → `designer-react/src/**` |
| **探针脚本** | **14 个** verify | `scripts/*.py` |
| **故障注入** | **18 个** fault-inject | `scripts/*.py` |

> `fault-inject` 那一格**漂过三次**：初版写 **13**，实测当时已是 **14** ——
> 加了新脚本没同步注释。2026-09-26 加 `fault-inject-issues-ui.py` 时核成 15；
> 2026-09-27 加 `fault-inject-mirror-semantics.py` 核成 16，
> 同日再加 `fault-inject-atomic-save.py` / `fault-inject-save-confirm.py` 与
> `verify-atomic-save.py`，核成 **18 / 14**。
> **这正是 §15.3 那条「数量钉子」要防的事，而它自己就漂了。**
> 判据：**注释里的数字是最容易漂的** —— 改完脚本记得 `ls scripts/*.py | wc -l` 核一遍。

### 15.2 聚合跑器 `scripts/check-all.sh`

**这是补上「验证设施一流，但让验证设施跑起来是缺的」那一步的产物。**

执行顺序（**便宜的排前面**；实测**热启动约 36 秒**，加上第 7 道后**实测 195 秒**）：

```
mirror-check       (1s)   Rust ↔ TS 契约对账（形状 24 组 + 语义 8 条）
ts-check           (2s)   TS 类型快查
ts-test            (5s)   dataset-import 66 / dataset-fetch 12 / grid-report 161
ts-project-check   (18s)  designer-react（tsc 5.9.3）
ts-project-check   (6s)   openprint（vue-tsc --build）
cargo test         (4s 热 / 1m33s 冷)
ts-test-designer   (145s) 设计器 UI 单测，jsdom 串行（grid-report- 过滤，12 文件 / 144 用例）
```

`--fast` = 前两项，3.5 秒。`--list` 只列不跑。

**第 7 道是 2026-09-26 补的，补的是一个真缺口**：`designer-react` 有 **43 个 spec
文件 / 375 条用例**，而在此之前**没有任何脚本会跑它们** —— `ts-test.sh` 只覆盖
`openprint/src/report/*.ts`（node 环境、无 DOM）。所以「改一次 `GridReportModal`，
`check-all.sh` 照样全绿」是**真的**：根本没有闸在管那个文件。

它排在最后，因为它**比前面几道加起来还贵**。代价换来的是**一个必须写进注释的坑**：

> **`--no-file-parallelism` 不是性能选项，是正确性要求。**
> 默认（文件级并行）下这套 UI 用例互相抢 CPU，实测
> **43 文件 / 375 用例 → 16 失败 / 359 通过**（238s）；
> 而**同样的 4 个失败文件单独跑 4/4 全绿**（各 14~21s）；
> 改成串行后 **0 失败 / 375 通过**（346s）。
> 失败形态全是 `Test timed out` / `expected '' to contain '标签网格'`，
> 即「渲染还没跑完就被判死」——**不是真缺陷，别当回归去修**。
> （与 2026-09-23 那条「这套 UI 用例别和 cargo 并发跑」同源。）

**已知覆盖缺口（明写，别当成已覆盖）**：第 7 道带 `grid-report-` 过滤，只覆盖
**12 文件 / 144 用例**。`designer-react` 另外那 32 个 spec（canvas / panels / toolbar /
stores…）仍然不在聚合跑器里 —— 不是「没必要」，是那 346s 太贵。
要全量：`bash scripts/ts-test-designer.sh`（无参数 = 全部）。

**退出码是「三态」而不是两态**（`check-all.sh` 的 `run()`）：

| 退出码 | 含义 | 输出 |
| --- | --- | --- |
| `0` | 通过 | `✓ 通过（Ns）` |
| `2` | **没跑成**（环境缺东西） | `⚠ 没跑成（退出码 2 = 环境缺东西，**不等于通过**）` |
| 其它 | 失败 | `✗ 失败（退出码 N）` |

**为什么必须三态**：`ts-project-check.sh` 在借不到编译器时返回 **2**——
那是「**没检查**」，把它叫「失败」是**假红**。
本项目有专门的注释记着这件事（`node-bin.sh`）。

末行是：

```
有闸没跑成 —— **不等于通过**，先把环境补齐再下结论
```

**这是「没有跑器的闸比红的闸更坏」那条结论的落地**：
绿闸会主动产出**虚假信心**。

### 15.3 内容钉子 vs 数量钉子

```rust
// chart.rs:400
#[test]
fn kinds_table_is_pinned_and_names_the_ts_mirror() {
    assert_eq!(KINDS.to_vec(), vec!["bar","line","pie"],
        "图表类型表变了：要同步 TS 的 CHART_KINDS（openprint/src/report/grid-report.ts）");
}
```

**钉内容而不只钉数量**：数量相同但名字改了（`pie` → `donut`）同样会让两边分叉，
而 `assert_eq!(KINDS.len(), 3)` 对改名**毫无反应**。

`PAPERS` 的钉子（`model.rs:1517`）从两件事扩到三件：

```rust
assert_eq!(PAPERS.to_vec(), expect,
    "纸张表变了：① HTML 尺寸 ② Excel 纸张码 ③ TS 的 PAPER_NAMES（mirror-check.py 会红）三处都要同步");
```

### 15.4 故障注入证明「探针有牙齿」

`scripts/fault-inject-table-pins.py`：**5 处注入，5/5 被抓**，含两处**改名**注入
（证明内容钉子强于数量钉子），并用 sha256 验证**逐字节还原**。

一个很细的陷阱（脚本里的注释）：

```python
def run_test(name: str):
    """跑指定用例，返回 `(是否真的跑了, 是否通过)`。
    「真的跑了」必须单独判：**`cargo test <过滤名>` 在匹配不到任何用例时仍然返回 0**。
    于是把用例名打错会伪装成「通过」..."""
    ran = out.count("... ok") + out.count("... FAILED")
    return ran > 0, r.returncode == 0
```

**「没跑成」≠「通过」≠「失败」——三个状态，不是两个。**

#### 15.4b 光知道「哪条用例红了」不够 —— 要钉到**哪一条断言**

`scripts/fault-inject-issues-ui.py`（2026-09-26 加）：**5 处注入，5/5 被抓**，
还原后逐字节一致、基线全绿。

这一份比前面的多一个字段：**期望红在哪个断言文案上**。原因是踩出来的 ——

> 注入「把按钮的 `disabled` 拆掉」和「把 `disabled` 和函数内那道闸一起拆掉」，
> **两条都让同一条用例变红**。只看「哪条用例红了」的话，这两次注入结果**完全一样**，
> 于是「函数内那道闸真的拦得住」这句话**没有任何证据** —— 因为
> `expect(disabled).toBe(true)` 写在 `expect(noPost).toBe(0)` **前面**，
> 用例在第一条断言就短路了，根本没走到发请求那步。

修法有两处，都值得记：

1. **用例里把断言按「契约 → 机制」排序**：先断言**契约**（一个 xlsx 字节都没发出去），
   再断言**机制**（按钮 `disabled`）。契约是行为，机制是实现；实现可以换，
   行为不能变。顺序反了就会**掩盖**「另一道闸有没有在工作」。
2. **注入脚本比对失败输出里的断言文案**，而不只是用例名。

**通用结论：故障注入的判据要细到「哪一条断言」，否则两条不同的注入会被算成同一条证据。**

### 15.5 本机环境的几个坑（已固化进脚本注释）

1. **macOS bash 3.2.57 会静默吃字节**：`$VAR` 紧跟一个非 ASCII 字符时，
   **变量的值和多字节字的第一个字节都会丢**。
   `echo "测试 $V）"` → `「测试 」+乱码`；`echo "测试 ${V}）"` → 正确。
   `$(...)` 命令替换**不受影响**；`zsh` **同样中招**；`printf` 安全。
   危害在于它落在**闸脚本自己的状态行**上——那句本该说真话的行。
   四个脚本站点已改成 `${VAR}` 并就地写了警告注释。
2. **BSD `grep` 不支持 `\|`**：`grep -E 'a|b'` 才对。危害是**静默失效**
   （不报错，只是没匹配），已因此误判过「字段不存在」。
   **判据：grep 说「没有」时先怀疑 grep。**
3. **`grep ... | head` 的退出码是 `head` 的（0）**——「没输出 + 退出码 0」
   完全可能是「grep 一条都没匹配」。
4. **工具输出重定向到文件后可能带 ANSI 色码**（`vue-tsc --pretty`）：
   `grep -c "error TS" 输出.txt` 是 **0**，而 `head` 看明明满屏都是。
   判据：输出是用 `>` 存下来的、且来自支持彩色输出的 CLI → 先剥 ANSI 再搜。
5. **`HTTP_PROXY` 会破坏探测 `127.0.0.1:18888`** → 需要 `curl --noproxy '*'`。
6. **量耗时别拿「紧接着上一次跑」的数当基准**：同一条 vitest 命令（`grid-report-`
   过滤）实测过 **96s** 和 **161s**，96s 那次是 vite 的 transform 缓存还热着。
   差点据此把 `check-all` 第 7 道的成本写小一半（**闸的成本写小了，就会被塞进不该塞的地方**）。
   顺手排除过一个嫌疑：**加不加 preload 都是 161s**，所以不是 preload 拖的。
   写进注释的数字一律取**偏保守**的那个。
7. **「文件级并行」默认开着，而这套 UI 用例经不起抢 CPU**：见 §15.2 那段 ——
   默认并行 **16 条假红**，串行 **375/375 全绿**，且那 4 个文件**单跑都是绿的**。
   判据：失败形态是 `Test timed out` / `expected '' to contain …`（渲染没跑完就判死），
   而**同样的文件单独跑是绿的** → 先怀疑并行，别去「修」那些用例。
6. **`cargo test --lib` 失败**（binary crate）→ 用 `--bin print-server`。

### 15.6 docx 的四条可判定不变量

因为「Word 能打开」验不了（§14.4），改用：

1. **良构 XML**（每个 part 能被解析器接受）；
2. **part 闭合**（`[Content_Types].xml` 与 `_rels` 覆盖所有 part）；
3. **`r:id` 闭合**（每个引用都有对应关系项）；
4. **子元素顺序**（OOXML 的 schema 是**有序**的，顺序错了 Word 会拒开）。

### 15.7 跨语言「语义」闸：`mirror-check.py` 的第二层

**2026-09-27 加。** §15.3 的内容钉子解决的是「**Rust 侧**改了会不会红」；
这一层解决的是另一半：**TS 侧单独改了会不会红**。答案在加它之前是「不会」。

`mirror-check.py` 现在是两层：

```
A. 形状层   24 组 (Rust 文件, Rust 类型, TS 类型) —— 只比字段名
B. 语义层    8 条规则类事实 —— 比「白名单 / 上限 / 别名 / 词表」的内容，每条自带方向
```

**每条事实自带方向**（这是本层唯一需要设计的点）：

| `mode` | 含义 | 用在 |
| --- | --- | --- |
| `equal` | 顺序 + 内容都要一致 | 会被 TS 拿去拼提示文案的清单（`PAPER_NAMES.join(' / ')`）—— 顺序变了用户看到的话就变了 |
| `set_equal` | 集合相等，顺序不算 | 只做「认不认识」判断的清单（`IssueCode`） |
| `ts_subset` | **TS 不允许比 Rust 宽**，允许更窄 | 别名表（TS 少认一个别名是合法收窄） |

**为什么方向不能统一**：本项目已经因为「三方相等」这个错误方向差点写出一个永久红的闸
（`AI优先…` §3.7）。清单**故意更窄**是合法设计，用相等断言会把合法设计判成漂移。
所以 `ts_subset` 那一条必须存在，而且必须有一条**对照组注入**证明它不误报（见下）。

#### 15.7b 设计要点：抽取失败必须红，不能静默跳过

每条事实的抽取函数返回 **`Optional[list[str]]`**，其中 **`None` 不是「空」，是「抽取失败」**，
而抽取失败**计为红**：

> 静默跳过会让这条检查退化成「永远绿、但根本没在检查」——
> 比红闸更坏，因为它产出的是**空头的信心**。

配套的 `_one()` 要求锚点**恰好匹配 1 处**，**0 处**（常量被改名）和 **≥2 处**（正则写松了）
**都算失败**。「≥2 处」这一半不是假想的：`engine.rs` 在 4 个测试函数内各自定义了局部
`const KINDS`（`:4559 :4803 :4852 :4921`）—— 抽取器若图省事做**全仓搜索**，
「取第一个」会**静默取到测试里的那张表**。

另一个自己踩出来的坑：取函数体靠数花括号，而 `normalise_symbology` 里有
`format!("…{raw}…")` 和 `"{}"` —— **字符串里的 `{` 会让计数错位**，切出错误的函数体、
可能给出**假 OK**。修法是先**把字符串内容抹成等长空格**再数括号（`_blank_strings`，
长度不变所以索引不漂）。

#### 15.7c 故障注入 9 条，9/9（`scripts/fault-inject-mirror-semantics.py`）

| # | 注入 | 期望 | 为什么要有它 |
| --- | --- | --- | --- |
| 1~6 | TS 侧分别改：图表类型 / 字节上限 / 条件运算符 / 纸张名 / `IssueCode` / 页脚上限 | 红，且报出**是哪一条事实** | 证明「TS 单独改会被抓」 |
| 7 | TS 码制别名**更宽**（加 Rust 不认的别名） | 红 | 证明 `ts_subset` 的**上界**真的在管 |
| 8 | TS 码制别名**更窄**（删 `qr-code`） | **仍然绿** | **对照组**：证明方向没写反 |
| 9 | Rust `KINDS` → `KINDS_RUST`（**只改锚点、不改事实**） | 红，报「**抽取失败**」 | 证明「抽取失败会红」这条路真的会走到 |

还原后 sha256 逐字节一致，基线回到 `exit=0` 全绿。

**第 8 条与第 9 条是这一组里最重要的两条**，因为它们证明的**不是「改了会红」**：

- 第 8 条证明「**合法的更窄不会被误报**」。没有它，前 7 条全绿也可能只是「碰巧都改宽了」。
- 第 9 条**没有改任何事实**，只改了锚点。没有它，前 7 条全绿也可能只是
  「恰好锚点还在、正则还松」——**闸根本没在检查**。

> 这与 §15.4b 那条同源：**故障注入的判据要细到「哪一条断言 / 哪一条事实」**，
> 否则不同的注入会被算成同一条证据。

**本层没有覆盖的（边界）**：只覆盖**声明**，不覆盖**用法** ——
闸能证明两边声明了同一张表，不能证明两边用同一套逻辑去用它（判据顺序、trim / 大小写敏感度）。
**表头行数判据**与**样式颜色 `#RRGGBB`** 也仍在闸外（理由见 §14.5）。

---

## 16. 附：坐标与单位换算速查

| 项 | 公式 / 值 | 位置 |
| --- | --- | --- |
| 列名 | `0→A`，`25→Z`，`26→AA` | `model.rs:1305` |
| 位置名 | `cell_pos(r, c) = "{列名}{r+1}"`（行 1 基） | `model.rs:1319` |
| 模板行跨度 | `merge_down + 1` | `engine.rs:206` |
| 模板列跨度 | `merge_across + 1`；`merge_to_end` 时是**下界** | `engine.rs:213` |
| 输出 colspan | `merge_to_end ? 总列数−起始列 : max(列子树跨度, merge_across+1)` | `engine.rs:1199-1203` |
| 输出 rowspan | `max(行子树跨度, merge_down+1)` | `engine.rs:1272` |
| xlsx 列宽 | 单位是**字符**；`px = round(chars × 7) + 5` | `xlsx.rs:89-93` |
| xlsx 列宽上下限 | `MIN_COL_WIDTH = 8` / `MAX_COL_WIDTH = 60`；图表列另有下限 | `xlsx.rs:718`、`xlsx.rs:731` |
| 行高 | `row_pixels(pts) = round(pts × 4/3)` | `xlsx.rs:108-109` |
| mm ↔ inch | `MM_PER_INCH = 25.4`（**只用于页边距**） | `model.rs:758` |
| 默认页边距 | Excel 默认（上下 0.75"、左右 0.7"） | `model.rs:743-749` |
| B5 纸张 | **JIS 182×257**（Excel 码 13） | `model.rs:772-781` |
| 条码每模块像素 | `PX_PER_MODULE = 3` | `barcode.rs:69` |
| 图表默认宽度 | `DEFAULT_WIDTH = 480.0` | `chart_svg.rs:29` |
| 布局递归上限 | `MAX_LAYOUT_DEPTH = 256` | `engine.rs:18` |
| 不动点轮数上限 | `MAX_ROUNDS = 4` | `engine.rs:1741` |

---

## 附：本文未覆盖的部分

- **票据指令**（`ticket/{esc,tspl,zpl}.rs`）：`/print` 的 ESC/POS、TSPL、ZPL 翻译。
  这是「布局 → 设备字节流」的另一条链路，与报表网格无关。
- **前端**（`openprint` 的 TS 引擎层、`designer-react` 的 UI）：本文只讲服务端。
  两边的镜像关系（`mirror-check.py` 守的那部分）只在 §15.3 提了一句。
- **`ReportDef` 的完整字段表**：见 `print-server/reports/sales-by-region.json` 与 `store.rs`。
