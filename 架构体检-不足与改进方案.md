# 架构体检 · 不足与改进方案

> 范围：`print-server`（Rust 展开/导出）、`openprint`（引擎层 TS）、`designer-react`（UI）、`scripts/`（验证设施）。
> 方法：只认源码与可复现命令，**每一条结论都带 `文件:行` 或一段能跑的命令**。凡我没验的一律标注「未验证」。
> 日期：2026-09-24

---

## §0 一句话结论

**这个项目的「验证设施」是一流的，「让验证设施跑起来」这件事是缺的。**

13 个故障注入脚本、13 个探针脚本、1 个跨语言契约核对脚本、3 个类型闸、1 个依赖纯净性守卫 —— 全部存在、全部有效，但**没有任何一个自动化入口会把它们串起来跑**：没有 CI，没有 `check-all`，而且**没有一条 shell 脚本调用过任何一个 `.py`**（实测见 §5.1）。

于是问题不在「闸门造得好不好」，而在「闸门靠人记得敲」。这跟本项目此前已经踩过两次的坑是同一族，只是换了个形态：

| 形态 | 症状 | 本项目是否踩过 |
| --- | --- | --- |
| 长期红的闸 | 看惯了红，等于没闸 | 是（TS 曾红 116 条） |
| **没有跑器的闸** | **闸是绿的，只是从来没人跑它 —— 比红闸更坏，因为它提供虚假信心** | **是（本次发现）** |

---

## §1 先说做对了的（不能只列问题）

这几条是真的强，且**不是自夸，是实测出来的**，改进方案必须建立在保留它们的前提上。

### 1.1 验证设施齐备且分类清晰

```
scripts/fault-inject-*.py   13 个   —— 故意注入故障，证明「闸真的会红」
scripts/verify-*.py         13 个   —— 对活服务的端到端探针
scripts/mirror-check.py      1 个   —— Rust↔TS 契约逐字段核对
```

「证明闸有牙齿」这一步，绝大多数项目不做。本项目的 `fault-injection-verify` / `silent-failure-hunt` 两套方法论已经沉淀成 skill，这是**方法论级别的资产**。

### 1.2 真正的架构守卫存在，而且是代码不是文档

| 守卫 | 位置 | 守什么 |
| --- | --- | --- |
| 依赖纯净性 | `designer-react/src/canvas/kernel-purity.spec.ts` | 从 `CanvasDesigner.ts` 出发 BFS 全 import 链，断言**永不触及 `vue`/`pinia`**，且外部包在白名单内 |
| 跨框架行为等价 | `designer-react/src/stores/designer-contract.spec.ts` | React 状态层跑 13 个场景，与 Vue 录的 golden **逐字段**一致 |
| 跨语言形状一致 | `scripts/mirror-check.py` | 24 组 struct/interface 字段 + 纸张名清单 |
| 类型 | `scripts/ts-check.sh` · `ts-project-check.sh` | 见项目记忆 §十七 |

`kernel-purity.spec.ts:107` 那句 `expect(reachable.size).toBeGreaterThan(5) // 链路确实展开过（防止静默解析失败）` 是**教科书级的写法** —— 它防的是「守卫自己静默失效」，这正是本项目一贯的自觉。

### 1.3 展开/计算只有一份实现

Rust 是展开与计算的唯一实现，TS 侧只声明契约 + 显示辅助函数。`openprint/src/report/grid-report.ts` **一个 `import` 都没有**（自包含），所以它能被 `ts-test.sh` 拷到临时目录里裸跑。这个性质很值钱：它让「引擎层单测」不依赖 DOM / Univer / 构建工具链。

### 1.4 已知的失败模式被显式设计过

`print-server/src/report/store.rs:234` 的注释 `// 单个文件坏了不能让整个列表挂掉` 后面跟着实际容错分支（`store.rs:253` 把坏文件降级成 `name: "（文件损坏，无法解析）"` 的占位条目）。**想过失败模式并落了代码**，这是 §2.5 那条批评能成立的前提 —— 因为作者显然有能力做对，只是这一处漏了。

---

## §2 不足清单（按「影响 × 修复成本」降序）

### §2.1 【最重】闸门齐全，但没有跑器

**事实（可复现）**

```bash
# 1) 没有任何 shell 脚本调用过任何 .py
grep -rnE 'python3? .*\.py|mirror' scripts/*.sh scripts/*.mjs scripts/*.cjs
# → 无输出

# 2) 没有 CI
ls -a .github          # → 不存在

# 3) 没有聚合入口
ls scripts/ | grep -iE 'all|check-all|ci'   # → 无输出

# 4) 两个 package.json 的 scripts 里也没有
#    designer-react: dev/build/preview/test/test:watch/test:app/test:engine
#    openprint:      dev/build/preview/build-only/build:sdk/type-check/format
```

**后果**

- 27 个 `.py` 验证脚本（13 注入 + 13 探针 + mirror-check）**全部是手工仪式**。
- `mirror-check.py` 尤其危险：它是**唯一**能发现 Rust↔TS 字段漂移的闸，而它的 docstring 自己写着「本脚本在无法跑 tsc / vitest 的环境里充当契约检查的替代品」—— 即它诞生于沙箱限制，而不是设计。今天 TS 闸已经能跑了，它的价值反而更高了（tsc 查不了跨语言），但**跑它的成本还是「人记得」**。
- 更坏的是：`mirror-check.py` 是绿的（实测 24/24 OK），所以任何看它一眼的人都会得到「契约一致」的信心 —— 而这份信心在没人跑它的那段时间里是**空头**。

**为什么这条排第一**：它的修复成本是本次清单里最低的（一个 shell 脚本），但它决定了其余所有闸门是否真的在起作用。**没有跑器的闸 = 装饰品。**

**建议**：见 §4 第 1 档。同时把「本次会话是否跑过闸」变成 commit 前的一次调用，而不是一段记忆。

---

### §2.2 【重】Rust↔TS 契约只对「形状」，不对「语义」— ✅ **2026-09-27 已修（见 §8）**

> **现状（2026-09-27）**：`mirror-check.py` 已加**语义层**，8 条规则类事实进了闸
> （下表「现状」列标 ✅ 的即已覆盖）。本节正文保留**体检当时（2026-09-26）**的判断，
> 便于看出「哪里是真的缺口、哪里是当时的误判」——实施记录见 §8。

`mirror-check.py` 覆盖：24 组字段名 + 纸张名清单（`mirror-check.py:29-54, 152-160`）。

**体检时它不覆盖任何规则体/常量。** 而这些恰恰是两边各写一份、且注释里明确写着「改一处要改两处」的东西：

| 事实 | Rust | TS | 有 tripwire 吗（体检时） | 现状 |
| --- | --- | --- | --- | --- |
| 二维码/一维码字节上限 | `barcode.rs:52,59`（213 / 48） | `grid-report.ts:197-202` | **无** | ✅ `equal` |
| 码制别名表 | `barcode.rs:186`（`normalise_symbology`） | `grid-report.ts:211` | **无** | ✅ `ts_subset` |
| 图表类型白名单 | `chart.rs:30`（`KINDS`） | `grid-report.ts:301`（`CHART_KINDS`） | **无** | ✅ `equal` |
| 条件运算符 8 个 | `model.rs:163`（`CondOp::NAMES`） | `grid-report.ts:417` | 半有（见下） | ✅ `equal` |
| 表头行数判据 | `model.rs:1071`（`header_row_count`） | `grid-report.ts:1543`（`headerRowCount`） | 半有（见下） | ❌ **仍未覆盖**（见 §8 末尾） |
| 纸张名清单 | `model.rs:772-779` | `grid-report.ts:677` | **有**（mirror-check 对账 + `model.rs:1517` 内容钉子） | ✅ `equal` |
| Excel 页脚 255 字符上限 | `model.rs:1028` | `grid-report.ts:748` | **无** | ✅ `equal` |
| 样式颜色 `#RRGGBB` | `html_style_attr` | `grid-report.ts` | **无** | ❌ **仍未覆盖**（正则 + 双端报错，形态不同） |
| 码制白名单（`SYMBOLOGIES`） | `barcode.rs:40` | `grid-report.ts:188` | **无** | ✅ `equal` |
| 诊断 code 词表 | `issue.rs:139-149`（`CODE_*`） | `grid-report.ts:799`（`IssueCode`） | **无** | ✅ `set_equal` |

实测 tripwire 存在性：

```bash
# 只有 .len() 形式：图表 / 码制两张表**一个钉子都没有**
grep -rnE 'assert_eq!\((KINDS|SYMBOLOGIES)\.len\(\)' print-server/src/report   # No matches

# 条件运算符有（但只钉 Rust 数字，不验 TS）
grep -n 'CondOp::NAMES.len()' print-server/src/report/model.rs                 # 1422
```

> **⚠️ 我在这一节犯过一个错，必须留着。** 初版我写的判据是
> `grep -rnE 'assert_eq!\((PAPERS|KINDS|SYMBOLOGIES)\.len\(\)'` → 无匹配，
> 然后据此说「**纸张 / 图表 / 码制三张表都没有钉子**」。
> **纸张那句是错的。** 纸张表有一条**内容钉子**（比数量钉子更强）：
>
> ```rust
> // model.rs:1517  paper_table_is_pinned
> assert_eq!(PAPERS.to_vec(), expect, "纸张表变了：…");
> ```
>
> 它的写法是 `.to_vec()` 而不是 `.len()`，**我的 grep 只匹配了 `.len()`** → 静默漏掉。
> 这跟本报告 §5.2 那条 BSD grep 的坑是同一族：**`grep` 说「没有」时先怀疑 grep**。
> 教训：**判「有没有闸」不能只搜一种写法** —— 要搜符号名本身（`PAPERS`），再逐个看用法。

「半有」是什么意思：`model.rs:1422` 和表头行数那组钉子（`sample_template_has_two_header_rows` 等 6 条）确实存在，但它们钉的是 **Rust 侧的数字**，作用是「改了 Rust 会红，逼你去看注释」。**它们不验证 TS 侧**。所以：

- 在 **TS** 侧加一个纸张名 / 图表类型 / 改一个字节上限 → **Rust 侧毫无反应，两边静默分叉**。
- 在 **Rust** 侧改 `MAX_QR_BYTES` → **一条闸都不会红**（那张表根本没钉子）。

这正是本次会话已经在 AI 层**证明过**的失败模式（同一份事实被声明 13 / 9 / 8 次 → 必然漂移，见 `AI优先-差距分析与改进方案.md` §3.1）。**同一个病，换了个器官。**

**为什么严重**：分叉的症状是「服务端能编的纸张，设计器下拉里没有」—— **界面上看不出来**，且两边各自的行为都是「合理」的。这类 bug 只能靠对账发现，而唯一能对账的脚本没人跑（§2.1）。两条不足叠乘。

**建议**：见 §4 第 2 档。注意方向必须是**单向蕴含**，不是三方相等 —— `VALID_TYPES` 那种「故意更窄」的清单，用相等断言会永久红（本项目已经犯过一次，见 `AI优先…` §3.7 的自我更正）。

---

### §2.3 【重】引擎层的闸门，长在被消费方的配置里

**事实**

- `openprint/package.json` **没有 `test` 脚本**（scripts 只有 dev/build/preview/build-only/build:sdk/type-check/format）。
- 引擎层的 70 个 spec 之所以会被跑，是因为 `designer-react/vite.config.ts` 的 `test.include` 里有 `'../openprint/src/**/*.spec.ts'`。
- 同一份 `exclude` 里排除了 4 个：

```
../openprint/src/design/stores/dataSource.spec.ts
../openprint/src/design/stores/designer-duplicate.spec.ts
../openprint/src/design/stores/designer-field-bind.spec.ts
../openprint/src/design/stores/designer-contract.spec.ts
```

这 4 个被排除的理由是「属于 Vue 应用层，React 不再消费」（`vite.config.ts` 注释，方向决定 2026-09-12）。**理由成立、注释清楚**。但结果是：

- 这 4 个 spec **永久不被任何跑器执行**（openprint 自己没有 test 脚本，也没有 node_modules）。
- 其中 `designer-contract.spec.ts` 是 Vue 侧的 **golden 录制器**。`designer-react/src/stores/designer-contract.spec.ts:27` 的失败提示写着：
  `请先在 openprint 项目跑：npx vitest run src/design/stores/designer-contract.spec.ts`
  —— 而**这条路今天跑不通**（openprint 装不出 node_modules）。golden 文件 `openprint/src/contracts/golden/designer-v1.json`（49 012 B）自 `8ced46b`（初始化提交）之后再未被修改过，即**它已经无法再生成**。

**后果（分层说，不夸大）**

1. 「跑一次测试」这件事的定义，取决于 `designer-react` 的配置。谁 reorganize 一下 designer-react，引擎层的 70 个 spec 就**静默消失**，退出码照样 0。
2. 跨框架契约退化成**单向冻结**：React 侧只能「不改变行为」，因为 golden 不可再生。这在「React 重写已完工、Vue 层已弃用」的当下是可接受的取舍 —— **但它是一个没人明说的约束**，将来有人想合法地改状态层行为时会撞上一堵「改不动」的墙，而墙上的提示语指向一条走不通的路。
3. 4 个永久不跑的 spec 留在树里，**看起来像覆盖率**。

**建议**：见 §4 第 3 档。

---

### §2.4 【中】没有 workspace / 构建边界

**事实**

```bash
ls Cargo.toml package.json pnpm-workspace.yaml   # → 三者都不存在
```

- 根目录没有 `Cargo.toml` workspace，也没有根 `package.json`。**两个子项目都无法从仓库根构建**，必须先 `cd`。
- `designer-react` 通过 vite alias **直接引用 `openprint/src` 源码**（`designer-react/vite.config.ts` 的 `@` → `openprint/src`，因为引擎层内部都用 `@/xxx` 互相引用）。所以两边**没有版本边界、没有依赖声明、没有发布步骤** —— 耦合是隐式且完全的。
- 更实际的问题：`scripts/ts-test.sh:38-43` 硬编码了**仓库外**的借用依赖路径：

```
/Users/lushaohui/project/ontology/web/node_modules
/Users/lushaohui/project/ontology2/ui/node_modules
```

即「跑引擎层单测」依赖**本机文件系统布局**。`ts-test.sh:11-16` 还解释了为什么要把源码拷到临时目录、手写一份最简 vitest 配置 —— 因为真实工具链跑不起来。

**为什么算不足（而不是「沙箱环境所限，没办法」）**

这是真实的工程约束，我不否认。但它的架构含义是：**测试环境是真实环境的一个近似物**。近似物会漂（借用的是别项目的 vitest 版本、临时目录里没有真 tsconfig、`ts-test.sh` 只覆盖 `openprint/src/report/*.ts`）。所以「绿」的语义比看起来弱。这一点必须在文档里写明，而不是靠记忆。

**建议**：见 §4 第 3 档（低成本项：把借用路径变成显式环境变量 + 在 README 里写明「绿」的边界）。

---

### §2.5 【中】报表存盘不是原子的 —— 而正确写法就在同一个仓库里 — ✅ **2026-09-27 已修（见 §9）**

**事实**

```rust
// print-server/src/report/store.rs:300-301（修之前）
let text = serde_json::to_string_pretty(&def).map_err(...)?;
std::fs::write(&path, text).map_err(|e| format!("写入报表文件失败: {e}"))?;
```

```bash
grep -rnE 'fs::rename|tempfile|NamedTempFile|\.persist\(' print-server/src
# → print-server/src/config.rs:385:  std::fs::rename(&tmp, path)
```

**`config.rs` 已经在用「写临时文件 + rename」的原子写法**，`store.rs` 没有。同一个仓库、同一个作者、两种写法 —— 这不是「不知道」，是「漏了」。

**影响面（实测，不夸大）**

- 崩在写入中间 → 磁盘上是**截断的 JSON**。
- `store::list`（`store.rs:234-262`）**容错**：坏文件降级成 `"（文件损坏，无法解析）"` 占位条目，**列表不会整个挂掉**。所以爆炸半径是「一份报表」，且**是可见的**。
- 但 `store::load`（`store.rs:267`）会解析失败 → 这份报表**不可恢复**，用户最后一次保存的内容静默丢失。

> 🔴 **更正（2026-09-27）**：本节初版还写了第四条 ——
> 「`save()` 在写文件**之前**就赋好 `def.updated_at`（`store.rs:295`）→
> 崩了以后磁盘上的文件带着**旧**时间戳，**从列表里看不出这次保存失败过**。这是「静默」的那一半。」
>
> **前半句对，后半句的推论是错的。** `def.updated_at` 是对一个**局部变量**的赋值，
> 而 `save` 在写盘失败时返回 `Err`、`def` 随之被丢弃 —— 那个提前赋值
> **没有任何可观测后果**。至于「用户看不看得出失败」：写失败 → HTTP 400 →
> UI 弹出错误；进程直接崩 → 前端那个 `fetch` 会 reject → 同样弹错误。
> 两条路都**是看得见的**。
> 所以这条缺陷的性质是「**可见的数据丢失**」，不是静默失败 ——
> 严重性不低（丢的是用户资产），但归类不能含糊，否则会误导后面的人去
> 「修一个并不存在的静默」。

**建议**：见 §4 第 4 档。约 5 行，直接抄 `config.rs:385`。

**顺带发现的另一半（体检时没看到）**：`store::save` 的文档注释写着
「静默覆盖同名文件会丢东西，**所以调用方（UI）要先经列表确认**」——
而 `GridReportModal` 的 `saveReport` **从来没有确认过**，一次都没有，
UI 手上明明就有 `savedReports` 列表。**声明与实现漂移**，
而那才是真正的静默：手打一个已存在的 id 点保存，那份报表无声无息被换掉。
详见 §9。

---

### §2.6 【中】`mod.rs` 7753 行 = 2200 生产 + 5553 测试；生产里还塞了 ~660 行 demo 夹具

先把数字量准，避免「god file」这种含糊指控：

| 文件 | 总计 | 生产 | 测试 | 测试占比 |
| --- | --- | --- | --- | --- |
| `report/mod.rs` | 7753 | **2200** | **5553** | **72%** |
| `report/engine.rs` | 5094 | 3702 | 1392 | 27% |
| `report/barcode.rs` | 1644 | 244 | 1400 | **85%** |
| `report/xlsx.rs` | 1453 | 791 | 662 | 46% |
| `report/model.rs` | 1729 | 1322 | 407 | 24% |
| `report/store.rs` | 679 | 429 | 250 | 37% |

（命令见 §5.2。判据：`^(mod tests|#\[cfg\(test\)\])` 首次出现处切分。）

**修正一个容易说错的结论**：`mod.rs` 大，主要不是「生产代码没组织」，而是**测试模块跟生产代码同文件，占了 72%**。真正的生产部分是 2200 行，其中：

- `mod.rs:1545-2201`（约 **657 行**）是演示夹具：**6 个 `pub fn` 模板生成器** —— `sample_template()`(1571) / `cross_tab_template()`(1674) / `cross_tab_two_metrics_template()`(1742) / `cross_tab_totals_template()`(1823) / `cross_tab_two_metrics_totals_template()`(1918) / `cross_tab_multi_level_template()`(2032) —— 加 **3 个私有数据构造器** `sample_data()`(1545) / `multi_level_data()`(2162) / `cross_tab_data()`(2182)。它们**全部编译进二进制**，对应 `main.rs:204-218` 的 **9 条路由**（`/api/report/sample*` 与 `/api/report/cross-tab-*`）。
- 于是真正跑渲染管线的代码只有约 1540 行。

**问题（分两条，轻重不同）**

1. **demo 夹具住在生产源码里**（较重）：改一个演示模板要重编译 Rust。而且这 660 行占掉了 `mod.rs` 生产部分的 30%，是它读起来像 god module 的主因。
2. **测试与生产同文件**（较轻，纯机械）：拆出去零行为风险。

**建议**：见 §4 第 4/5 档。

---

### §2.7 【轻】`admin.html` 用 `include_str!` 编进二进制

```rust
// print-server/src/admin.rs:29
const ADMIN_HTML: &str = include_str!("admin.html");
```

后果：改一行管理界面 HTML 要**重编译 Rust**；而且这份 UI **对全部 TS 闸门不可见**（`ts-check.sh` / `ts-project-check.sh` 都不看它），所以它只能靠 `scripts/verify-admin-odbc-display.mjs` 那种手工探针守。

这与 §2.6 的夹具是同一种取向：**「资产进二进制」对部署友好，对迭代不友好**。作为取舍成立，但值得写进文档而不是留在代码里。

---

### §2.8 【轻】错误面不统一（沿用前文结论，不重复论证）

- 业务错误是**中文 `text/plain`**（`(StatusCode, String)`，`report/mod.rs` 里 20+ 处 `map_err`）；axum 提取器错误是**英文 serde 散文 + 422**。**没有错误码** → 调用方（含未来的 agent）只能字符串匹配。
- 时间格式不一致：`/api/reports` 的 `updatedAt` 是 UTC（`…Z`），`/health` 的 `time` 是本地（`+08:00`）。

详细证据与改进方向见 `AI优先-差距分析与改进方案.md` §3.4 / §3.5，此处只做交叉引用。

---

## §3 我查过、确认**不是**问题的（诚实边界）

不列出来，就会变成「只挑毛病」；列出来，下次不用重查。

| 曾怀疑 | 实测结论 |
| --- | --- |
| `mirror-check.py` 自身是否已漂 | **不漂**。实测 24/24 类型 OK + 纸张名 6 项一致，退出码 0 |
| TS 侧是否**重实现了展开/聚合**（会与 Rust 分叉） | **没有**。`grid-report.ts` 只有 `headerRowCount` / `parentChainOf` / `parentPosOf` 这类**显示辅助**，零 `import`；展开只有 Rust 一份 |
| 报表存盘坏了会不会连累整个列表 | **不会**。`store.rs:234-262` 有显式容错，坏文件降级为占位条目 |
| 跨框架契约是否已失效 | **没失效**。`designer-react/src/stores/designer-contract.spec.ts` 在 React 侧跑，13 个场景 + 逐字段比对；只是 golden 不可再生（§2.3） |
| 纸张名清单会不会漂 | **有闸**。`mirror-check.py:152-160` 专门对账，是 §2.2 里唯一被覆盖的规则类事实 |
| 表头行数会不会漂 | **有 Rust 侧钉子**（6 条单测），但只钉 Rust 数字，不验 TS（§2.2） |
| `openprint` 的 spec 是否「根本没被跑到」 | **被跑到**。`designer-react/vite.config.ts` 的 `include` 含 `../openprint/src/**/*.spec.ts`。**此前我在 `AI优先…` 初版里说「没有任何脚本会跑它」是错的，已在 §3.7 更正** |

---

## §4 建议（按成本分四档，越前越便宜）

### 第 1 档：加一个跑器（半天内，收益最大）— ✅ **2026-09-26 已做**

已落地 `scripts/check-all.sh`，把**已经存在**的闸串起来，顺序按「快 → 慢」。
**与本文初版方案的三处差异**（都是实现时发现的，比初版更严）：

1. **区分三种结果，不是两种。** 初版草稿只有「通过 / 失败」。
   实现时意识到这会踩本项目最忌讳的坑：`ts-project-check.sh` 在**借不到编译器**时
   `exit 2` —— 那是「**根本没检查**」，不是「检查过了没问题」（假绿），
   也不是「检查过了有问题」（假红，会让人去改本来没错的代码 —— `node-bin.sh` 里
   专门为此写过一段注释）。所以现在是 **0 通过 / 1 失败 / 2 没跑成**，
   整体退出码取最坏的一种，汇总里把「没跑成」单独列名。
2. **加了 `--fast`**（只跑 mirror-check + ts-check，3.5 秒）与 `--list`。
   没有它，一个 4~5 分钟的脚本会没人跑 —— 那就又回到「有闸没跑器」。
3. **顺序调整**：`ts-test.sh` 提到两个项目级类型检查之前（它比 `vue-tsc --build` 快得多），
   且每道闸打印耗时，便于发现哪道突然变慢。

实测（2026-09-26）：`--fast` 3.5s 全绿；全量见 §7 实测记录。

要点：
- **`mirror-check.py` 放第一个** —— 秒级、零依赖、且是唯一能发现 §2.2 那类漂移的闸。
- 13 个 `fault-inject-*.py` **不进 `check-all`**（它们要起服务、要改源码、跑得慢）。改成**按需**：新增/修改某特性时跑对应的那一个。但要在 README 里写清「哪条特性对应哪个注入脚本」的映射表 —— 现在这个映射只存在于作者脑子里。
- **有 CI 最好**（`.github/workflows/check.yml` 调 `check-all.sh`）。没有 CI 的话，`check-all.sh` 至少把「记得敲 6 条命令」降成「记得敲 1 条」。

### 第 2 档：把 `mirror-check.py` 从「形状」扩到「语义」（1 天）— ✅ **2026-09-27 已做**

在 `mirror-check.py` 里加规则类事实的抽取 + 比对，**方向一律单向蕴含**。
实做 8 条（比初版方案多 2 条：`SYMBOLOGIES` 白名单、诊断 code 词表），实施记录见 §8：

| 加什么 | 怎么抽 | 断言方向（实做） |
| --- | --- | --- |
| 纸张名 | 已有（`rust_paper_names` / `ts_paper_names`），移进事实表 | `equal` |
| 图表类型 | Rust `KINDS` ↔ TS `CHART_KINDS` | `equal` |
| 码制别名 | Rust `normalise_symbology` 的 match 臂 ↔ TS `normaliseSymbology` 的 if 链 | `ts_subset`（TS ⊆ Rust；更窄合法，**更宽必须红**） |
| 字节上限 | Rust `MAX_QR_BYTES`/`MAX_CODE128_BYTES` ↔ TS `BARCODE_MAX_BYTES` | `equal` |
| 条件运算符 | Rust `CondOp::NAMES` ↔ TS `CONDITION_OPS` | `equal` |
| 页脚上限 | Rust `escaped_len + 2 > 255` ↔ TS `escaped + 2 > 255` | `equal` |
| **码制白名单** | Rust `SYMBOLOGIES` ↔ TS `BARCODE_SYMBOLOGIES` | `equal` |
| **诊断 code 词表** | Rust `CODE_*` 常量 ↔ TS `IssueCode` 联合类型 | `set_equal`（顺序无意义） |

**为什么方向要单向**：本项目已经因为「三方相等」这个错误方向差点写出一个永久红的闸（`AI优先…` §3.7）。清单**故意更窄**是合法设计，用相等断言会把合法设计判成漂移。

**三种方向的判据**（写进 `Fact.mode` 的注释）：

- `equal` —— **顺序 + 内容**都要一致。用于**会被 TS 拿去渲染下拉/提示文案**的清单
  （`PAPER_NAMES.join(' / ')`、`CHART_KINDS.join(' / ')`）：顺序变了，用户看到的提示就变了。
- `set_equal` —— 集合相等，**顺序不算差异**。用于只做「认不认识」判断的清单（`IssueCode`）。
- `ts_subset` —— **TS 不允许比 Rust 宽**，允许更窄。用于别名表（TS 少认一个别名是合法收窄）。

**顺带**：给 `KINDS` / `SYMBOLOGIES` 补上 Rust 侧钉子 —— ✅ **2026-09-26 已做**，但改成了
**钉内容**而不是钉数量：`assert_eq!(KINDS.to_vec(), vec!["bar","line","pie"])`。
理由：`assert_eq!(X.len(), 3)` 对**改名**（`pie` → `donut`）毫无反应，而改名同样会让两边分叉。
这一点已由 `scripts/fault-inject-table-pins.py` 的两条「改名」注入证明（5/5 抓到）。

> ⚠️ **`PAPERS` 不用补** —— 它**本来就有**一条内容钉子（`model.rs:1517 paper_table_is_pinned`，
> 写的是 `.to_vec()` 比较）。本文初版说「三张表都没有钉子」是**错的**，原因见 §2.2 的更正框。
> 那一条只需把消息补全（原先只点了 HTML 尺寸与 Excel 纸张码，漏了 TS 的 `PAPER_NAMES`）—— 已补。


### 第 3 档：把闸门搬回它该在的地方（1 天）

1. **给 `openprint` 加 `test` 脚本**，让引擎层的闸不依赖 `designer-react` 的配置。哪怕只是转发：
   `"test": "vitest run"`（配一份自己的 `vitest.config.ts`，`include: ['src/**/*.spec.ts']`）。
2. **给 4 个永久不跑的 spec 一个明确归宿**：要么删除，要么挪到 `openprint/src/design/stores/__vue_legacy__/` 并在 README 写明「已废弃，不参与任何闸」。**别让它们继续伪装成覆盖率。**
3. **修正 `designer-contract.spec.ts:27` 那句走不通的提示语** —— 既然 golden 已不可再生，提示应该改成「golden 已冻结于 `8ced46b`，若需合法变更行为，须先恢复 Vue 端录制能力」，而不是指一条死路。
4. `ts-test.sh` 的借用路径改成显式环境变量优先（已有 `BORROWED_MODULES`，把两个硬编码候选降为「找不到时报错并提示」，而不是静默挑一个）。
5. **README 里写明「绿」的边界**：`ts-test.sh` 只覆盖 `openprint/src/report/*.ts`；引擎层其余部分靠 designer-react 的跑器；临时目录里的 vitest 配置是近似物。

### 第 4 档：结构性改进（按需，非紧急）

1. **原子写报表**：`store.rs:301` 换成 `config.rs:385` 那套（写临时文件 + `fs::rename`）。约 5 行。
2. **demo 夹具搬出生产源码**：`mod.rs:1545-2201` → `print-server/src/report/samples.rs`（或直接落成 `reports/*.json`）。`mod.rs` 生产部分立刻从 2200 降到 ~1540。
3. **测试模块拆出去**：`mod.rs` 的 5553 行测试 → `report/tests/`。纯机械，零行为风险。
4. **加根 workspace**：`Cargo.toml`（workspace）+ 根 `package.json`（scripts 转发）。让「从根构建/测试」成立，也给第 1 档的 `check-all.sh` 一个自然的家。
5. **`admin.html` 改为运行时读盘**（或至少在文档里写明「改它要重编译」）。

---

## §5 复现命令附录

### §5.1 证明「没有跑器」

```bash
cd /Users/lushaohui/project/report
grep -rnE 'python3? .*\.py|mirror' scripts/*.sh scripts/*.mjs scripts/*.cjs   # 无输出
ls -a .github                                                                 # 不存在
ls scripts/ | grep -iE 'all|check-all|ci'                                     # 无输出
ls scripts/fault-inject-*.py | wc -l    # 13
ls scripts/verify-*.py | wc -l          # 13
python3 scripts/mirror-check.py         # 24/24 OK + 纸张名 6 项，退出码 0
```

### §5.2 量「生产 vs 测试」

```bash
cd /Users/lushaohui/project/report/print-server/src/report
for f in mod.rs engine.rs model.rs store.rs xlsx.rs barcode.rs chart.rs docx.rs; do
  total=$(wc -l < $f)
  tstart=$(grep -nE '^(mod tests|#\[cfg\(test\)\])' $f | head -1 | cut -d: -f1)
  printf "%-12s total=%-6s prod=%-6s tests=%s\n" "$f" "$total" "$((tstart-1))" "$((total-tstart+1))"
done
```

> ⚠️ 必须用 `grep -E`。本机是 BSD grep，`grep 'a\|b'` 在 BRE 下把 `\|` 当字面量，**静默不匹配** —— 我本次就因此先得到「没有测试模块」的错误结论，改用 `-E` 后才对。见项目记忆「grep 的 `\|` 不可用」。

### §5.3 证明 §2.2 的覆盖缺口

```bash
cd /Users/lushaohui/project/report
# 图表 / 码制两张表连数量钉子都没有（注意：**纸张表有内容钉子**，见 §2.2 的更正）
grep -rnE 'assert_eq!\((KINDS|SYMBOLOGIES)\.len\(\)' print-server/src/report   # No matches
grep -n 'paper_table_is_pinned' print-server/src/report/model.rs               # 1517（纸张表其实有）
# 条件运算符有（但只钉 Rust 数字）
grep -n 'CondOp::NAMES.len()' print-server/src/report/model.rs                 # 1422
# 纸张名是唯一被 mirror-check 覆盖的规则类事实
grep -n '纸张名清单' scripts/mirror-check.py                                    # 152
```

### §5.4 证明 §2.5

```bash
cd /Users/lushaohui/project/report
sed -n '298,302p' print-server/src/report/store.rs     # std::fs::write
grep -rnE 'fs::rename|tempfile|NamedTempFile|\.persist\(' print-server/src   # config.rs:385 有
sed -n '/pub fn list/,/^}/p' print-server/src/report/store.rs  # 容错分支
```

### §5.5 证明 §2.3

```bash
cd /Users/lushaohui/project/report
python3 -c "import json;print(json.load(open('openprint/package.json')).get('scripts'))"   # 无 test
sed -n '/test:/,/},/p' designer-react/vite.config.ts                                       # include/exclude
git log --oneline -- openprint/src/contracts/golden/designer-v1.json                        # 只有 8ced46b
```

---

## §6 我没做的（边界声明）

- **没有修改任何代码**。本次是体检，不是修复；§4 只是方案。
- **没有跑 13 个 `fault-inject-*.py` 与 13 个 `verify-*.py`**（它们要起服务、要改源码）。§2.1 的结论来自**静态事实**（没有任何脚本调用它们），不依赖它们是否通过。
- **没有验证 `designer-contract` 的 golden 是否与当前 React 行为真的一致** —— 那要跑 `designer-react` 的 vitest（会起 happy-dom + antd，慢）。§2.3 的结论只到「golden 不可再生」这一步，不断言它是否已过期。
- **没有评估 `openprint/src/design/**`（25 488 行）的内部结构** —— 那是 Vue 应用层，方向已定（React 重写完工），本次只统计了体量。
- §2.2 那张表里的「TS 侧对应实现」我读了 `barcodeProblem` / `chartProblem` / `conditionalProblem` / `pageSetupProblem` / `headerRowCount` 的函数体，**当前内容与 Rust 是逐条对齐的**（连 `code128` 的码集判据顺序、纸张名大小写不敏感、方向只 trim 不忽略大小写都对齐了）。**即：现在没漂，缺的是防止将来漂的闸。** 这一点必须说清楚，免得被读成「已经坏了」。

---

## §7 第 1 档的实施记录（2026-09-26）

§6 说的是「体检那天没改代码」。这一节记录**之后按 §4 第 1 档真的动手**的部分。

### 7.1 新增 `scripts/check-all.sh`

把已有的闸串起来，**按「便宜优先」排序**（不是为了快，是为了出错时几十秒内就看到）：

| # | 闸 | 实测耗时（热） |
| --- | --- | --- |
| 1 | `python3 scripts/mirror-check.py` | 1s |
| 2 | `bash scripts/ts-check.sh` | 2s |
| 3 | `bash scripts/ts-test.sh` | 5s |
| 4 | `bash scripts/ts-project-check.sh`（designer-react） | 18s |
| 5 | `bash scripts/ts-project-check.sh openprint`（`vue-tsc --build`） | 6s |
| 6 | `cargo test --bin print-server` | 4s（暖）／1m33s（冷编译） |

全量约 **36 秒**（热），比预想快得多 —— 之前「慢」的印象来自冷启动。

**三种结果，故意不合并成两种**（这是实现时对初版方案最重要的修正）：

```
0  通过     —— 真的检查过了，没问题
1  失败     —— 检查过了，有问题
2  没跑成   —— 环境缺东西（借不到 tsc / vitest / node / cargo），**根本没检查**
```

「没跑成」算成通过是**假绿**（最坏）；算成失败是**假红**（会让人去改本来没错的代码 ——
`node-bin.sh` 里专门为此写过一段注释）。所以分开报，整体退出码取最坏的一种。

**实测三态**（2026-09-26）：

```bash
bash scripts/check-all.sh                              # → 退出码 0，通过 6 · 失败 0 · 没跑成 0
PATH=$(echo "$PATH" | tr ':' '\n' | grep -v rustup | paste -sd: -) \
  bash scripts/check-all.sh                            # → 退出码 2
#   ⚠ 没跑成：找不到 `cargo`
#   通过 5 · 失败 0 · 没跑成 1
#   没跑成：cargo test
#   有闸没跑成 —— **不等于通过**，先把环境补齐再下结论
```

另有 `--fast`（只跑 1、2，3.5 秒）与 `--list`。
**没有 `--fast` 的话，一个 36 秒的脚本会没人跑 —— 那就又回到「有闸没跑器」。**

### 7.2 新增 `scripts/fault-inject-table-pins.py`（5 条注入，5/5 抓到）

证明新加的表钉子**真的有牙齿**，而不是恒真断言。

| 注入 | 期望被谁抓 |
| --- | --- |
| `KINDS` 加一项（+donut） | `kinds_table_is_pinned_and_names_the_ts_mirror` |
| `KINDS` **改名**（pie→donut，数量不变） | 同上 |
| `SYMBOLOGIES` 加一项（+code39） | `symbology_table_is_pinned_and_names_the_ts_mirror` |
| `SYMBOLOGIES` **改名**（code128→code-128，数量不变） | 同上 |
| `PAPERS` 加一项（+A2） | `paper_table_is_pinned`（已有那条） |

两条「**改名**」是这次的关键：它们证明**钉内容比钉数量强** ——
`assert_eq!(X.len(), 3)` 对改名毫无反应。文件全部逐字节还原（脚本内比对 sha256）。

脚本里还防了一个坑：**`cargo test <过滤名>` 匹配不到任何用例时仍然返回 0**，
所以打错用例名会伪装成「通过」。现在会数输出里真的出现了几条 `... ok` / `... FAILED`，
匹配不到就报「**没验过**」，而不是当成绿。

### 7.3 修了 3 个闸脚本自己的输出被静默吃字节（顺手发现，与本档无关但必须修）

跑 `check-all.sh` 时发现日志里有非法 UTF-8 字节。追下去是本机 **bash 3.2.57** 的坑：

```bash
V=abc; echo "测试 $V）"      # → 「测试 」+ 乱码，abc 与 ）的头一字节都没了
V=abc; echo "测试 ${V}）"    # → 「测试 abc）」正确
```

**「`$VAR` 紧跟一个非 ASCII 字符」会静默吃掉变量的值 + 多字节字的第一个字节。**
退出码正常、脚本照跑，只是**打印出来的数字凭空消失**：
`检查 31 个文件（源码 13 / 测试 18）` 变成 `测试 ）`。

受影响的是 `ts-check.sh:69`、`ts-project-check.sh:69`、`ts-project-check.sh:76`（共 4 处），
以及我新写的 `check-all.sh:66`。已全部改成 `${VAR}` 并就地写明原因（防止被「顺手简化」回去）。

**为什么这条值得单独记**：它出在**闸脚本自己的状态行**上 ——
也就是「本该说真话的那一行」。一个会少印一个数字的状态行，正是本项目最忌讳的那类静默。
排查手法：搜 `\$[A-Za-z_]\w*(?=[\x80-\xff])`（`\$` 转义的不算，`$(...)` 不受影响）。

### 7.4 更正：`PAPERS` 本来就有钉子（§2.2 那处判断是错的）

详见 §2.2 的更正框。一句话：`paper_table_is_pinned`（`model.rs:1517`）用的是
`assert_eq!(PAPERS.to_vec(), expect, …)`，**是内容钉子，比数量钉子更强**；
我初版只 grep 了 `.len()` 形式 → 静默漏掉 → 错说成「三张表都没有钉子」。
**`KINDS` 与 `SYMBOLOGIES` 那两句是对的**（它们确实一个钉子都没有，现已补上）。

### 7.5 仍未做

> 先更正一句：本节写于 2026-09-26，当时列的第一条「第 2 档没做」**已于 2026-09-27 做掉**，
> 见 §8。下面保留其余未做项。

- 第 3 档（`openprint` 自己的 `test` 脚本、4 个永久不跑的 spec、golden 提示语）**没做**。
- 第 4 档：**第 1 条（原子写报表）已于 2026-09-27 做掉，见 §9**；
  夹具搬出 `mod.rs`、测试模块拆出、根 workspace **没做**。
- `check-all.sh` **没挂 CI** —— 本仓库没有 `.github`，加 CI 需要先定用哪套 runner。

---

## §8 第 2 档的实施记录（2026-09-27）

### 8.1 结论先写

`scripts/mirror-check.py` 从**单层**（字段名形状）变成**两层**：

```
A. 形状层   24 组 (Rust 文件, Rust 类型, TS 类型) —— 只比字段名。行为与之前一致。
B. 语义层    8 条规则类事实 —— 比「白名单 / 上限 / 别名」的内容，每条自带方向。
```

实测：`python3 scripts/mirror-check.py` → 24 组形状 `OK` + **8 条语义 `OK`**，
末行 `结果： 镜像一致（形状 + 语义）`，退出码 0。

**这补上的正是 §2.2 那句话的缺口**：以前「在 TS 侧单独改一个字节上限 → Rust 侧毫无反应」，
现在会红，且**报出是哪一条事实**。

### 8.2 一个决定成败的设计：抽取失败必须红，不能静默跳过

这是本档唯一真正需要设计的点，也是最容易写错的地方。

每条事实由一个 `Fact` 描述：

```python
@dataclass
class Fact:
    label: str
    rust: Callable[[Path], Optional[list[str]]]
    rust_path: Path
    ts: Callable[[Path], Optional[list[str]]]
    mode: str   # equal | set_equal | ts_subset
    note: str   # 为什么是这个方向
```

注意返回类型是 **`Optional[list[str]]`** —— **`None` 不是「空」，是「抽取失败」**，
而抽取失败**计为红**。理由写在文件里：

> 静默跳过会让这条检查退化成「永远绿、但根本没在检查」——
> 比红闸更坏，因为它产出的是**空头的信心**。

配套的 `_one()` 要求锚点**恰好匹配 1 处**：

```python
def _one(pattern: str, text: str, flags: int = 0) -> Optional[re.Match]:
    """**恰好匹配 1 处**才返回，否则 None（= 抽取失败）。"""
    ms = list(re.finditer(pattern, text, flags))
    if len(ms) != 1:
        return None
    return ms[0]
```

匹配 **0 处**（常量被改名）和 **≥2 处**（正则写松了）**都算失败**。

**「≥2 处」这一半不是假想的**：`engine.rs` 里在 4 个测试函数内各自定义了局部的
`const KINDS`（`:4559 :4803 :4852 :4921`）。如果抽取器图省事做**全仓搜索**而不是
锁定 `chart.rs`，就会匹配到 5 处 —— 而「取第一个」的写法会**静默取到测试里的那张表**。
`_one()` 把这种情况变成红。

### 8.3 另一个自己踩出来的坑：`{` 出现在字符串里

取函数体靠数花括号（`_fn_body`），而 Rust 的 `normalise_symbology` 里有
`format!("…{raw}…", …)` 和 `"{}"` —— **字符串字面量里的 `{` 会让计数错位**，
切出错误的函数体，可能给出**假 OK**。

修法是先**把字符串内容抹成等长空格**再数括号（`_blank_strings`，长度不变所以索引不漂）：

```python
src = re.sub(r'"(?:\\.|[^"\\])*"', repl, src)   # Rust / TS 双引号
src = re.sub(r"'(?:\\.|[^'\\])*'", repl, src)   # TS 单引号
src = re.sub(r"`(?:\\.|[^`\\])*`", repl, src)   # TS 模板串
```

### 8.4 8 条事实与实测值

| 事实 | 方向 | 实测抽取结果 |
| --- | --- | --- |
| 纸张名清单 | `equal` | `['A3','A4','A5','B5','Letter','Legal']` |
| 图表类型 | `equal` | `['bar','line','pie']` |
| 码制白名单 | `equal` | `['qr','code128']` |
| 字节上限 | `equal` | `['qr=213','code128=48']` |
| 条件运算符 | `equal` | 8 项 |
| 码制别名 | `ts_subset` | 两边各 7 项 |
| 页脚字符上限 | `equal` | `255` |
| 诊断 code 词表 | `set_equal` | 两边各 6 项 |

### 8.5 故障注入：9 条，9/9（`scripts/fault-inject-mirror-semantics.py`）

新检查必须**自证有牙齿**，否则又是一条恒真断言。9 条注入分三类：

| # | 注入 | 期望 | 实测 |
| --- | --- | --- | --- |
| 1 | TS 图表类型 `pie` → `donut` | 红，报「图表类型」 | ✅ |
| 2 | TS `qr: 213` → `240` | 红，报「字节上限」 | ✅ |
| 3 | TS 删一个条件运算符（`not_between`） | 红，报「条件运算符」 | ✅ |
| 4 | TS 删一个纸张名（`Legal`） | 红，报「纸张名清单」 | ✅ |
| 5 | TS 删一个 `IssueCode`（`generic`） | 红，报「诊断 code 词表」 | ✅ |
| 6 | TS 页脚上限 `> 255` → `> 250` | 红，报「页脚字符上限」 | ✅ |
| 7 | TS 码制别名**更宽**（加 Rust 不认的别名） | 红，报「码制别名」 | ✅ |
| 8 | TS 码制别名**更窄**（删 `qr-code`） | **仍然绿** | ✅ 绿（对照组） |
| 9 | Rust `KINDS` → `KINDS_RUST`（改锚点） | 红，报「**抽取失败**」 | ✅ |

还原后 sha256 逐字节一致，基线回到 `exit=0` 全绿。

**第 8 条是这一组里最重要的**：它是**对照组**。
前 7 条证明「改了事实会红」，第 8 条证明「**合法的更窄不会被误报**」——
也就是**方向没有写反**。没有它，前 7 条全绿也可能只是「碰巧都改宽了」。

**第 9 条证明的是元性质**：它**没有改任何事实**，只改了锚点。它绿的话说明
「抽取失败」这条路径真的会走到红，而不是静默当成「两边一致」。
没有它，前 7 条全绿也可能只是「恰好锚点还在、正则还松」。

> 这条纪律与 §15.4b（另一份报告里）那条同源：**故障注入的判据要细到「哪一条断言 / 哪一条事实」**，
> 否则不同的注入会被算成同一条证据。

### 8.6 本档**没有**覆盖的（边界，别当成已覆盖）

- **表头行数判据**（`model.rs:1071` ↔ `grid-report.ts:1543`）**没进语义层**。
  它不是「一张表」而是一段**判据**（从第一行起连续「既无 `expand_type=r` 也无 `row_parent`」），
  抽成事实要跑两边的代码，不是正则能做的。它目前仍只有 Rust 侧的内容钉子
  （`sample_template_has_two_header_rows` 等）——**只改 TS 一侧仍然无人拦**。
- **样式颜色 `#RRGGBB`** 没进。两边形态不同（Rust 是 `html_style_attr` 里的校验，
  TS 是一组正则），而且它是**两端点都报 400** 的那种，抽成「清单」不自然。
- **语义层只覆盖「声明」，不覆盖「用法」**：闸能证明两边**声明了同一张表**，
  不能证明两边**用同一套逻辑去用它**（比如判据顺序、trim / 大小写敏感度）。
  这一层仍然靠 `§6` 末尾那条人工核对结论。
- 因此 §2.2 的标题**仍然成立**（契约只有一部分进了闸），只是缺口小了一圈。

---

## §9 第 4 档第 1 条的实施记录：保存不再毁用户的报表（2026-09-27）

### 9.1 结论先写

「保存」这条路上有两个**独立**的毁数据缺口，这次一起修掉：

| # | 缺口 | 性质 | 修法 |
| --- | --- | --- | --- |
| **A** | 手打一个**已存在**的 id 点保存 → 那份报表被**无声无息换掉** | **静默**（界面上只多一句「已保存模板」） | UI 覆盖前确认（`GridReportModal`） |
| **B** | 写到一半崩 / 磁盘满 → 文件变**截断 JSON**，旧内容没有备份，不可恢复 | **可见**的数据丢失（列表显示「文件损坏」） | 原子写：`.bak` + `.tmp` + `rename` |

**A 是这次真正的发现**，而且它不在 §2.5 里 —— §2.5 只写了 B（非原子写）。
A 是顺着 B 的代码读出来的：`store::save` 的文档注释写着

> 保存。**覆盖写**：报表是用户的资产，静默覆盖同名文件会丢东西，
> **所以调用方（UI）要先经列表确认**

而 `saveReport` **一次确认都没有**，UI 手上明明就有 `savedReports` 列表。
**声明与实现漂移** —— 这正是 §2.2 / §2.3 那一族病的第三个器官。

> 教训：**读到一个「所以调用方要 X」的注释时，去调用方看一眼 X 做了没有。**
> 这种注释很容易在重构里变成一句**没人执行的空头承诺**，而且它比没有注释更坏 ——
> 它会让后来的人**以为这件事已经处理过了**。

### 9.2 服务端：原子写

`store.rs` 新增 `write_atomic()`（与 `config.rs::ServerConfig::save` 同一套做法）：
先备份 `.bak` → 写 `.tmp` → `rename` 覆盖。

**为什么 `.bak` 是必须的**（不是顺手加的）：`rename` 保证的是「不会出现半成品」，
它**不保证**「覆盖是可逆的」。A 那道确认只能防「手滑」，
防不了「改错了想退回去」——留一份上一版才有手工恢复的路。

**新引入的风险，也一并钉住**：`.bak` / `.tmp` 不能被 `list()` 当成报表读进来。
`list()` 的判据是 `extension() == Some("json")`，而 `foo.json.bak` 的扩展名是 `bak`
→ 天然跳过。但**改成 `file_name().ends_with(".json")` 还不够**（它同样跳过），
真正会漏的是 `contains(".json")` —— 那时列表里会冒出一份「id 对、内容是上一版」的
幽灵报表。有单测钉住，注入 C 专门证明它会红。

### 9.3 客户端：覆盖前确认

判据（写在 `saveReport` 里）：

```
id !== lastSavedId  &&  (!reportsListKnown || 列表里有这个 id)
```

拆开看每一半为什么必须有：

- `id !== lastSavedId` —— 打开 A 再存 A 是**正常操作**。每次都弹框会把用户
  训练成闭眼点确定，那这道确认就**白做了**（比没有更坏：多了一次点击，少了一层保护）。
  `lastSavedId` 在「打开报表」和「保存成功」时更新。
- `!reportsListKnown || ...` —— 列表**读不到**时它也是空数组，而「空数组」和
  「一份报表都没有」**在数据上完全一样**。只按数组判，就会在拉列表失败时**静默放行**。
  所以拉失败时把 `reportsListKnown` 置回 `false`，判据退化成「不确定就问」。

确认框里写明**要换掉的是哪一份**（拿列表里的展示名），以及上一版留在哪个 `.bak` 里。

**已知边界（明写，别当成已覆盖）**：这份列表是**打开弹窗那一刻的快照**。
别的客户端在这之后新建的同名报表，这里看不见。
真正的兜底要做在服务端（已存在且没带 `force` 就返回 409）—— 那是改 API 的事，
**这次没做**。

### 9.4 验证：两条路各一套闸，都要证明有牙齿

**Rust 侧（4 条单测 + 1 个真机探针）**

单测覆盖的是「写入失败」这一路径 —— 把 `.tmp` 的路径预先占成**目录**，
`fs::write` 就 `EISDIR` 失败。4 条：写入失败时旧文件完好（含时间戳没变）/
覆盖时留 `.bak` / 成功后不残留 `.tmp` / `.bak`-`.tmp` 不进列表。

**但单测覆盖不了最要命的那一种：进程写到一半直接死掉。**
那一瞬间 `fs::write` 已经把目标文件 `truncate` 成 0 了 —— 而 `truncate` 不可逆。
要复现必须让**真进程**在**真写盘**中途被杀，所以新增
`scripts/verify-atomic-save.py`：用 `RLIMIT_FSIZE` + SIGXFSZ
（写超过上限 → 内核发信号 → 默认处置就是终止进程），存一份 1 MiB 的报表，
服务必死在中途，然后断言目标文件**逐字节仍是上一版**。

判据是**字节相同**，不是「能解析」—— 后者在旧内容恰好也合法时会给出假绿。

> ⚠️ **踩了一个坑，记在这儿**：探针第一次跑出来是
> 「服务进程已退出 = True」**同时**「写入成功、文件是完整的新版」——
> 自相矛盾。根因是 18888 上**留着上一轮的一个 print-server**：
> 我起的那个（带着限额）在 bind 时失败退出了，而 health 检查与 PUT
> 全被旧进程答上了，那个进程没有限额，于是写了个完整的大文件。
> **症状是「服务死了但活干完了」**。
> 修法两步：起服务前 `pkill` 清端口；**并且确认占着端口的 PID 就是我起的那个**
> （只查「端口有人答话」不够 —— 那正是这个坑的入口）。
> 这条在 `fault-injection-verify` 的反面模式里写着（「复用上一轮的端口而不先杀进程」），
> 我这次是又踩了一遍。

**闸：`scripts/fault-inject-atomic-save.py` → 4 道门禁 4/4**（注入 A 同时挂单测与探针两道）：

| 注入 | 拆掉什么 | 被谁抓到 |
| --- | --- | --- |
| A | 写 `.tmp` + `rename` → 直接写目标文件 | 单测**和**真机探针 |
| B | 删掉 `.bak` 拷贝 | `覆盖时留下上一版备份` |
| C | 列表判据 `extension == json` → `contains(".json")` | `备份与临时文件不进报表列表` |

**A 的锚点只包住「原子性」那三行，不碰 `create_dir_all`。** 这条是踩出来的：
第一版把 `save()` 里整句 `write_atomic(&path, &text)?;` 换成 `fs::write(&path, text)?`，
结果**连 `create_dir_all` 一起删掉了**，于是 `由配置路径推出的目录能存能列` 也红了 ——
而它红的原因是 `No such file or directory`（目录都没建），**跟原子性毫无关系**。
那种注入会让人误以为「有 3 条用例守住了原子性」，其实只有 2 条。
**注入的锚点要恰好包住被测的那个机制，多一行都是噪声。**

**TS 侧（6 条用例 + 6 条注入）**

`designer-react/src/modals/grid-report-save-confirm.spec.tsx`：
目标已存在 → 弹确认且**0 个 save 请求**（契约在机制之前断言）/
点覆盖 → 真的发出去且 body 的 id 对 / 点取消 → 0 请求且再点保存会重新问 /
**三条对照组必须不弹**（列表里没有 / 确认过一次 / 列表读不到时**要弹**）。

`scripts/fault-inject-save-confirm.py` → **6 条注入 6/6**，每条拆**一个子句**：

| 注入 | 期望红在哪 |
| --- | --- |
| 1 判据整段删掉 | 已存在却发出请求 |
| 2 后半句改成恒真（**每次都弹**） | 对照组「id 不存在 → 不弹」 |
| 3 去掉 `reportsListKnown` | 「列表读不到 → 仍然弹」 |
| 4 去掉 `lastSavedId` | 对照组「确认过一次 → 不再弹」 |
| 5 `onOk` 里不落盘 | 「点覆盖 → 请求发出去」 |
| 6 `onCancel` 干起了 `onOk` 的活 | 「点取消 → 请求为 0」 |

**注入 2 与 4 是这组里最关键的**：它们证明那三条**对照组**不是摆设。
没有它们，「每次都弹框」和「只有覆盖才弹」在用例上分不开 ——
而「每次都弹」正是会让用户闭眼点确定、把这道闸作废的那种实现。

注入 5、6 拆的是**按钮接线**而不是判据：判据对了、按钮没接上也照样是 bug。

脚本除了比对「哪条用例红了」，还比对**红的必须是我期望的那条** ——
不区分的话，一条无关用例红也能冒充「注入被抓到」。

**踩到的第二个坑（jsdom 特有）**：`confirmVisible()` 一开始写的是
「DOM 里有没有那个确认按钮」，结果 3 条用例同时红在「框应该关掉」上。
根因是 **jsdom 里 rc-motion 的离场动画永远不会结束**（没有真实 `transitionend`），
`destroyOnHidden` 等不到 `afterClose`，**关掉的框照样留在 DOM 里**，
只是 `.ant-modal` 上多了 `ant-zoom-leave ant-zoom-leave-active`。
拿存在性当判据会得到「永远开着」的假象 —— 于是「该弹」那条**恒真**、「不该弹」那条**恒假**，
两边都等于没测。现在改看 rc-motion 的状态类（带 `-leave` 才算关）。

### 9.5 本档没做的（边界）

- **服务端的 409 + `force`**：把「覆盖要确认」这件事从「客户端自觉」变成
  「服务端强制」，才能覆盖「列表快照过期 / 别的客户端刚建了同名」。
  要改 API 与 UI 流程，这次没做。
- **`.bak` 的恢复入口**：目前只能手工去服务端报表目录取。UI 上没有「还原上一版」。
- **`.bak` 只有一代**：连续存两次就把中间那版覆盖掉了。