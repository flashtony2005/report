# 报表 PDF 资料通读笔记（总索引）

> 2026-09-12 通读。工作区根目录那批 PDF 共 12 份 + 1 份 docx，去掉重复后是 **7 份独立资料**。
> OCR 全文产物在 `pdf-text/`，逐份详尽的笔记在 `pdf-text/digest-*.md`，本文件是总索引与交叉结论。

## 一、资料清单与处理状态

| 资料 | 页数 | 性质 | 状态 |
|---|---|---|---|
| 基于单元格扩展的报表引擎的研究与实现（= `报表.pdf`，同一篇硕士论文的两个副本） | 249（正文 82，164 页为空白重复页） | 扫描件 | 已 OCR 去重 → `pdf-text/基于单元格扩展的报表引擎-去重.txt` |
| 报表引擎设计（广联达 周金根，2010-02-25） | 78 | 扫描件 | 已 OCR → 笔记 `digest-报表引擎设计.md` |
| 报表引擎设计(20160305112551) | 78 | 同上，疑似重导出副本 | 未单独处理（md5 不同，内容应同源） |
| 报表引擎系统研究开发（西安理工 裴晓华，2009） | 84 | 扫描件 | 已 OCR → 笔记 `digest-报表引擎系统研究开发.md` |
| 非线性报表模型原理(1) / 原理.docx | 133 | 蒋步星原著，已有完整文本 | **已由 `非线性报表设计详解.md` 覆盖**，未重复处理 |
| 非线性报表模型-展现 / 数据 / 运算 / 子报表模型 | 2 / 4 / 12 / 5 | 润乾官方文档，有文字层 | 已读，要点见第二节 |
| 填报表模型-扩展 / 采集模型 | 3 / 11 | 润乾官方文档，有文字层 | 已读，要点见第二节 |

说明：
- 根目录原有的 `extracted/*.txt` 与 `ocr/`（50 页截图+文本）是**早期低质量 OCR 产物**，中文识别几乎不可用（如"纵展主格"→"纵展主格"级错乱），已弃用。
- 本次改用 macOS Vision 框架 OCR（150 dpi 渲染 + `zh-Hans` 精确模式），识别质量可用。工具在 `/tmp/report_tools/`（`ocr.swift` + `run_ocr.sh`），临时目录会被清理，需要长期用可移进仓库。

## 二、润乾官方文档要点（有文字层，直接读取）

这几份是润乾（raqsoft）官网文档，定义了「非线性报表」这套术语体系，与蒋步星的书互为表里——书讲形式化算法，文档讲产品语义。

### 展现模型
- 报表 = **表格 + 格中数据 + 数据间运算逻辑**；采用类 Excel 的 N 行 M 列矩形格子平面图。
- 单元格命名 `列号+行号`；合并格以**左上角**格编号命名。
- 强调：展现模型直接决定绘制效率，且**数据处理模型对展现模型有依赖性**。

### 数据模型
- 数据集 = 由数据数组构成的二维表，同列同类型；行为「记录」列为「字段」（刻意与报表的行/列区分）。
- `#0` 表示行号列；**行集**是记录按某种顺序构成的数组（数据集本身也是行集）；分组 = 划分成 N 个行集。
- 数据集函数 `dsName.f(...)`。
- **多源是本质特性**：多个数据集不做 union/join 整合，直接在报表运算中关联。文档明确指出「很多号称支持多数据集的传统工具其实是把多个集整合成一个再算，实质仍是单数据集模型」——整合运算吃 CPU、放大数据量。这条是我们自研必须坚持的架构红线。

### 运算模型（最有实现价值的一份）
- **层次坐标**：`cellx[Lk:lk, ..., L1:l1; Tk:tk, ..., T1:t1]`，`` `0 `` 代表根坐标。
- `&cellx`：层次坐标的**逆运算**，返回当前格所属 cellx 主格在扩展结果中的排位。
- **格集**：`cellx[坐标]{}` —— 层次坐标有 `{}` 返回集合，无 `{}` 只返回范围内第一个格。
  - 固定格：`list(C1,C2,...)`、`[Cellx:Celly]` 矩形（**含 link 操作符的格不允许设为扩展格**）。
- **条件表达式**：`cellx[坐标]{条件}`，用于同比/环比这类"坐标定位不到"的场景。
- **`$` 运算符**：条件表达式中 `$B2` 指**当前格**的 B2 主格，裸 `B2` 指**目标格**的 B2 主格。经典写法：
  `C2 - C2[A2:-1]{$B2==B2}`（比上年同月）。没有 `$` 就区分不了当前格和目标格——这个设计很精巧，值得直接抄。
- **空白单元格**：不存任何属性、只占位保矩形，几乎不占内存；扩展时**优先占用**紧邻右/下的空白格。
- **分页时伸缩**：其他区域从右往左找第一个设了"分页时伸缩"的格拉大，没设就拉最右边一格；其余格位置不变。合并格**不能跨区域**，否则分页混乱。
- 区域顺序：横向 左表头→常规列→右表头；纵向 分页页眉→头标题→报表头→分组表头→数据区→表尾区→尾标题→分页页脚。

### 子报表模型
- **引入式子报表**：以当前格为扩展格向右下扩展，占用主表空白行列、接受主表行高列宽，扩展后仍是统一二维矩形 → 主表**不能**访问子表值。
- **嵌入式子报表**：始终缩在当前格内或仅撑大当前格 → 格中嵌表、格线不对齐 → **不能导出 Excel**（Excel 不支持格中嵌表）。取值用 `eval(StringExp, SubRptExp)`，如 `eval("B2+10", A1)`。
- 主子表**共用运算环境**：参数/数据集重名即共用，内容不同则不得重名。子表可逐级嵌套。

### 填报表-扩展模型
- 维度格表达式为**维序列**时按成员纵/横扩展。
- 扩展方向判定：左（上）有扩展格则沿用；否则右（下）有数值格就纵（横）扩；右、下均有或均无则**纵向**扩展。多层扩展直到遇到数值格为止。

### 填报表-采集模型（回填/写库，工程细节最密）
- 维度格 + 数值格 = 字段值，普通格 = 字段名。
- **分片 → 序表**：连续一片数值格（连同对应维度格）为一片；连续两行以上只有数值格、无维度格和非空普通格、左右无合并格 → 行式分片（可自由增删行）；其余单值拼进一行记录的序表。
- 字段名认定（未显式设置时）：取**上（左）同宽（高）的普通格**内容。
- 同序表认定：数值格对应的维度格字段名相同即视为同一对象；单行数据的序表里的维字段为**公共维**，自动拼入其他多行序表。
- 取数表达式：`变量名.字段名`，变量由取数 spl 返回（可多个序表/维序列/下拉序表）。
- 回填：`score` 为填写后数据集，要拿填写前需在数据处理里另存旧值（如 `score_old`）。
- 行式填报表**只支持纵向分片**。

## 三、三份扫描件的核心结论

### 1. 报表引擎设计（周金根，广联达 2010）→ `digest-报表引擎设计.md`
模型源自润乾，但补上了润乾资料里通常缺失的**实现层细节**。最具移植价值的 5 点：
- **主格认定五步递归算法**：显式设置 → 相邻可扩展格且范围覆盖 → 跟随相邻格 → 递归 → 兜底落 00 格。纵向向左查、横向向上查，完全对称。这是"零配置也能跑对"的来源。
- **`ExtCells` 双层结构**：单元格持有 源格/左主格/上主格/附属格集合/`ExtCells`；求值 = 求两格共同主格 → 沿主格链递归下降 → 在 `ExtCells` 中按另一主格过滤。
- **扩展与分页引擎分离 → 表达式两阶段求值**，明令"页统计表达式不能影响扩展过程"。
- **"拉大 vs 复制"由主格链是否同级判定**（非几何位置）：同级 → 拉大成合并格；附属格才复制。配合"缺省聚集 = 所属主格区域"，小计格零配置正确。
- **9 类打印带区 × {是否参与扩展、是否每页重复} 矩阵** + 分页参数表，可直接抄进配置 schema。
- 立场：填报表 = 普通报表 + 5 个属性增量，**不建第二套模型**；明确**否定用子报表组合复杂报表**（子表与主表非整体、不能互相运算），改走统一报表内多源分片。
- 文档自身留了坑：层次坐标 `Lk` 次序自相矛盾（p19 说由近到远、p43 说由远到近）、`{}`/`[]` 分工、宏 `${}` 与参数 `%year` 两套记法。缺性能方案与缓存设计。

### 2. 报表引擎系统研究开发（裴晓华，西安理工 2009）→ `digest-报表引擎系统研究开发.md`
**注意：这不是非线性单元格扩展模型**，而是以 Eclipse BIRT 为蓝本的剖析与复现（ROM 元素、`rptdesign`/`rptdocument`、`ReportItemExecutor`）。作者自承对中国式交叉表"捉襟见肘"。价值在于给出了一条工业级管线骨架：
- **迭代器式执行器树**：`execute()/hasNextChild()/getNextChild()/close()` + `isCanceled()` + `parent` 回连 + 随递归下传的 emitter，同时解决流式生成 / 可取消 / 父子上下文继承。可直接映射为 Rust trait + 工厂表。
- **双阶段 + 二进制中间文档**：`rptdesign → 执行器树 → rptdocument（含分页/TOC）→ 分页索引 → 多格式发射`。生成与展现解耦，一次生成多次展现——建议作为 `print-server` 的序列化边界。
- **表达式下推改写**：`prepareTotalExpression()`（`row`→`dataSetRow`）与 `ITotalExprBindings`（`Total`→`row` + 列绑定集合），把聚合下推到数据集/DB 层而非展现层二次遍历。
- **三级分页索引** `UnresolvedRowHint`/`PageHint`/`InstanceIndex`：把"跨页未决行"建模为一等公民。
- 滑动窗口（选择重传）做模块间调度，实测 1000kb −23%、3000kb −22%；但样本仅 8 点无方差，只能定性参考。
- 不建议照搬：javascript 表达式引擎、XSL-FO + FOP 出 PDF。

### 3. 基于单元格扩展的报表引擎的研究与实现（硕士论文）→ `digest-基于单元格扩展的报表引擎.md`
与我们的技术路线**最贴近**的一份。核心是"单元格继承关系驱动扩展"骨架：算继承关系 → 初始化表格矩阵 → 填充 → 扩展子格/更新继承 → 算最终结果矩阵 → 分页输出。
- **BEB→BE→BoxCE 三层模型**值得直接搬：BE 只能持一个行/列父格引用，父格扩出 N 个值时子格要 N 份且分别指向不同父格值，所以必须上层加 BEB 用 `beList` 管理。映射到 Rust 时父子引用建议用 `Weak` 防环。
- **`ExtendedArea` 决定扩展步长**（`to−from+1` 而非固定 +1）：处理"父格 1×1、子格跨 1×2/1×3"时一次扩展要多列/多行——自研时最容易漏的正确性细节。
- **默认父格自动上溯**（行父格必须是纵向扩展格、列父格必须是横向扩展格）+ 双映射序列三段式增量扩容（`info[startPos,from,to,copyLength]`），避免重建大矩阵且直接服务分页。
- **直接/间接更新二分**（`Direct/IndirectExtendedBEContainer`：父有实值则 1:N 拷贝，父子同被拷贝则 1:1），逻辑清晰可单测；三处死循环防护（genealogy HashSet / isCalculatingSet / 分页零进展）建议全量引入。
- **明显缺陷**：表达式引擎近乎空白（只有 SUM/AVERAGE 举例，无解析器、无层次坐标，循环引用只靠抛异常）；交叉格取值算法未给出；多源仅靠 `deptId = deptId` 声明式等值、易 N+1；**全文零性能数据**；schema 与第 6 章条件属性不一致。

## 四、与现有文档的关系

- `非线性报表设计详解.md`（32k 字）已经把蒋步星《非线性报表模型原理》吃透：数学基础、表格形式化、扩展变换算法 3.1/3.2/3.3、层次坐标、计算算法、填写回写。**本次未重复处理**。
- `非线性报表计算引擎架构设计.md` 是我们自己的架构 v2。本次新增资料主要补充两个此前偏薄的方向：
  1. **填报/回写的工程细节**（润乾采集模型的分片→序表→回填链路）——对应架构文档第十二章可以补实。
  2. **工业级管线与执行器树**（BIRT 那套双阶段 + 中间文档）——对应架构文档的序列化边界与分页子系统。

## 五、落到自研引擎的待办清单

1. 表达式层补 `$` 运算符语义（当前格主格 vs 目标格主格），否则同比/环比的条件表达式写不出来。
2. 主格认定实现五步递归算法（周金根版），替代临时/猜测式判定。
3. 扩展步长用 `ExtendedArea`（`to−from+1`），不要固定 +1。
4. 分页与扩展严格分离，页统计表达式禁止反噬扩展结果。
5. 多源坚持"不做整合运算、直接在报表运算中关联"（润乾明确论证过），这是架构红线。
6. 中间结果序列化成分页/TOC 就绪的二进制文档，生成与展现解耦。
7. 三处死循环防护全量引入。
8. 表达式引擎是论文与两份设计共同的短板，需要我们自己补强（解析器 + 层次坐标 + 循环引用检测）。
