# 对《架构评审》的逐条核验

> 对象：外部评审（针对《报表引擎详解-功能与算法.md》与 `print-server` 源码）提出的 7 条架构问题、
> 目标架构与实施顺序。
>
> 方法：**逐条回到源码核对，能复现的就复现**。评审自己声明「本次没有修改代码，
> 也没有重跑测试集」——所以我把它的每一条**都跑了一遍**。
>
> 结论先行：**7 条我全部确认成立，其中 5 条已经用可复现的测试钉住。**
> 评审是准的。但它在**两处建议上我不同意**，另外它**低估了一件事、也漏了一件事**。
> 本文也**更正了我自己的报告**（初稿在两处太客气）。

---

## 0. 一句话结论

评审的方向判断正确：**这套内核已经有非线性能力，但「展开 / 计算 / 布局 / 分页」之间缺少
可验证的契约**。我额外确认的是：**这 5 个缺陷全部落在现有 512 条测试的覆盖范围之外**——
这才是它们能长期存活的原因。所以修复的第一步不是改代码，是**先把它们变成会红的测试**。

> **进展（2026-09-26）**：用户批准后按 `② → ① → 结构化错误 → ③④` 动手。
> **② 与 ① 已修**（探针 → 常驻回归测试 + 故障注入），全套件
> `514 passed / 0 failed / 16 ignored`。③④ 与「结构化错误」待做。
> 下面 §3 里 ①② 两节已就地标注修法与验收方式。

---

## 1. 版本核对（评审这一点说对了）

| 项 | 值 |
| --- | --- |
| 评审读的文档版本 | `3759813` |
| 核验时仓库 HEAD | `81c2cc4` |
| 两者差异 | **4 个文件，全部 `.md`**（1 份新文档 + 3 份记忆） |

```
$ git diff --name-only 3759813..81c2cc4 | grep -vE '\.md$'
（无输出）
```

**所以评审引用的行号全部仍然有效**，不存在代码漂移导致结论失效的问题。
这一点它自己主动声明了，是负责任的做法。

---

## 2. 逐条核验

| # | 评审的论断 | 判定 | 依据 |
| --- | --- | --- | --- |
| 1 | 模板视觉顺序被当成结构依赖顺序 | ✅ 成立 | `engine.rs:1030-1061` 只认「父格下标更小」；显式前向 `row_parent` → 挂根 → **取全量而非本组** |
| 2 | 「最多四轮」不保证数值与过滤一致 | ✅ **成立且已复现** | `engine.rs:1740-1781`；见 §3.② |
| 3 | 行列分开布局，缺二维占用校验 | ✅ **成立且已复现** | `engine.rs:1269` 无占用检查；见 §3.① |
| 4 | 多数据集关联只处理了部分路径 | ✅ **成立且已复现（两处）** | 列主格按裸行号 `engine.rs:2560-2579`；`join_view` 首行假设 `engine.rs:931-937`；见 §3.③④ |
| 5 | 惰性求值与可变变量混在一起 | ✅ 成立 | `engine.rs:2230` 明写「变量不构成格依赖」→ 读到哪个值取决于求值顺序 |
| 6 | 输出丢掉实例上下文 | ✅ 成立 | `GridCell`（`model.rs:1249`）只有模板 `pos`；**且图表按 `pos` 去重使「每组一张图」不成立**（见 §3.⑤） |
| 7 | 分页太晚，拿到的只是拍平的表 | ✅ 成立 | `paginate`（`mod.rs:412`）只接收 `&[Vec<GridCell>]`，结构上不可能知道分组 |

**行号抽查**：评审的引用基本准确。少数几处指向**文档注释**而非代码本体
（`engine.rs:1020` 实际逻辑在 1030-1061；`engine.rs:1062` 实际在 1088-1116；
`mod.rs:169` 实际在 172-181；`engine.rs:1319` 的去重在 1356-1357），
但都在同一段落内，**不构成误读**。

---

## 3. 已复现的缺陷（本文新增的部分）

评审对第 3 条自述「属于静态推导的风险，**尚未构造样例复现**」。
我把它和另外四条一起做成了可复现的测试，放在 `engine.rs` 末尾，
以 `#[ignore]` 标记（默认不跑，所以闸保持绿；`cargo test -- --ignored` 可复现）。

> 为什么用 `#[ignore]` 而不是直接修：这些缺陷的修复涉及语义决策（见 §5），
> 不该由我单方面定。但**复现是昂贵的部分**，先钉住。

> **修复进度（2026-09-26，用户批准后按「② → ① → 结构化错误 → ③④」动手）**：
> ② 与 ① **已修**，两个 `#[ignore]` 探针已转成**常驻回归测试**并做过故障注入。
> ③④ 仍未修（`#[ignore]` 保留）。

### ① 二维落位冲突：后写静默覆盖前写 —— ✅ 已修

`grid[inst.row_start][inst.col_start] = Some(..)`（原 `engine.rs:1269`）**没有任何占用检查**。

复现（原 `layout_collision_probe`，同一模板行放两个列展开格，模板列 0 与 1）：

```
实例数=6 落位数=4 冲突数=2
  冲突 (0, 1) ← ["A1", "B1"]
  冲突 (0, 2) ← ["A1", "B1"]
网格 1x4
  行0: "1月"@A1 | "Q1"@B1 | "Q2"@B1 | "Q3"@B1
```

3 个月 × 3 个季度的 6 个数据格，渲染成 `1月 Q1 Q2 Q3`：
**2月 / 3月 连同它们的值一起消失，零告警。**

根因在 `layout_columns`（`engine.rs:1945-1960`）：每个列展开组从**自己的 `tpl_col`**
起占列（`let mut cursor = self.insts[group[0]].tpl_col;`），两组区间重叠时**不检测**。

**修法**：写入点加占用检查 —— 冲突时保留**先创建**的实例，后到者标 `dropped`，
冲突逐条告警（落位坐标 + 两个 `pos` + 可执行建议），上限 20 条。
**刻意不「修好布局」**：两个独立的列展开没有合法的二维布局，硬排出来的
`1月 Q1 Q2 Q3` 反而**像一张正常交叉表**，比报错更危险。现在的输出
`1月 2月 3月 Q3` 一眼能看出是坏的。

**故障注入**：把 `if let Some(prev) = grid[..].as_ref()` 改成 `None::<&GridCell>` →
新测试立刻红，报 `落位冲突未被拦下：2 个落位被多个实例占用`；还原后绿。

**顺带得到的事实**：用临时 `panic!` 在冲突点拦截、跑完整套件 ——
**513 条既有测试里一条都不触发冲突**。所以这次检测是**纯增量**，
没有改变任何既有模板的落位（这也回答了下文 §8 里「修复的回归风险」那条担忧）。

### ② 强制退出时，数值口径落后布局一轮 —— ✅ 已修

`evaluate_to_fixpoint`（原 `engine.rs:1740-1781`）在 `round + 1 == MAX_ROUNDS` 时 `break`，
**不重置 `evaluated`**。于是最后一轮 `changed` 为真时：

| 项 | 用的是哪一轮的 `hidden` |
| --- | --- |
| `insts[..].value` | 第 3 轮（**上一轮**） |
| `insts[..].hidden`（布局按它过滤） | 第 4 轮（最新） |

复现（原 `nonconvergence_probe`，行测试 `COUNTA(B1) <= 1` 造成震荡）：

```
未丢弃 A1 实例数 = 3
C1 的值 = ["0.0", "0.0", "0.0"]     ← 按「全被删」那轮算的
网格 3 行                            ← 但最终 3 行都可见
```

**汇总显示 0，而明细明明有 3 行。**

**附带缺陷（评审没提）**：那句告警「已按最后一轮结果出表」（`engine.rs:1769`）
**与事实相反**——数值恰恰不是最后一轮的。一条**说反了的告警**比没有告警更坏：
它会让作者相信结果是对的。

**修法**：未收敛时的收尾改成「清轮缓存 → 重置 `evaluated` → 按**最终**（已冻结的）
`hidden` 重算全部值」；抽出 `clear_round_caches()` / `reset_evaluated()` 两个 helper
（`cycle_warned` 刻意不重置 —— 它是「同一个环只报一次」的闸）；告警文案换成
「…该条件在最终数据上仍不成立 —— 这张表的结果**不稳定**，不要当作可发布的结果」。

**故障注入**：把 `if !converged` 改成 `if false` → 新测试红，报
`C1=[0.0, 0.0, 0.0]，但最终可见 B1 有 3 个`；还原后绿。

### ③ 跨数据集列主格按**裸行号**关联

行父格跨数据集时会强制要求 `join_on`（`join_view` `engine.rs:921-929`），
**列父格这条路没有同等检查**。`col_parent_index`（`engine.rs:2560-2579`）把列主格的
`rows` 当**裸行号**索引，与当前格的 `base` 直接求交。

复现 `cross_ds_col_parent_probe`（ds1 两行 / ds2 三行）：

```
网格 1 行 x 2 列
  行0: "111" | "222"
告警数 = 0
```

ds2 的第三行（333）**静默消失，零告警**。

**补充**：`debug_assert!`（`engine.rs:1108-1111`）只守**非分桶**那条路，
**分桶路径没有保护**——而分桶恰恰是主路径。

### ④ `join_view` 用**父实例首行**的关联键代表整组

`engine.rs:931-937` 的注释写：

> 父行当前的键值：父实例可能覆盖多行，取第一行（**分组格下它们同键**）

**括号里那个前提没有任何校验。** 只要**分组字段 ≠ 关联字段**，组内各行的关联键就不同，
「首行代表整组」立刻出错。

复现 `join_view_first_row_probe`（父按 `region` 分组，`join_on: "cust"`）：

```
网格 1 行
  行0: "华东" | "100"
告警数 = 0
```

华东组内有 `cust=1` / `cust=2` 两条子表行（100 / 200），**正确合计 300，实际 100**，
200 **静默丢失，零告警**。

评审说这一点「也需要验证，不能默认成立」——**验证结果是：它不成立。**

### ⑤ 图表「每组一张图」不成立

`resolve_charts`（`engine.rs:1356-1357`）按 `pos` 去重：

```rust
let mut seen: BTreeSet<String> = BTreeSet::new();
decls.retain(|(_, _, pos, _, _)| seen.insert(pos.clone()));
```

所有分组实例共享同一个 `pos` → 只剩第一份；而 `ChartIndex` 也按 `pos` 收集
（`engine.rs:1365-1377`）→ 那一张图会把**所有组**的类目都收进去。

**结论**：`make_insts` 里那句注释「挂在分组格上的『每组一张图』走的就是这条展开分支」
（`engine.rs:1517-1518`）**是误导性的**——声明确实被拷到了每个实例，但随后全被丢掉。
**「每组一张、数据各不相同的图」当前做不到。**

> ⚠️ **这条更正了我自己的报告**：初稿 §8.2 沿用了那句注释的说法。
> 已在报告 §8.2 与 §14.6 更正。

---

## 4. 评审里我**不同意**的两处

### 4.1 「错误父格不应挂根，应返回明确的模板错误」——**我不同意这个改法**

评审原文：

> 错误父格也不应"挂根保数据"，而应返回明确的模板错误，避免改变业务口径。

**它诊断对了，处方开错了。** 理由：

1. **本项目的失败粒度是「一格」不是「整表」**，这是有明文的设计约定
   （`compile_conditionals` 的注释 `engine.rs:626-628`：坏规则不该让整张表出不来）。
   把无效父格升级成模板错误，等于让**一个笔误**废掉整张报表。
2. **评审自己后面的建议才是对的**：「形成明确的错误类型，而不只是字符串告警」。
   正确的做法是**让调用方决定**：
   - 挂根照旧（保住数据可见性）；
   - 但把「挂根」这件事写进**结构化的 `errors`**，而不是混在 `warnings: Vec<String>` 里；
   - 设计器可以据此**红字拦住导出**，CI / API 可以据此拒绝发布。
3. 挂根之所以危险，**不是因为它保数据，而是因为告警和「表达式解析失败」这种
   小毛病混在同一个 `Vec<String>` 里**，没法分级、没法拦。

所以我的建议是**同意「需要明确错误类型」，不同意「直接返回模板错误」**。

### 4.2 「零依赖不应成为目标本身……不应优先于核心报表正确性」——**它在反对一件代码已经决定不做的事**

**优先级**那半句我同意。但这段的隐含前提（自研编码器是持续投入、挤占了核心工作）
**没有证据**，而且**代码自己早就划了线**：

| 自研件 | 代码里的收敛声明 | 测试数 |
| --- | --- | --- |
| 二维码 | 版本 11+ **明确不做**（`barcode.rs:30`：213 字节已远超单据载荷） | 44 |
| Code128 | **不自动切码集**（切换是启发式，猜错＝扫不出来） | ↑ |
| PNG | 用**非压缩** stored 块，**不写 Huffman 编码器**（`png.rs`） | 10 |
| ZIP | **只写 method 0**，不写压缩（`zip.rs:7`） | 6 |
| OOXML | 只写 4~5 个 part | 15 |

也就是说：**这几个编码器都已经做完并测过了，是沉没成本而不是在途投入**，
而且每一个都主动放弃了「做得更全」。它们买到的性质是真的（无原生依赖 → 可移植）。

**我的判断**：这条对「未来别再往里投」是对的，对「过去投错了」不成立。
把它当成**前瞻约束**接受，不要当成**追责**。

---

## 5. 评审低估的一件事

**它没注意到：这 5 个缺陷全部落在现有 512 条测试的覆盖范围之外。**

这不是巧合——`engine.rs` 31 条测试 + `mod.rs` 172 条，覆盖的是
「单数据集 + 纵向展开 + 规整交叉表」这一族形状。而这 5 个缺陷全都需要：

- 同一行里**两个**列展开格（①）
- **震荡**的测试表达式（②）
- 列父格与当前格**不同数据集**（③）
- 分组字段 ≠ 关联字段（④）
- 图表挂在**会展开**的格子上（⑤）

**没有一条是「函数算错了」，全都是「组合起来才错」。** 这正是评审说的
「复杂模板越多，就越容易出现『表能生成，但数据或位置不对』」的机制解释。

**所以修复的第一步应该是补测试形状，而不是改代码** —— 否则改完了也无法证明改对了。

---

## 6. 评审漏掉的一件事（让修复更便宜）

评审第 6 条建议在输出里保留「模板格 ID / 实例 ID / 行列分组上下文」。
它不知道的是：**这套身份信息已经算出来了，只是没往外送。**

`layer_coordinate`（`engine.rs:2246-2258`）已经在生成形如 `"A2:0,B3:2"` 的
**层次坐标链**（模板 pos + 每层实例序号），`dump_text` 在用。

所以「实例身份」这件事的成本比评审估计的低得多：

```
现在：  GridCell { pos: "A3", ... }                     ← 三个 A3 长得一模一样
已有：  layer_coordinate(i) = "A2:0,B3:2"               ← 内部已能区分
改法：  GridCell 加一个 Option<String> 字段带上它         ← 不是重写，是**透出**
```

同理，评审建议的「LogicalSheet / PageLayout 分离」**不是新范式**：
项目里**已经有**「声明 vs 结果」的成对类型（`CellChart`/`ResolvedChart`、
`PageConfig`/`ResolvedPageSetup`、`CellConditional`/`CondRule`）。
评审的目标架构是**这套既有约定的自然延伸**，不是引入新东西——
这会显著降低它的实施成本估计。

---

## 7. 对实施顺序的意见

评审的「第一步：先让错误结果不能被当成成功结果」**我完全同意**，这是正确的排序。
但我要调整它的**内容顺序**和**验收方式**：

| 评审的顺序 | 我的调整 | 理由 |
| --- | --- | --- |
| ① 非收敛过滤 | **① 先补 5 个复现测试**（已完成，`#[ignore]`） | 没有红测试就没有「修好了」的判据 |
| ② 跨数据集行号求交 | ② 非收敛过滤（含**改掉那句说反的告警**）—— ✅ 已修 | 它是唯一会**主动误导**人的缺陷 |
| ③ 无效父格自动挂根 | ③ 结构化错误类型（`warnings` → `issues` 分级） | 挂根要能被拦住，得先有通道 |
| ④ 布局冲突覆盖 | ④ 布局占用校验（检测 + 上报 + 先创建者胜）—— ✅ 已修 | ①③ 修完才有意义 |
| ⑤ 循环公式与空值混淆 | ⑤ 跨数据集列主格 + `join_view` 首行 | 两条都是「静默错数」，同一族 |

**用户最终采纳的顺序**：`② → ① → 结构化错误 → ③④`。②① 已落地；
把「结构化错误」排在 ③④ **之前**是对的 —— ③④ 的正确修法是「拒绝 + 报错」，
而报错需要一个比 `Vec<String>` 更强的通道（能区分「提示」与「结果不可信」）。

**一条补充建议（评审没提）**：`expr::parse` 在 `ensure_value` 里**每个实例、每轮都重解析**
（`engine.rs:2059`）。10000 行 × 4 轮 = 同一表达式解析 4 万次。
加一张 `HashMap<String, Expr>` 的 AST 缓存是**几行代码**的事，
和评审第 2 步的「模板表达式编译一次」完全一致，但**不必等模板编译层做完**。

---

## 8. 未核验 / 无法核验

- **缺陷 ① 的真实触发频率**：我构造的是一个刻意的最小样例。
  「两个列展开格同一行」在真实模板里有多常见，我没有数据。
  （不过 `col_after` 的第二遍也可能撞列，那条路径我没单独构造。）
  → **部分回答（2026-09-26）**：用临时 `panic!` 在冲突点拦截、跑完整套件，
  **513 条既有测试一条都不触发**。这只说明「测试集里没有」，不等于「真实模板里没有」，
  但它至少证明：**修这个不会改变任何既有模板的落位**。
- **评审对 NopReport 官方行为的描述**（「双向交叉、多源、档案式、兄弟节点并行展开」）：
  我**没有去核对上游**，本文只谈本项目源码。
- **`join_view` 首行假设在「分组字段 == 关联字段」时是否正确**：
  那种情况下首行确实代表整组，我没构造反例，倾向于认为它成立。
- **修复这些缺陷的回归风险**：测试是「绿」的，但它们没覆盖这些形状，
  所以**不能保证改动不会碰坏别的东西**。这也是我坚持先补测试的原因。
  → **对 ①② 已解除**：修完后全套件 `514 passed / 0 failed / 16 ignored`，
  两条新测试都做过故障注入（注入 → 红且报出预期文案，还原 → 绿）。
  （③④ 修完后需要重新做一次同样的确认。）

---

## 附：复现方法

```bash
# 默认全绿（③④ 被 #[ignore] 挡住，闸不受影响）
cargo test --manifest-path print-server/Cargo.toml --bin print-server
#   → 修 ①② 之前：512 passed; 0 failed; 18 ignored
#   → 修 ①② 之后：514 passed; 0 failed; 16 ignored

# 显式跑剩下的复现探针（应看到 ③ 红、④ 特征化通过）
cargo test --manifest-path print-server/Cargo.toml --bin print-server -- --ignored --nocapture
```

五个探针模块（都在 `engine.rs` 末尾）：

| 模块 | 缺陷 | 现状 |
| --- | --- | --- |
| `layout_collision_probe` | ① 落位冲突静默覆盖 | ✅ 已修，探针已转为常驻测试 |
| `nonconvergence_tests` | ② 强制退出数值落后一轮 | ✅ 已修，探针已转为常驻测试 |
| `cross_ds_col_parent_probe` | ③ 跨数据集列主格裸行号 | 仍 `#[ignore]`，待修 |
| `join_view_first_row_probe` | ④ 首行代表整组 | 仍 `#[ignore]`，待修 |
| （⑤ 图表去重无需探针：`engine.rs:1356-1357` 的 `retain` 一眼可见） | | |
