# 非线性报表模型与设计详解

> 本文基于《非线性报表模型原理》（蒋步星 著，2007，润乾报表理论基础）原文逐章梳理，对其中的**数学定义、核心算法、变换性质与计算/填写方法**进行系统化重构与详尽说明，目标是"把非线性报表的设计讲清楚"。
>
> 文中凡涉及"算法 N.M""定理 N.M"均对应原书编号，便于回查。

---

## 一、为什么需要非线性报表模型（背景与设计动机）

传统报表工具采用的数学模型被称为**线性报表模型**。其源自数据库理论：先对数据集做分组/聚合运算，再把结果"填入"一个布局简单的表格。它的核心在"数据集运算"，而"单元格/表格布局"被严重弱化。这导致：

- 表格的变换和计算被迫高度规律化，**难以描述复杂多样的报表样式**（合并单元格、不规则分片、交叉表、套表等）；
- 单元格之间的计算/引用关系被限制得很死，**无法灵活表达"相对引用""层次引用"**；
- 在填写（回写数据库）方面几乎是空白。

《非线性报表模型原理》的核心思想是**把"单元格"提升为整个理论体系的第一公民**：数据集退化为"提供数据来源的从属属性"，几乎所有变换与计算都以**表格（单元格网格）为主体**进行。由此带来的能力：

- 用一套统一的"扩展 + 引用"机制描述绝大多数复杂报表；
- 实现真正意义的"零编码"设计复杂报表；
- 理论上可证明停机性、封闭性、可计算性，并给出复杂度界。

> 严格地说，"线性报表"与"非线性报表"不是某类报表，而是两套**设计报表的理论体系**。所有线性报表都能用非线性模型设计（故也是非线性报表），反之不成立。原书把"线性报表"通俗地刻画为以下交集的真子集：① 整报表 ② 单片报表 ③ 单源报表 ④ 所有表达式都是线性计算串 ⑤ 无符号循环引用 ⑥ 单向报表。

---

## 二、数学基础（原书第 1 章）

非线性报表模型建立在严格的集合论与函数论之上。理解下列基本概念是后续一切算法的前提。

### 2.1 集合、数组、矩阵

- **集合**：用 `{…}` 表示；`|X|` 表示元素个数；`A⊆B`、`A∪B`、`A∩B`、`A\B` 为并/交/差。
- **相交 / 相含 / 交叉**：`A∩B≠∅` 相交；`A⊆B` 或 `B⊆A` 相含；相交但不相含为交叉。
- **数组**：有限集按规则排序得到的序列，用 `(a₁,a₂,…,aₙ)` 表示，第 `i` 个成员序号为 `i`。**相等**要求长度与成员次序完全相同。
- **n 元组 / 序偶**：长度 `n` 的数组；2 元组 `(左元, 右元)`。
- **叉积（笛卡儿积）**：`X₁×X₂×…×Xₙ`。
- **矩阵**：由多个 n 元组构成的数组；行数=成员数，列数=成员长度。

### 2.2 四种基本变换符号（算法的基本动作）

| 符号 | 含义 |
|---|---|
| `S += a` | 把 `a` 加入集合/数组 `S`（数组缺省加到末尾） |
| `S -= a` | 从 `S` 中删除 `a` |
| `a → b` | 上下文中所有含 `a` 的集合把 `a` **替换**为 `b`（保持数组次序） |
| `a → x` | 上下文所有含 `a` 的集合先 `S -= a`，若 `S` 是 n 元组则递归 `S → x` |

> 注意：`a → x` 是"删掉以 `a` 为元素的集合"，不是替换 `a` 本身；有限上下文必在有限步停止。

### 2.3 函数、聚合函数、集合函数

- **聚合函数**：参数本身是集合（或数组）的函数，如 `sum(X)`、`min`、`max`、`avg`。
- **集合函数**：返回值都是数组的函数，如 `list(v₁,…,vₙ)`、`to[q](n,m)`（返回数组 `q` 中 `n` 到 `m` 之间的成员）。
- **关键区分**：聚合函数≠集合函数。集合函数是"报表自动化"的基础（一个单元格可展开成一组），聚合函数用于处理"统计运算"——二者对建模都至关重要。

### 2.4 表达式与计算串

- **表达式**：由数据、函数经运算符连接而成，可计算出一个"数据"（数、字符串、数组、甚至图片）。
- **计算串**：内容为一个表达式的**字符串**。`eval(x)`：把计算串 `x` 当作表达式计算，返回结果（结果仍可能是计算串，可再 `eval`）。

### 2.5 属性（属性的本质是函数）

- 属性 `p` 定义为两集合 `X×Y` 的子集，且无两个元素同左元。
- `x.p`（或 `p(x)`）为 `x` 的 `p` 属性值；`x.p = v` 表示设置属性值。
- **属性表达式**：属性值写成表达式形式，其计算结果才是属性值。

---

## 三、表格与报表的形式化定义（原书第 2 章）

### 3.1 网格、表格、单元格

**网格 T**：以 `N×N` 为定义域的可逆函数 `e`（格映射），`T = { e(r,c) | 1≤r≤n, 1≤c≤m }` 即 n 行 m 列网格，元素称为**元素格**，`|T| = n·m`。

**表格**：为网格 `T` 设置一个由 `T` 上**矩形**（矩形 = 任意两格之间的元素格都包含在内）构成的有限集 `UG[T]`，且 `UG` 中单元格两两相离。此时 `UG` 的元素就是**单元格**。

- **合并格**：`|U| > 1`；行数 `rows(U)`、列数 `cols(U)`、行号 `row(U)`、列号 `col(U)` 由矩形边界确定。
- **满表格**：每个元素格都落在某个单元格内（现实报表几乎都满，但不强制假定）。
- **矩形引理**：单元格 `U` 必可写成 `M(a,b,c,d) = {E(x,y) | a≤x≤c, b≤y≤d}`，其相交行/列集是连续行/列集。
- **平面坐标**：用列字母+行数字命名单元格，如 `A3`、`B4`。

### 3.2 原序、最前格、跟随格、单元格数组

对单元格 `A,B`：若 `row(A)<row(B)` 或（`行号相同 且 列号更小`），记 `A<B`，称为**原序**。

- **定理 2.2**：原序具有**可序性**（任意两格必有 `A<B` 或 `B<A`）与**传递性** → 所有单元格可排成一个数组，即**单元格数组 `UG[T]`**。
- **最前格**：比所有其他单元格都靠前的格（通常是 `U(1,1)`）；**跟随格**：紧邻其后的格。
- "按原序遍历每个单元格 U 执行 P"：从最前格开始，沿跟随格递推直到无跟随格。

### 3.3 基本变换算法

#### 算法 2.1 / 2.2　表格行列增删
- **增加行**（第 k 行后加 1 行）：`E(i,j) → E(i+1,j)`（k<i≤n）平移下方元素格；补入新行元素格；若某单元格同时跨 `R(k)` 与 `R(k+2)`，则把它向新行"撑大"。
- **删除行**（删第 k 行）：先删仅在该行的单元格；`E(k,j) → x`；`E(i,j) → E(i-1,j)`（k<i≤n）整体上移。
- **定理 2.3**：对任一表格执行增/删行列后**仍是表格**（UG 仍由两两相离的矩形构成）。

> 由"导出表/原表""继承格/原格"概念统一描述变换：变换可视为先复制原表 `T`，再在其上执行算法得到导出表 `T'`，原格 `U` 在 `T'` 中的对应格 `U'` 称**继承格**。

#### 算法 2.3　子表引入
允许单元格 `X` 的格值 `X.val` 是另一张表格 `S`（子表）。引入过程：
1. **撑大**：若子表行/列数大于子表格，先在宿主表格对应位置加行/列，把跨越的子表格"撑"到与子表同尺寸；
2. **填入**：把子表单元格按偏移映射 `E[T](a+i-1,b+j-1)` 逐个加入宿主 `UG`，并复制 `val`。

**定理 2.4**：子表引入后仍是表格。
**关键约束**：必须**先把所有子表格都撑大，再统一填入**；若撑一个填一个，会把已填入的子表单元格再次撑大，导致尺寸错乱。该算法用于处理"有限扩展""交替扩展"等纯靠扩展算法搞不定的复杂表。

### 3.4 主格与扩展属性（非线性报表的灵魂）

为表格 `T` 引入抽象**根格 `@`**，并定义如下**扩展属性**：

| 属性 | 取值集 | 含义 |
|---|---|---|
| 扩展方向 | `{纵, 横}` | 有定义者称**扩展格**（纵主格 `USₓ` / 横主格 `USᵧ`），否则**非主格** |
| 左主格 `xp` | `{@}∪USₓ` | 定义于 `UG\USᵧ`；`@` 与扩展格统称**主格** |
| 上主格 `yp` | `{@}∪USᵧ` | 定义于 `UG\USₓ` |
| 主格 `pu` | — | 扩展格 `U` 的 `pu = U.xp`（或 `U.yp`） |

- 扩展格 `U` 的**主格线**：满足 `P₀=U, Pᵢ₋₁=Pᵢ.pu, Pₙ 无主格` 的主格数组。
- **定理 2.5**：主格线唯一；不含重复成员；首成员必为 `@`；同一主格线内扩展方向相同。
- 用图论看：以主格为节点、`(主格, 子格)` 为边，构成以 `@` 为根的**主格树**；主格线即树上从根到某节点的路径。
- **深度**：扩展格主格线长度−1；`@` 深度 0；报表深度 = 最大深度。

**辖区（核心引用范围）**：
- `DOM[P]`：以扩展格 `P` 为**导格**的单元格集合（不含子格）；
- `DOMA[P]`：P 及其**先主格**为导格的单元格（先辖区）；
- `DOMB[P]`：P 及其**后主格**为导格的单元格（后辖区）；
- `DOMC[P] = DOMA[P] ∪ DOMB[P]`（全辖区）；
- 纵主格 `P` 与横主格 `Q` 的**交辖区** `DOM[P,Q] = DOM[P] ∩ DOM[Q]`。

### 3.5 扩展带（扩展发生的物理范围）

- **包含带**：扩展格 `P` 的连续行(列)集 `B`，使 `DOMB[P]` 中所有单元格都落在 `B` 内。
- **扩展带 `B[P]`**：`P` 的所有包含带的**交集**。纵主格的扩展带是连续行集（纵扩展带），横主格是连续列集。
- **定理 2.6**：扩展带是非空连续行/列集；子格的扩展带是 `P` 扩展带的子集；不在任何扩展带内的单元格，其左/上主格为 `@`。
- **空心带**：扩展带 `B` 内存在某行 `L` 与 `DOMB[P]` 无任何单元格相交 → `L` 是空心行。`B\L` 必是连续集，故 `L` 一定在 `B` 中部。

### 3.6 报表的分类（设计时的形式化判据）

**按主格形态**：
- 无扩展格 → **静态报表**（深度 0）
- 每主格子格≤1 → **单片报表**；存在某主格≥2 子格 → **分片报表**；有扩展格≥2 子格 → **真分片报表**
- 无横主格 → **单向报表**；有纵有横 → **交叉报表**

**按与扩展带的关系**（最重要的一类判据）：
- 与 `B[P]` 相交的单元格 `U` 满足 `U∈DOMB[P]` 或 `R[U]⊆B[P]` → **正报表**
- `U∈DOMC[P]` → **实报表**
- `U∈DOMB[P]` → **整报表**
- 非实报表称**非实报表**；既是实又是正 → **正实报表**（整报表必是正实报表）

> 现实中"有业务含义的报表几乎全是实报表，且绝大多数是正实报表"，这是算法设计与优化的重点区域。

**判定实报表的充要条件（定理 2.8）**：
1. 若扩展格在另一扩展格扩展带之内 → 两者必在同一主格线内；
2. 任意两个扩展带不交叉。

### 3.7 临时报表与自动主格设置（算法 2.4）

实际设计报表时，逐格设置主格很繁琐。书中给出**自动主格设置算法**：

> 通俗理解：对每个未设主格的单元格，在它**左（上）边、同行（列）**里找第一个"更大（不小于）"的单元格设为它的左（上）主格。

- **算法过程**：先按原序处理所有纵主格（设左主格），再按原序处理所有非主格；每个单元格在左侧交行集中、在"扩展格集合 X 或非主格已设主格集合 Y"中，取列号最大者 `W`：若 `W∈X` 则 `U.xp=W`；否则 `U.xp=W.xp`；空集则 `U.xp=@`。上主格对称。
- **定理 2.10**：对临时报表执行后必成**报表**。
- **定理 2.11 / 2.12**：给出保证结果是实（整）报表的判定条件（如临时实报表经设置后仍临时实 → 导出实报表；无"异向相交"且同向相交者左（上）者行（列）集相等 → 得实报表）。现实绝大多数报表都能、也确实用此算法完成。

---

## 四、扩展变换算法（原书第 3 章）——非线性报表的核心机制

"扩展"是非线性报表把"源表（模板）"生成为"实例表（结果）"的过程。它由三个层层递进的算法构成：

```
扩展带复制(3.1)  →  主格扩展(3.2)  →  报表扩展(3.3)
   (一次复制)         (复制 n−1 次)       (调度所有主格)
```

### 4.1 算法 3.1　扩展带复制（基础原子操作）

**输入**：报表 `T` 上纵主格 `P`，其扩展带 `B[P]=R(p,q)`，`k=|B[P]|=q−p+1`。

步骤：
1. 对任何与 `B` **交叉**且与 `R(q)` 相交的单元格 `U`，截断为 `V = {E∈U | p≤row(E)≤q}`（去掉超出扩展带的部分）；
2. 在第 `q` 行后**加入 k 行**（执行 k 次算法 2.1）；
3. 对任何与 `B` 相交的单元格 `U`：
   - 若 `U∈DOMB[P]`，则 `U` 与新复制部分 `U'` 合并（仍是矩形）；
   - 若 `U∉DOMB[P]`，则把复制部分 `U'` 作为**复制格**加入 `UG`，并 `U'.val = U.val`；
4. 对 `P` 的每个属格/子格的复制格，递归设置其 `(左/上)主格 = 原主格的复制格`。

横扩展带复制对称。

- **定理 3.1**：复制后仍是报表（继承格保持主格线，复制格经算法设置后也有主格线）。
- **定理 3.2（单元格变迁）**：复制纵主格 `P` 后，单元格 `U` 的继承格/复制格的行号、行数、列号、列数有 5 种确定情形（按 `U` 与 `B[P]` 的位置关系）。**推论 1**：继承格及其复制格**列号、列数与原格相同**；行号/行数相同的单元格其继承格与复制格对应相同。
- **定理 3.3（扩展带变迁）**：其他扩展格 `Q` 的扩展带在复制 `P` 后如何变化，同样有 5~6 种情形。**推论 2/3**：实报表上扩展带相离的扩展格，复制后其继承格及复制格的扩展带仍两两相离。

### 4.2 算法 3.2　主格扩展（把一次复制推广到 n 份）

**规则 3.1**：未扩展格的格值必为非空数组（单值视为 1 元组，空值视为含 NULL 的 1 元组）。

**过程**（对未扩展格 `P`）：
1. 计算 `P.val`，得数组 `(v₁,…,vₙ)`，`n≥1`；
2. 把 `P` 的扩展状态置真；
3. 连续对 `P` 执行 `n−1` 次**扩展带复制**，产生 `P` 的 `n−1` 个复制格 `P₂,…,Pₙ`（记 `P₁=P`）；
4. 令 `Pᵢ.val = vᵢ`。

→ `DOMB[P]` 中每个单元格 `U` 产生 `n` 个**导出格**（继承格 `U₁` + 复制格 `U₂…Uₙ`），第 `i` 个称 `U` 的第 `i` 个导出格。

- **定理 3.5**：主格扩展后单元格变迁（把定理 3.2 中的 `+k` 推广为 `+k·(n−1)`）。
- **推论 2（整报表重要）**：整报表上扩展后**任何单元格的行数、列数都不变** → 可据此设计存储效率更高的实现。
- **定理 3.6 / 3.7**：其他扩展带的变化；纵主格与横主格的**交辖区在扩展中一一对应（基于取原格映射）**——这是后面"层次坐标"合理性的根基。
- **封闭性（定理 3.4 推论）**：主格扩展对**实报表 / (满)正实报表 / 整报表**封闭且反封闭。

### 4.3 算法 3.3 / 2.3　报表扩展（调度全部主格）

**规则 2.2**：主格扩展只能对"其主格已是已扩展格"的未扩展格执行。

**算法**：
1. 找一个可执行的未扩展格（其主格已扩展），找不到则结束；
2. 扩展它（算法 3.2）；
3. 回到 1。

- **扩展停机定理（定理 3.9）**：对任意报表，报表扩展必在有限步结束，结束时无未扩展格。
  *证明要点*：用数组 `u=(u₁,…,uₙ)` 记录各深度未扩展格数。每扩展一个深度 `k` 的格，`uₖ` 减 1，更深的 `uᵢ` 可能变大但永远有限；归纳证明有限次后全为 0。
- **扩展交换定理（定理 3.8，实报表核心性质）**：对**实报表**，扩展两个"主格已扩展的未扩展格 `P`、`Q`"**可交换**——先 `P` 后 `Q` 与先 `Q` 后 `P` 结果相同（含导出格与 `val`/扩展状态）。
  → 因此扩展实报表的结果**不依赖于主格选择顺序**。实践中因列数远少于行数，一般**先扩展横主格、再扩展纵主格**效率更高。
- **扩展封闭定理（定理 3.10）**：报表扩展对实报表/(满)正实报表/整报表封闭且反封闭。
- **非实报表**不具交换性，结果依赖选择顺序（需显式指定，否则取原序靠前者）。

### 4.4 源格、同源、同束（实例表单元格的身份系统）

扩展把一张源表变成多张中间表，最终实例表单元格极多。书中建立三套"坐标"来唯一定位它们：

- **源格 `src(u)`**：中间表单元格 `u` 在源表 `T₀` 上的根（递归取原格的源格），`@` 的源格是 `@`。
- **同源 / 同源号 `seq(p)=#p`**：同源格 = 源格相同的单元格；同主格的同源格按行(列)号排序后的序号。
- **同束 / 同束号 `ord(p)=&p`**：同束格 = 同一未扩展格的导出格（或其继承格），同源且同束。
- **定理 3.11 / 3.12**：同源扩展格列(行)号相同、行(列)号互异；同束格同源号连续；同源扩展格同束 ⇔ 主格相同。这些是"层次坐标唯一性"的保证。

---

## 五、语法与引用机制（原书第 4 章）

源表在设计时写表达式，但计算时要引用"尚未存在"的实例表单元格。因此需要三种坐标系统。

### 5.1 层次坐标（最常用、最安全）

**基格**：表达式总是"相对某个单元格 `c`"计算，`c` 称基格（如单元格的格表达式基格就是它自己）。

对中间表单元格 `c`，`C=src(c)`：
- 非主格：取左主格线 `(P₀…Pₙ)`、上主格线 `(Q₀…Qₘ)`，记 `Pᵢ=src(pᵢ)`、`xᵢ=ord(pᵢ)`，写成
  `C[P₁:x₁, …, Pₙ:xₙ ; Q₁:y₁, …, Qₘ:yₘ]`（`P₀=Q₀=@` 省略）。
- 扩展格：取主格线写成 `C[P₁:x₁, …, Pₙ:xₙ]`。

`xᵢ/yⱼ` 即 `c` 在主格 `Pᵢ/Qⱼ` 上的**层次坐标**（值是同束号，非 0）。

- **位移坐标**：相对基格 `a`，`b` 在 `P` 上的层次坐标差 `±x`/`0` → 写成 `+x`/`−x`/`0`，即**相对基格的层次坐标**。全部为 0 时退化为**零层次坐标**（只剩源格名 `C`）。
- **定理 4.1**：层次坐标**唯一**标志单元格。
- **定理 4.2**：零层次坐标 ⇔ 只能引用到"基格导格的先辖区"内的单元格。

**格集**：`C[P₁:x₁; Q₁:y₁]{过滤条件}` 表示满足条件的同源单元格集合；可对其用聚合函数 `sum(...)` 等。

### 5.2 线性坐标

用**同源号 `seq`** 代替同束号 `ord` 的坐标：`C[x;y]`（非主格）、`C[x]`（扩展格）。

- **定理 4.3**：线性坐标唯一。
- **定理 4.4**：单层主格的层次坐标与其等价线性坐标一致（`C[P:±x;Q:±y] = C[±x;±y]`）。

### 5.3 平面函数（绝对方位引用）

不依赖主子关系的方位函数：
- `rows()` / `cols()`：报表总行列数；
- `row(c)` / `col(c)`：单元格行列号；
- `cel(c,y,x) = U(row(c)+y, col(c)+x)`（特别 `cel(@,y,x)=U(y,x)`）；
- `range(a,b)`（记作 `a:b`）：矩形区域内单元格集合，可 `sum(a:b)`。

> 平面函数返回值会随进一步扩展而变化，计算时需特别处理（见 §6.3 隐含引用）。

### 5.4 数据源：数据集与缺省行集（原书 4.2）

**数据集**：由"数据数组"构成的矩阵，同列同类型；行=记录，列=字段（可起列名）。

- **划分函数**：`select(x)`（按表达式值分组，连续相同合并）、`group(x)`（完全划分）；派生 `rselect/rgroup/select(x,fx,sx)/group(x,fx,sx)`。
- **数据源属性**（定义在扩展格上）：`dsf`（过滤条件 `{(ds,x)}`）、`dsr`（关联行集 `{(ds,R)}`）。
- **主格扩展算法增加两步**：若 `P.val` 是某数据集 `ds` 的划分函数，把对应行集写入 `P.dsr`；再据 `dsf` 过滤补充 `dsr`。
- **缺省行集 `pdr(p,ds)`**（规则 4.1）：递归——`p=@` 时用全表；已扩展且 `p.dsr` 中有 `ds` 则用其行集；否则沿 `p.pu` 上溯。
  → 非主格 `c` 引用数据集时，缺省行集 = `pdr(c.xp,ds) ∩ pdr(c.yp,ds)`。
  **意义**：交叉表中横纵主格交辖区内的表达式能"智能、自动"地按所在位置取正确行集，无须手写，且可复用分组提高效率。

---

## 六、计算算法（原书第 5 章）

### 6.1 计算状态与可计算格（算法 5.1）

**规则 5.1**：单元格属性表达式**只能引用其他单元格的格值**（避免任意引用造成循环）。

- **计算状态**：已计算 / 未计算（特别地 `@` 视为已计算）。
- **可计算格**：其主格线上的扩展格均已计算、且表达式引用的单元格均已计算（或不存在）的未计算格。
- **计算 c**：扩展格则扩展之；非主格则算 `c.val` 并置已计算。

**算法 5.1　报表计算**：
1. 找可计算格 `c`，找不到则结束；
2. 计算 `c`；
3. 回到 1。

- **计算停机定理（定理 5.1）**：有限步结束（但结束≠无未计算格，只保证无"可计算格"）。
- **计算交换定理（定理 5.2）**：中间表上两个可计算格 `a,b` 计算可交换，**计算同构**（同位的格计算属性相同）。实报表计算结果与选择顺序无关。

### 6.2 隐含引用规则（消除二义性）

为判断"是否可计算"，需知道表达式引用了哪些单元格。书中规定 4 条隐含引用：

- **规则 5.2（平面函数）**：含平面函数的表达式**引用所有未扩展格**（保证 `row()`/`cols()` 等返回值确定）。
- **规则 5.3（线性坐标）**：`C[x]` 隐含引用 `P[1]…P[x]`（随扩展不再变化）。
- **规则 5.4（层次坐标）**：扩展格 `C[…]` 隐含引用 `Pₖ[…,1]`（把末位同束号降到 1 的同束格）；非主格类似引用左右主格线降序格。
- **规则 5.5（数据集）**：扩展格 `dsf` 中的过滤条件表达式须被其 `val` 表达式引用覆盖（保证 `dsr/pdr` 可计算）。
- **定理 5.3 / 5.4**：已计算/可计算格的层次坐标在扩展中保持不变；若层次坐标隐含引用的格均已计算，则该坐标在实例表存在 ⇔ 在中间表存在——消除"不存在 vs 未扩展出来"的二义性。

### 6.3 三种计算方法与复杂度（核心工程取舍）

| 方法 | 算法 | 失败试算复杂度 | 特点 |
|---|---|---|---|
| 反复遍历 | 5.2 | **O(n²)** | 反复扫描整表直到一轮无成功试算；最坏 `n(n−1)/2` 次失败 |
| 单次遍历 | 5.3 | **O(n)** | 用栈 `q` 记录依赖，失败则压入依赖格；可给出循环引用队列；不能保证异常时中间表无已算格 |
| 源格遍历 | 5.4 / 5.5 | **O(0)** | 先在源表找"可计算队列" `Qs`，按 `Qs` 顺序对"同源格"试算，**必全成功**；仅适用于无符号循环引用且表达式无平面函数的报表 |

**符号引用 / 可计算队列**：
- 源表上 `A` 显式出现 `B`（或 `B` 是 `A` 主格），或用了平面函数/`eval` → `A` **符号引用** `B`。
- 无符号循环引用 ⇒ 存在**可计算队列** `Qs`（成员符号引用的单元格都在其之前）。
- **定理 5.11 / 5.12**：有可计算队列 ⇔ 无符号循环引用 ⇔ 源表可计算（注意逆否不成立：可计算源表未必有可计算队列）。
- **实践建议**：先用代价极低的源格遍历"试算"，若异常（有符号循环引用）再退回到单次遍历法。这兼顾了大报表（实例表单元格多但关系简单）的高效性与通用性。

---

## 七、报表填写 / 回写（原书 5.3）

"填写" = 按源表规则改实例表格值，并写回数据库。

### 7.1 更新属性 `upd`

单元格 `C.upd = {(dt₁,u₁),…,(dtₙ,uₙ)}`：`dtᵢ` 为数据表，`uᵢ={(f,exp)}` 为"字段→更新表达式"。允许定义在 `@` 上。扩展时随单元格复制到导出格。

**算法 4.6　数据更新**：对每个有 `upd` 的单元格，按主键 `PK` 算 `v` 与 `u`（`v` 在原表 `T` 算、`u` 在改后表 `T'` 算）；若 `SELECT(v)>0` 则 `UPDATE` 否则 `INSERT`。
→ 实现"**有来有去、来去无关、一来多去**"：同一批格值可同时存多张表。

填写形态（可在同一实例表混合）：
- 整表对应单条记录（定义在 `@` 或其属格）：**自由式填写**；
- 每行列对应一条记录（定义在纵主格辖区）：**行列式填写**（最常见）；
- 每格对应一条记录（定义在横纵交辖区）：**交叉式填写**。

### 7.2 平面填写（平面计算串）

把表达式中的单元格/格集替换成**实例表平面坐标** → **平面计算串**。`a:b` 表示连续矩形，不连续则罗列。
- 优点：名称自带方位，**无需主子关系、无需算层次坐标**，适合 WEB 等弱计算环境；还能导出带公式的 EXCEL。

### 7.3 线性填写（线性计算串 + 动态增删）

**规则 4.6**：填写中的增删只能针对**叶子主格**，且一次增删必须是该主格的**完整扩展带**。
**整填报表**：仅主格有 `upd` 定义（故不能做交叉式填写）。

**线性计算串**（规则 4.7）：① 层次坐标全是零层次坐标；② 格集无过滤条件；③ 无线性坐标/平面函数。
→ 只能引用"导格先辖区"内单元格；**无法由平面化自动得到，必须在源表设计时限制**。

**算法 4.7　动态更新**：先对带删除标记的叶子主格 `DELETE`（仅原格有 `upd` 的），再在其扩展带执行删除；随后对剩余有 `upd` 的单元格 `UPDATE/INSERT`（存在则更新，否则插入）。

> 平面 vs 线性：**平面计算串**适合"格式运算复杂但单元格少"的报表；**线性计算串**适合"格式简单但容量巨大"的报表（占用空间少、可实时计算）。纯线性计算串的整报表甚至可用于超巨报表计算以提升性能、减少存储。

---

## 八、核心算法一览（便于实现时对照）

| 编号 | 名称 | 作用 |
|---|---|---|
| 2.1 / 2.2 | 表格行列增删 | 在表格中增/删行列并维护 UG 与属性 |
| 2.3 | 子表引入 | 把子表撑大后填入宿主表，处理套表/有限扩展/交替扩展 |
| 2.4 | 自动主格设置 | 按"左边第一个更大格"自动设主格，临时报表→报表 |
| 3.1 | 扩展带复制 | 复制一次扩展带，产生复制格，原子操作 |
| 3.2 | 主格扩展 | 对未扩展格执行 n−1 次复制，产生 n 个导出格 |
| 3.3 / 2.3 | 报表扩展 | 调度所有主格完成整表扩展（停机、可交换） |
| 4.4 / 4.5 | 可计算队列寻找 / 源格遍历 | 在源表找无环计算顺序，O(0) 失败试算 |
| 5.1 | 报表计算 | 反复找可计算格计算，直至无可计算格 |
| 5.2 | 反复遍历法 | 全表扫描试算，O(n²) 失败 |
| 5.3 | 单次遍历法 | 依赖栈试算，O(n) 失败，可给出循环引用队列 |
| 4.6 | 实例表数据更新 | 按 upd 属性 INSERT/UPDATE 回写数据库 |
| 4.7 | 动态更新（含增删） | 处理填写时增删叶子主格扩展带并回写 |

---

## 九、关键性质与设计要点总结

1. **一切以单元格为中心**：网格→表格→单元格→扩展属性→主格树→扩展带→辖区，构成完整的几何+代数结构。
2. **三层扩展算法**保证把一个"模板（源表）"确定性地展开成"结果（实例表）"；对实/(满)正实/整报表**封闭且可交换**，结果与扩展顺序无关（实践先横后纵）。
3. **扩展必停机**（定理 3.9），**计算必停机**（定理 5.1），二者都可证明。
4. **三套坐标**解决"设计时写、运行时引用"的错位：层次坐标（唯一、安全、支持相对引用）、线性坐标（简洁）、平面坐标（弱环境友好）。
5. **缺省行集 `pdr`** 让交叉表的数据集引用"自动对位"，是表达式智能化的关键。
6. **可计算性可静态判定**：符号循环引用 ⇔ 无可计算队列 ⇔ 不可（静态）计算；运行时用"单次遍历"在 O(n) 失败试算内完成并定位循环。
7. **填写三形态 + 两种计算串**：平面（小而复杂）/线性（大而简单），覆盖自由式、行列式、交叉式及动态增删。
8. **线性报表是交集真子集**：非线性模型在表达能力上严格包含线性模型，但线性模型在"能用它描述的简单大报表"上时空效率更高——二者互补而非替代。

---

## 十、原文指出但尚未完全解决的开放问题（设计延展方向）

- **布局**：有限扩展（扩展带仅矩形而非整行/列）、交替扩展（纵主格有横子格）、分页处理；
- **计算**：格间关联模型与统计图、允许属性引用时的循环引用消解；
- **填写**：非叶子主格的动态填写、交叉动态填写（更新属性不只定义于主格）；
- **实现**：算法的高效工程化方案（如利用整报表"单元格大小不变"特性优化存储）。

---

## 十一、工程实现算法补充（新增 · 计算引擎架构）

以上内容（二–十）主要是**理论算法**：已建立"单元格第一公民""扩展 + 引用"的模型，并证明了停机、封闭、可计算等性质。但开放问题（§十）也指出，实现层仍缺"算法的高效工程化方案"。本章给出把**非线性报表数学模型**落地为**非线性报表计算引擎**所需的关键工程算法层，与前面的 2.x / 3.x / 5.x 理论算法一一对应。

> **落地映射（呼应 Agent Runtime / Ontology OS 思路）：** `Cell` ≈ Entity（实体）· `MasterGraph` ≈ Ontology Graph（本体图）· `ExpressionGraph` ≈ Rule Graph（规则图）· Expand Engine ≈ Runtime（运行时）· Calculation Engine ≈ Agent Execution（智能体执行）。

### 11.1 报表内部对象模型算法

**理论模型**把报表拆成 5 个对象：`Report` → `Grid` / `Cell` / `MasterGraph` / `ExpressionGraph` / `RuntimeContext`。**实际实现**用扁平化结构体承载：

```rust
struct Report {
    cells: Vec<Cell>,        // 单元格数组（原序）
    masters: MasterGraph,     // 主格树
    expressions: ExprGraph,   // 表达式依赖图
}

struct Cell {
    id: CellId,
    row: i32, col: i32,       // 原始坐标
    rowspan: i32, colspan: i32, // 合并范围
    xp: Option<CellId>,       // 左主格
    yp: Option<CellId>,       // 上主格
    value: Value,             // 值（运行期填充）
    expr: Option<Expression>, // 表达式
    state: CellState,         // 计算状态
}
```

**核心思想**：`Cell` **不保存结果，而保存"如何产生结果"**（坐标、主格关系、表达式、状态）。结果在运行时由 Expand/Expression 引擎惰性填充到 `value` / `state`，从而支持增量更新与缓存。

（图示见 HTML 版 图 14：Report 聚合 Grid/Cell/MasterGraph/ExpressionGraph/RuntimeContext。）

### 11.2 主格树构建算法

**已有理论**：主格线 `P₀=@, P₁.xp=@, P₂.xp=P₁, P₃.xp=P₂` 形成树。**工程实现**把它转成一次性建树：

```
输入:  Cell 集合
输出:  MasterTree

BuildMasterTree():
  1. 创建根节点 @
  2. 遍历所有 cell:
       if cell.xp != null:
           parent = cell.xp
           parent.children.add(cell)
  3. 对 yp 同理（横主格方向）
  4. 计算每个节点深度 depth
  return tree
```

**复杂度：O(N)**（N = 单元格数，单遍遍历）。构建后即可供扩展调度（§11.3）与单元格定位（§11.8）直接复用，无需每次重算主格线。

（图示见 HTML 版 图 15：单向遍历 Cell 按 xp/yp 挂父节点，O(N) 建树。）

### 11.3 扩展计划算法（优化算法 3.3）

原算法 3.3 每轮"扫描全部格找可扩展主格"，最坏 **O(N²)**。优化：用**扩展队列**替代全表扫描。

```
struct MasterNode { id; expanded:bool; depend_count:int }

初始化:
  for master:
    if parent.expanded:        // 主格已扩展 → 可扩展
        queue.push(master)

while queue not empty:
    P = queue.pop()
    expand(P)
    for child in P.children:
        if child.ready():      // 依赖均已扩展
            queue.push(child)
```

**复杂度：O(M)**（M = 主格数量，M ≤ N）。每个主格入队/出队各一次，避免重复扫描非主格单元格。实践仍"先横后纵"（§4.3）。

（图示见 HTML 版 图 16：扩展队列入队/出队，O(M)。）

### 11.4 增量扩展算法（重要）

原算法每次"重新生成整张实例表"。实际系统用户追加一条数据（如销售明细新增"商品D"）时，不应重算整表，只应更新受影响的 `DOMB[P]`。

```
IncrementExpand(P, newData):
  1. 定位主格 P 及其扩展带 B[P]
  2. 计算新增数量 k = |newData|
  3. 复制扩展带：在 B[P] 末尾追加 k 份（算法 3.1 局部版）
  4. 更新主格索引 / 同束号 ord
  5. 触发依赖重算（仅 DOMB[P] 及下游）
```

**复杂度**：从整表重算的 **O(N)** 降为 **O(size(DOMB))**（仅受影响的后辖区单元格）。配合主格树版本号 `version`（新增数据 `version++`），可实现"只动变化部分"。

（图示见 HTML 版 图 17：新增数据只在扩展带末尾追加，局部更新 DOMB[P]。）

### 11.5 依赖计算图算法（DAG + 循环检测）

理论已规定"单元格只能引用其他单元格的格值"（规则 5.1），因此天然形成**有向无环图 DAG**。据此可拓扑排序计算，并检测符号循环引用。

```
ExpressionGraph:  A1 → B1 → C1   （引用方向）

拓扑排序 Kahn:
  indeg=0 的节点入队
  while 队列非空:
      计算节点; 删除其出边; 更新下游入度
  若仍有未计算节点 ⇒ 存在环

CycleDetector:
  输出环路径，如  A1 → B1 → C1 → A1
```

**复杂度：O(N+E)**（N 节点 / E 依赖边）。拓扑排序同时完成"计算顺序"与"循环检测"——对应理论的可计算队列（§6.3，定理 5.11/5.12），但工程版能**精确给出环路径**用于报错定位。

（图示见 HTML 版 图 18：拓扑排序得计算顺序，剩余节点即循环引用。）

### 11.6 高性能计算调度算法（三级缓存）

普通"对每个 cell 计算"会大量重复。优化为三级缓存，逐级命中避免重复计算：

```
L1  CellCache           cell_id  → value          // 单元格结果
L2  ExprCache           hash(expr) → result         // 同表达式结果
L3  MasterInstanceCache master_version → 实例片段  // 整段扩展结果

流程:
  用户请求 → 检查 L3 实例缓存 → 检查 L2 表达式缓存
          → 必要时计算 → 写回 L1/L2/L3
```

**复杂度**：命中缓存为 **O(1)**。配合 §11.4 的主格版本号，L3 可在"数据未变"时直接复用整段实例，大报表收益显著。

（图示见 HTML 版 图 19：实例 → 表达式 → 单元格，逐级短路重复计算。）

### 11.7 并行计算算法

非线性报表的 DAG 天然可并行：入度为 0 的节点彼此无依赖，可由线程池并发执行。

```
while DAG 非空:
    ready = find_all(indegree == 0)   // 一批可并行节点
    parallel_execute(ready)           // 提交线程池
    update_graph()                    // 完成后删边、更新下游

线程模型:
  Expansion Thread        // 扩展
       │
  Calculation Thread Pool // 并行计算
       │
  Render Thread           // 渲染
```

**复杂度：O(N/P)**（P = 线程数）。与 §6.2 的隐含引用规则配合——只有"引用所有未扩展格"的平面函数类表达式需串行屏障，其余纯值依赖可完全并行。

（图示见 HTML 版 图 20：入度为 0 的节点批量并发，DAG 天然分层并行度。）

### 11.8 单元格定位算法

Excel 用 `A1/B2` 二维坐标；非线性报表还需**层次坐标**。工程上用"主格路径 + 偏移"唯一定位实例单元格：

```
struct Position {
    master_path: [ @, Region, Customer, Order ],  // 主格线（源格序列）
    offset: { row: 3, col: 5 },                  // 在该主格辖区内的物理偏移
}

定位:  Cell → Master Path → Instance Node → Physical Coordinate
```

该结构与理论"层次坐标（源格名 + 主格线同束号）"同构，但把"同束号"显式展开为 `master_path` + `offset`，便于在运行期实例表中 O(1) 跳转到物理坐标（对应 §5.1 定理 4.1 的唯一性）。

（图示见 HTML 版 图 21：主格路径 + 偏移 → 物理坐标，O(1) 跳转。）

### 11.9 全局算法流程

最终 Runtime 把上述算法串联成一条流水线：

```
Template → Master Analyzer → (Expand Engine | Expr Engine) → Instance Table → Render Engine → HTML/Excel/PDF
```

（图示见 HTML 版 图 22：模板 → 主格分析 → 扩展/表达式引擎 → 实例表 → 渲染。）

### 11.10 推荐增加算法总表

| 编号 | 算法 | 作用 | 复杂度 |
|---|---|---|---|
| A1 | Master Tree Builder | 建立主格树（§11.2） | O(N) |
| A2 | Expand Scheduler | 扩展调度，替代全表扫描（§11.3） | O(M) |
| A3 | Increment Expand | 增量扩展，仅更新 DOMB（§11.4） | O(size(DOMB)) |
| A4 | Dependency Graph | 建立计算依赖 DAG（§11.5） | O(N+E) |
| A5 | Topo Calculator | 按拓扑序公式计算（§11.5/§6） | O(N+E) |
| A6 | Cycle Detector | 循环引用检测，输出环路径（§11.5） | O(N+E) |
| A7 | Cache Engine | 三级缓存，命中 O(1)（§11.6） | O(1) |
| A8 | Parallel Executor | 并行计算（§11.7） | O(N/P) |
| A9 | Expansion Band Model | 矩形扩展带模型，扩展单位下沉为 Band（§11.11） | — |
| A10 | Finite Expand | 有限扩展，局部矩形复制（§11.11） | O(count×band) |
| A11 | Coordinate Mapping | 非整行/列的逻辑→物理坐标映射（§11.11） | O(1) |
| A12 | Alternating Expansion Tree | 纵/横交替扩展树（§11.12） | O(T) |
| A13 | Recursive Expansion | 嵌套主格递归扩展（§11.12） | O(N) |
| A14 | Page Layout Engine | 分页扫描与装填（§11.13） | O(B) |
| A15 | Band Split Algorithm | 跨页带切分，主格不裂（§11.13） | O(band) |
| A16 | Repeat Header Algorithm | 分页重复标题（§11.13） | O(1) |

> **升级结论：** 增加 §11 后，文档从"非线性报表**数学模型**"升级为"非线性报表**计算引擎架构设计**"。它与前面 2.x（表格结构）、3.x（扩展）、5.x（计算）理论算法形成闭环：理论保证正确性与终止性，工程算法保证效率与可落地性。这也与 Ontology OS / Agent Runtime 思路同构——`Cell≈Entity`、`MasterGraph≈Ontology Graph`、`ExpressionGraph≈Rule Graph`、`Expand/Calculation Engine≈Runtime/Agent Execution`。

---

### 11.11 有限扩展算法（Expansion Band Algorithm）

传统报表的明细扩展本质是"插入整行 / 复制整行"。但非线性报表的扩展单位**不是 Row / Column，而是矩形扩展带（Rectangle Band）**——只允许商品区、数量区、金额区这类局部矩形被复制，整表其余部分（如外层"销售单"标题、客户主格）保持不动。

#### 11.11.1 扩展带模型

把"可扩展区域"抽象为一个带边界与方向的矩形对象：

```rust
struct ExpandBand {
    id:          BandId,
    start_row:   i32,   // 矩形上界
    end_row:     i32,   // 矩形下界
    start_col:   i32,   // 矩形左界
    end_col:     i32,   // 矩形右界
    direction:   ExpandDirection,  // 扩展方向
    master:      CellId,          // 主格
    clone_rule:  CloneRule         // 复制规则
}
```

例：模板中 `A5:C5` 这一横条即一个 Band：`start_row=5, end_row=5, start_col=1, end_col=3`。它只覆盖"商品|数量|金额"三列，扩展时只复制这一条，不影响同表的客户主格与销售单标题。

> 图 23（见 HTML 版）：扩展带模型——扩展单位从"整行"下沉为"矩形 Band"，由四边界 + 主格 + 方向唯一刻画。

#### 11.11.2 有限扩展算法

```
输入： ExpansionBand, data[商品A, 商品B, 商品C]
输出： 实例区域（如 A5:C7）

FiniteExpand(Band, data):
  1. 取 Band 尺寸
       height = end_row - start_row + 1
       width  = end_col - start_col + 1
  2. for item in data:
       克隆 Band  →  offset += height      // 沿方向堆叠实例槽位
  3. 建立主格关系： newCell.xp = master
  4. 更新坐标索引
  return InstanceBand

复杂度： O(data_count × band_size)
```

#### 11.11.3 非整行 / 列坐标映射算法

由于扩展单位是矩形而非整行，不能用 `row++` 推进。必须建立**逻辑坐标 → 物理坐标**的映射：

```
physicalRow = band.startRow + index × band.height + localRow
physicalCol = band.startCol                          + localCol
```

其中 `index` 是第几条数据，`localRow/localCol` 是单元格在 Band 内的相对位置。这样任意实例单元格都能被唯一定位，且与"整行 / 整列"解耦。

### 11.12 交替扩展算法（Alternating Expansion）

这是非线性报表**最关键的差异化能力**：一个纵向主格之下，可以挂一个横向子主格。例如"客户（纵向）"内部，"订单（横向）"再扩展，订单内"商品（纵向）"再扩展——纵横交替嵌套。

#### 11.12.1 扩展方向模型

```rust
enum ExpandDirection { Vertical, Horizontal, Alternating }

主格 Customer  direction = Vertical   // 向下复制整带
子格 Order      direction = Horizontal // 向右复制整带
   ↓
Customer Master → Order Master → Product Master
```

#### 11.12.2 交替扩展树

```rust
struct ExpandNode {
    master:     CellId,
    direction:  ExpandDirection,
    children:   Vec<ExpandNode>
}
```

> 图 24（见 HTML 版）：交替扩展树——纵 / 横方向在树的不同层交替出现，每层独立决定复制方向。

#### 11.12.3 递归扩展算法

```
fn expand(node, data):
    area = create_band(node)            // 按 direction 生成行或列
    for child in node.children:
        childData = filter(data, child.master)
        expand(child, childData)        // 递归嵌套
```

#### 11.12.4 横纵交替坐标计算

父（纵向）贡献行偏移，子（横向）贡献列偏移，二维实例坐标由二者叠加：

```
Y = Py + parentIndex × parentHeight   (行)
X = Px + childIndex  × childWidth     (列)
```

> 图 25（见 HTML 版）：横纵交替坐标——实例格 = 父偏移 + 子偏移，与整行 / 整列无关，是交替扩展的定位基础。

### 11.13 分页处理算法（Page Break Algorithm）

非线性报表的分页比 Excel 复杂：一个扩展带可能跨页，不能简单用 `row % pageHeight` 切。需要页面模型、扫描、跨页切分与重复标题四步。

#### 11.13.1 页面模型

```rust
struct Page {
    number:      i32,
    height:      i32,
    used_height: i32,
    bands:       Vec<Band>
}
```

#### 11.13.2 分页扫描算法

```
for band in bands:
    if page.used_height + band.height <= page.height:
        page.add(band)              // 装得下则并入
    else:
        page = new_page()           // 装不下则开新页
        page.add(band)
```

#### 11.13.3 跨页扩展算法（Band Split）

当某 Band 高度超出当前页剩余空间时，不能直接切断，须定义切分结构，保持主格关系不断裂：

```rust
struct SplitBand { original_band, part1, part2 }
// 第 1 页：客户A + 商品1/2 ；第 2 页：商品3/4（接续，主格仍指向同一客户）
```

#### 11.13.4 重复标题算法

```rust
struct PageHeader { repeat_band: BandId }
// 每开新页： new_page() → clone header band → insert 到页首
```

> 图 26（见 HTML 版）：分页处理——扫描装填、跨页 Band Split、每页重复标题三件套。

### 11.14 综合扩展 Runtime

把 11.1–11.13 串成一条完整运行时流水线，模板到渲染的每一步都由独立算法支撑：

> 图 27（见 HTML 版）：综合扩展 Runtime——模板 → 主格分析 → 扩展树 →（有限 / 交替）扩展 → 分页布局 → 渲染。

```
模板解析 → 主格分析 → 扩展带生成 → 有限扩展 → 交替扩展 → 依赖计算 → 分页布局 → 渲染输出
```

至此，非线性报表引擎的**核心算法闭环基本形成**（A1–A16 见 §11.10 总表）。它已接近润乾报表 / FineReport 类非线性报表引擎的核心 Runtime 设计，并可直接映射到 Ontology Runtime：

| 报表引擎概念 | Ontology Runtime 映射 |
|---|---|
| Expansion Tree（扩展树） | Ontology Graph（本体图） |
| Band（扩展带） | Entity Instance Template（实体实例模板） |
| Cell Dependency（单元格依赖） | Rule Graph（规则图） |
| Page Engine（分页引擎） | Projection Layer（投影层） |
| Runtime（运行时） | Agent Execution Engine（智能体执行引擎） |



---

## 十二、展现模型与数据模型（润乾官方模型层）

本章内容取自润乾报表（Raqsoft）官方《高级指南·非线性报表模型》的两章权威定义：**展现模型**（`zxmx36.html`）与**数据模型**（`sjmx43.html`）。作者蒋步星即润乾报表创始人，因此这两章是本文档理论部分（§2 表格结构、§6 缺省行集、§11 对象模型）所依据的工程源头。它们构成"模型基础层"——在扩展算法与计算算法之前，先定义**报表长什么样（展现）**与**报表数据从哪来、如何组织（数据）**。

### 12.1 展现模型（Presentation Model）

采用"类 Excel"方案：报表是由 N 行 M 列的基本矩形格子构成的平面图形，这些格子称为**单元格**。核心定义：

- **单元格**：平面图形的基本单元（矩形格子）。
- **合并单元格**：选中一片**矩形**连续区域并合并后得到的大单元格（合并区域只能是矩形，与 §2.1 矩形引理一致）。
- **行 / 列**：水平排列的一组单元格为行（从上到下 1,2,3…）；垂直排列的一组为列（从左到右 A,B,C…）。
- **单元格命名**：用"列号 + 行号"命名，如 A 列第 1 行 = `A1`；合并单元格以**左上角**单元格编号作为整格编号（如合并格 `B2`）。
- **报表**：由单元格、合并单元格、行、列组成的二维矩形表格；加上"表格中的数据 + 数据间的运算逻辑"即构成报表。

> 图 28（见 HTML 版）：类 Excel 的单元格/合并格/行列/命名体系，是 §2 表格结构的形式化源头。

### 12.2 数据模型（Data Model）

数据是报表的基础。润乾用以下概念组织数据：

- **数据集**：由"数据数组"构成的二维数据表，同列元素类型相同，每列有唯一名字。
- **记录 / 字段**：为区别于报表的行/列，数据集的**行**称记录、**列**称字段。记录有行号 1,2,3…（存于第 0 列）。
- **行集**：由数据集的行按某种顺序构成的数组；数据集本身也是行集。组成上是数据集的子集，但次序未必相同。
- **分组与组集**：把记录按规则划分成 N 个行集的过程称为**分组**，每个行集称为一个**组**，多个组构成**组集**。
- **数据集函数**：对记录取数/分组/运算的函数，书写为 `dsName.f(…)`。
- **单源 / 多源报表**：数据源含 1 个数据集为单源（ds1），含多个为**多源**（ds1, ds2…）。

关键书写规则（取行号）：

```
dsName.#0              // 获得当前行的行号（#0 即第 0 列）
dsName.select(#0)      // 获得数据集所有记录的行号
dsName.f(...)          // 数据集函数
```

**多数据集模型**是润乾的差异化设计：多个数据集**不做 union/join 整合运算**，而是直接用于报表运算，并在运算过程中按关联键（如左表头"项目名称"）动态关联。它支持异构数据源与多源分片报表，避免了整合运算对 CPU / 内存的占用，性能显著优于"先合并再计算"的传统单数据集模型。

> 图 29（见 HTML 版）：数据集（记录/字段）经分组得到组集；多源数据集直接关联，无需整合运算。

### 12.3 与本设计文档的映射

| 官方模型层 | 本设计文档对应 |
|---|---|
| 展现模型：单元格 / 合并格 / 行列 / 命名 | §2 表格结构（网格、单元格数组、矩形引理 2.1、主格/扩展属性） |
| 数据模型：数据集 / 记录 / 字段 | §6 数据集与缺省行集 `pdr`、§11.1 对象模型 `Report.cells` |
| 行集 / 组集（子集、有序、分组） | §6 缺省行集 `pdr` 即为某主格对应的行集；分组即扩展生成组 |
| 多数据集模型（直接关联、异构） | §11.1 多源结构 + §11.5 依赖图关联；应作为 Runtime 的**数据层**前置 |
| 数据集函数 `dsName.f(…)` | §6 平面函数 / 数据集函数；§11.5 表达式图节点 |

> **延伸建议：** 官方《非线性报表模型》下还有**运算模型**（`ysmx46.html`，对应本设计 §5/§6 的计算与层次坐标）与**子报表模型**两章。把"数据层（本章）+ 展现层（本章）+ 扩展层（§3）+ 运算层（§5/§6）+ 工程 Runtime（§11）"拼齐，即可得到与润乾完全对齐的端到端模型—引擎架构说明。

---

## 十三、运算模型与子报表模型（润乾官方）

本章取自润乾官方《高级指南·非线性报表模型》的另两章：**运算模型**（`ysmx46.html`，对应本稿 §3 扩展、§2.4 主格、§6 层次坐标/格集、§11.2/§11.8/§11.13）与**子报表模型**（`zbbmx52.html`，对应 §2.3 子表引入）。它们把"如何计算"与"如何嵌套组合"从理论层面落到官方可操作的书写规则上，是 §5/§6 计算算法与 §11 工程 Runtime 的规范源头。

### 13.1 集合、数组与表达式基础

- **集合**：确定事物的汇集；润乾中元素可以是数据、单元格、行、列、记录、字段、组等。
- **数组**：集合按规则排序所得的有序序列，成员个数为长度；是集合的特例。
- **集合函数 / 操作符**：结果为集合的运算；如 `select()`、`group()`、`list()`、`query()`、`call()`、`to()`。
- **集合表达式 / 单值表达式**：结果分别为集合 / 单值；前者使单元格成为**可扩展单元格**（默认按扩展方向复制若干份），后者为**不可扩展单元格**。

### 13.2 扩展模型与主格模型

扩展方向有横、纵两种，一个扩展格同时只能有一个方向。**主格 / 附属格**是相对概念：扩展格主动复制，其右（下）的格被动跟随，称前者为后者的主格，后者为附属格；**左主格**（纵向扩展时）与**上主格**（横向扩展时）可分别人为设置，但须满足：左主格必为纵向扩展格、上主格必为横向扩展格、禁止循环设置、横向格无左主格、纵向格无上主格。

- **根格（报表首格）**：左上角灰格，存报表级属性，层次坐标特殊基准，书写为 `` `0 ``（反引号 0，非引号）。
- **缺省主格认定**：纵向扩展时左主格缺省为左边纵向扩展格（无则 `` `0 ``）；横向扩展时上主格缺省为上方横向扩展格（无则 `` `0 ``）。
- **扩展变化规则**：从主到次逐级扩展；横向扩展拉同列上主格、纵向扩展拉同行左主格；主/附属格重叠区域形成**交叉扩展**，横纵扩展相互独立、顺序无关（呼应 §3.8 扩展交换定理）。
- **缺省引用规则**：格间运算直接写单元格名即可，前提是被引格能由当前格唯一确定——通常是当前格的主格、或与其有相同一级主格、或是固定格。

> 图 30（见 HTML 版）：主格/附属格树——根格 `0 为根，纵向扩展产生左主格、横向扩展产生上主格，多层扩展按级别递推。

### 13.3 层次坐标、位移坐标与根坐标

扩展前无法预知某格会复制出几个、在哪儿，因此引入**层次坐标**唯一描述扩展后的每个单元格：

```
cellx[ Lk:lk, Lk-1:lk-1, … L1:l1 ; Tk:tk, Tk-1:tk-1, … T1:t1 ]
// Lk/Tk 为左/上主格，lk/tk 为其扩展后的次序（从远到近）
// 只有上主格时，分号不可省；只有左主格时，分号可省
```

- **位移坐标**：描述目标格相对当前格的位置（相邻 n 行/列），如同期比、环比；符号用 `±lk / ±tk`。
- **根坐标表示法**：完整坐标从根开始，写成 `cellx[`0, Lk:lk,… ; `0, Tk:tk,…]`，`` `0 `` 即根。
- **`&` 运算符（逆运算）**：`&cellx` 返回当前格所属的 cellx 主格在所有扩展格中的排序（第几个）。
- **缺省表示法**：目标格某主格与当前格相同时可省略；满足缺省引用规则时直接写单元格名。

> 图 31（见 HTML 版）：层次坐标 / 位移坐标 / 根坐标 / `&` 运算符——扩展前精确定位扩展后实例格。

### 13.4 格集与 $ 运算符

对一组单元格运算时引入**格集**：

```
list(Cell1, Cell2, …)            // 枚举若干格
[Cellx : Celly]                  // 以两格为对角的矩形区域（link 操作符）
sum([A1:B3])                     // 对矩形区聚合
cellx[层次/位移坐标]{}            // 坐标界定范围内的单元格集合（有{}返集合，无{}返首格）
cellx[坐标]{条件表达式}          // 集合内再按条件过滤
C2[A2:-1]{ $B2 == B2 }           // $B2 指代当前格的 B2 主格，B2 指代目标格的 B2 主格
```

缺省格集：`cellx[\]{}` 或 `Cellx{}` 表示当前格主格管辖范围内的全部目标格集合。

> 图 32（见 HTML 版）：格集表示——矩形区 / 枚举 / 坐标范围加 `{}` 成集合；`$` 运算符指代当前格主格，简化条件表达式。

### 13.5 定位与拉伸（报表区域）

整表分区（横向：左表头→常规列→右表头；纵向：分页页眉→头标题→报表头→分组表头→数据区→表尾区→尾标题→分页页脚），区域顺序不可颠倒，合并格不能跨区。关键规则：

- **空白单元格优先占用**：不存属性的占位格，保证整表为矩形且不占内存；扩展复制时紧邻的空白格被优先占用。
- **分页时伸缩**：标题区无法预知数据区横向扩展大小，对中间某格设"分页时伸缩"属性，横向扩展时引擎对其拉伸；无设定则拉伸最右格。
- **位置不变**：横向扩展时，除靠右对齐与被拉大的格外，其余格保持位置不变。

### 13.6 子报表模型

含多片有共同主题但各自独立子主题的单元格片段的报表称**主子报表 / 复合报表**；片段即**子报表**。润乾基于单元格模型提供两种方式：

- **引入式子报表**：以当前格为扩展格向右下扩展，占用主表空白行列、接受主表行高列宽，扩展后与主表形成**统一二维矩形**；源格不保留，主表**不能**访问子表值。
- **嵌入式子报表**：缩在当前格内或仅撑大当前格，不影响周边；格中嵌表、格线不对齐，**不能导出 Excel**；源格保留，主表可用 `eval()` 访问。

**参数传递与关联**：子表参数统一由主表入口传递（主表参数 / 表达式结果 / 常数）；主子表共用运算环境，参数或数据集重名即共用（内容相同建议同名以省数据库访问）。嵌入式访问语法：

```
eval( StringExp, SubRptExp )
// 例：取嵌入式子报表 A1 中 B2 的值加 10
eval("B2+10", A1)   或   eval("B2", A1) + 10
```

> 图 33（见 HTML 版）：子报表——引入式（统一矩形、源格不保留）vs 嵌入式（格中嵌表、eval 访问），参数由主表统一传递。

### 13.7 与本设计文档的映射

| 官方运算/子报表概念 | 本设计文档对应 |
|---|---|
| 集合 / 数组 / 集合函数 / 集合表达式 | §1 数学基础（四种变换、聚合/集合函数）、§3 扩展（集合表达式→可扩展格） |
| 主格 / 附属格 / 左主格 / 上主格 / 根格 `0 | §2.4 自动主格设置、§11.2 主格树构建（根 `@` 即 `0） |
| 扩展变化规则 / 交叉扩展顺序无关 | §3.8 扩展交换定理（实报表顺序无关）、§11.12 交替扩展 |
| 层次坐标 / 位移坐标 / 根坐标 / `&` | §6 层次坐标、§11.8 单元格定位（master_path+offset） |
| 格集 / `{}` / `$` 运算符 / 条件表达式 | §6 平面函数与集合运算、§11.5 表达式图节点 |
| 定位与拉伸（空白格优先 / 分页伸缩） | §11.13 分页处理（Band Split / Repeat Header） |
| 引入式 / 嵌入式子报表 / eval() | §2.3 子表引入、§4 报表填写（更新属性 upd） |

> **延伸建议：** 官方《非线性报表模型》下，**填报表模型**的**采集模型**已并入 §14（对应本稿 §4 报表填写）；该模型其余子章（如"扩展模型"）尚未纳入。纳入后，"展现 + 数据 + 运算 + 子报表 + 填报表"五模型齐备，即与润乾理论完全对齐。

---

## 十四、填报表采集模型（润乾官方）

> 本章取自润乾官方《高级指南·填报表模型·采集模型》（`cjmx56.html`，蒋步星）。它把本稿 §4 报表填写的理论（更新属性 `upd`、自由式/行列式/交叉式三形态、动态增删）落到工程级"如何把一张格子表自动识别为数据库可增删改的**序表**"。图示见 HTML 版 图 34–35。

### 14.1 三类单元格与序表

填报模型把单元格分成三类，再由采集程序自动拼成"序表"（序表 ≈ 一个二维数据表，可作增删改的单元）：
- **普通格**：一般作为字段名（表头）；
- **维度格**：字段值，标注"属于哪个对象的哪个维度"；
- **数值格**：字段值，是真正要采集/回填的数据；自动计算的数值格可由其它数值格算出（既用于展现，也用于采集回填）。

采集程序**自动分片**：行列上连续的一片数值格（连同对应维度格）为一片；连续两行以上"只有数值格、无维度格/非空普通格、且不合并"的视为**行式分片**（支持自由增删行）；其余单值（维度/数值格）被拼成一个"一行记录"的序表。**序表**由"维度格字段名相同的数值格"聚合而成——单值同对象名 → 并入同一序表，不同对象名 → 不同序表（单行序表的维字段作为公共维默认拼入其它多行序表）。

### 14.2 数据采集规则

**字段名采集**：数值格/维度格未显式设字段名时，规则为——扩展后表格，取上（左）同宽（高）的普通格内容作字段名。例：B1/D1 以左侧 A1/C1 为字段名；A4–D5 以顶部 A3–D3 为字段名。

**同序表 / 同记录采集**：维度格字段名相同的数值格视为同一序表（对象）；同分片内维度值不同的为不同记录，格内容作为相应字段值。行式填报表中"数值格区同一行 = 同一条记录"。例：数值格 B3 的维度字段为 {地区, 年度}，与 C3 同对象 → 组成 (地区, 年度, 金额, 数量) 序表；而 C2–C5 维度为 {年份, 地区}、E2–E5 为 {年份, 地区, 状态} → 裂成两个序表。指定对象名可显式分片（同名则合并，异名则分离）。

### 14.3 取数表达式与回填

一个完整填报表由三部分组成：
- **取数 SPL**：把要回填的序表赋值给变量，可返回多个（序表变量、维序列、下拉序表）；
- **填报表 SHT**：设计模板，在普通格用 `变量名.字段名` 引入，如 `score.SCORE`；
- **回填 SPL**：页面填好后通过同一变量名取走填写后的序表，处理写库。

空数据展现时取数 SPL 可省；如要取回填前数据，需在"数据处理"中保存旧数据（如 `score_old`）。维度格表达式遵循集算器脚本语法，可直接返回单值或维序列；也可在单元格里直接定义（如只取 STUDENTID 为 1、3 的数据）。

### 14.4 与本设计文档的映射

| 采集模型概念 | 本稿对应 | 说明 |
|---|---|---|
| 三类单元格（普通/维度/数值格） | §2 表格结构、§11.1 对象模型 | Cell 的 state/value/expr 可扩展一个 role 标记 |
| 自动分片 / 序表 | §4 报表填写、§11.4 增量扩展 | 序表 ≈ 一个可写回的实例分片 |
| 取数/回填 SPL + 变量名.字段名 | §11 Runtime、§12.2 数据模型 | 对应数据层与填写引擎的 I/O 接口 |
| 行式分片 / 自由增删行 | §4.3 动态增删 | §4 已实现其工程化定义 |

> **定位**：采集模型与扩展模型（§15）共同构成润乾"五模型"中**填报表模型**的完整工程来源（本稿 §3 扩展 + §4 报表填写已实现其理论）。至此，润乾《非线性报表模型》五模型——展现、数据、运算、子报表、填报表——的工程来源已全部覆盖。

---

## 十五、填报表扩展模型（润乾官方）

> 本章取自润乾官方《高级指南·填报表模型·扩展模型》（`kzmx59.html`，蒋步星），是 §14 采集模型的同级子章。图示见 HTML 版 图 36–37。

### 15.1 扩展触发条件

当**维度格的单元格表达式是维序列**时，按序列成员**纵向或横向扩展**表格。即：扩展以维度格为单位，数据源是写在单元格里的维序列（如 `["中国","美国"]` 或嵌套的 `[[...],[...]]`），而不是普通数据集的行集。

### 15.2 扩展方向判定规则

对每个需要扩展的维度格，方向按以下优先级判定：
- **若其左（上）有扩展格** → 延用该扩展格的方向；
- **否则，若其右（下）有数值格** → 纵（右）/横（下）向扩展；
- **若右、下均有 或 均无数值格** → 默认**纵向扩展**。

### 15.3 撑大与终止

扩展时，同行/列上**相邻的非扩展格被"撑大"**（随扩展方向一起增长，保持对齐）。**多层扩展**：当维序列是嵌套结构时按层依次展开（国家→省→市…），扩展**持续进行直到遇见数值格为止**——数值格作为叶子终止扩展。

### 15.4 示例

- **例1**：B4 纵向扩展，A3 被撑大；
- **例2**：B4 纵向扩展 + C2 横向扩展，C1、A3 被撑大（纵横混合）；
- **例3（多层）**：A2 `["中国","美国"]` → B2 `[["河南","河北","山东"],["阿拉斯加","加州"]]` → C2 三层嵌套到市 → D2 数值格终止。

### 15.5 与本设计文档的映射

| 扩展模型概念 | 本稿对应 | 说明 |
|---|---|---|
| 维度格 + 维序列触发扩展 | §3 扩展变换、§12.1 展现模型扩展、§11.3/§11.11 | 扩展单位下沉为 Band 的工程化定义 |
| 方向判定（左/上→右/下→默认纵） | §2.4 自动主格、§3.2 主格扩展方向 | 方向规则与主格设置同源 |
| 多层扩展直到数值格终止 | §3.3 报表扩展、§11.12 交替扩展树 | 父子方向交替的递归扩展 |
| 相邻格撑大 | §11.13 分页/拉伸、§13.5 定位与拉伸 | 空白格优先占用、分页伸缩 |
| 先扩展后采集 | §14 采集模型 | 扩展生成实例分片 → 采集拼成序表 |

> **定位**：扩展模型与采集模型共同构成润乾"五模型"中**填报表模型**的完整工程来源（本稿 §3 扩展 + §4 报表填写已实现其理论）。至此，润乾《非线性报表模型》五模型——展现、数据、运算、子报表、填报表——的工程来源已全部覆盖。

---

### 附：本文所据原文与材料说明

- **主依据**：《非线性报表模型原理》（蒋步星，中国科学技术出版社，2007）文本（由同名词 `.docx` 提取），覆盖第 1–6 章全部定义、算法（2.1–2.4、3.1–3.3、4.4–4.7、5.1–5.3）与定理（2.1–2.12、3.1–3.12、4.1–4.4、5.1–5.12）。这是非线性报表模型的**权威理论来源**，本设计文档的算法与设计均据此重构。
- 《基于单元格扩展的报表引擎的研究与实现》（豆丁网 PDF）：文本提取结果仅为网页抓取外壳（页眉"基于单元格扩展的报表引擎的研究与实现"、页码、下载/会员按钮），**不含正文**，未作为算法来源。
- **润乾报表官方《高级指南·非线性报表模型》**（蒋步星，Raqsoft）：`展现模型`（https://d.raqsoft.com.cn:6443/report/senior/zxmx36.html）与 `数据模型`（https://d.raqsoft.com.cn:6443/report/senior/sjmx43.html）为 §12 模型基础层的权威在线来源，与本书同源；`运算模型`（https://d.raqsoft.com.cn:6443/report/senior/ysmx46.html）与 `子报表模型`（https://d.raqsoft.com.cn:6443/report/senior/zbbmx52.html）已纳入 §13；`填报表模型·采集模型`（https://d.raqsoft.com.cn:6443/report/senior/cjmx56.html）已纳入 §14；`填报表模型·扩展模型`（https://d.raqsoft.com.cn:6443/report/senior/kzmx59.html）已纳入 §15。至此润乾《非线性报表模型》五模型（展现/数据/运算/子报表/填报表）的工程来源已全部覆盖。
- 其余 4 份 PDF（《报表》《报表引擎设计》《报表引擎设计(20160305112551)》《报表引擎系统研究开发》）均为**扫描图像版**，原 PDF 文本层缺失。已安装中文 OCR（`chi_sim`）并对《报表引擎设计》做了抽样 OCR（前 50 页）。其目录可辨认为**周金根《报表引擎设计》（2010）**一类**工程实现文档**——目录含"报表应用思路 / 报表类型 / 主要技术 / 横向扩展 / 横伸主格 / 单元格的层次坐标和汇总运算 / 相对层次坐标到位移 / 用例模型 / 操作符 / 单元格背景图 / 打印模型 / 填报表"等，明显是**复用非线性报表模型的概念去落地一个报表引擎**。但扫描件版面含大量表格与示意图，OCR 识别率过低（正文严重乱码），无法逐字提取其独立算法；其复用概念已在本设计文档中严格化，故不再另列。如需对其做高质量识别，建议提供清晰文本层版本或对其做人工校对后的 OCR 精修。
- 同目录 `extracted/`（文本提取）与 `ocr/`（图像 OCR 中间产物）保留以供核对。
