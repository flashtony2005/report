# 2026-09-22 · B2 条码 / 二维码进服务端报表（收口）

承接 `引擎差距分析-对照积木报表.md` 的 **B2**。上一轮已把编码器 + HTML + xlsx 打通，
本轮做**收尾**：故障注入复验、TS 镜像补齐、全套回归、记忆归档。

## 做了什么

### 1. 故障注入：13 条全被抓到（但第一轮漏了 1 条）

第一轮 12 条里 **`priority` 那条没红** —— 探针报告「这条检查是摆设」。
查下去发现**不是探针的问题，是注入点选错了**：

原注入把 `decode_images` 里图片格改成返回 `Graphic::None`（图片干脆不嵌位图）。
可探针压根没验「图片格有没有位图」，它验的是条码 —— 于是**行为差异在断言上不可观察**，
红不了是必然的。**结论：注入必须打在「行为差异能被断言观察到」的地方。**
先怀疑注入，别先怀疑检查。

修法两步：
- **补断言**：`verify-xlsx-barcode.py` 新增「优先级」段，构造一个
  `C3 图片+图表 / D3 只有图表 / E3 条码+图片 / F3 条码+图表` 的模板，
  从 **xlsx 产物**验「位图 1 张、原生图表 2 张」，并断言被盖住的两个条码内容
  不出现在任何 XML 里。
  为什么必须从 xlsx 验：JSON 和 HTML 都走 `graphic()`，渲染端分叉了它们看不出来
  （HTML 侧压根不出原生图表）。
- **换注入点**，拆成两条：
  - `priority-xlsx`：`write_charts` 不走 `graphic()` → 「图片+图表」的格子位图和图表都嵌
    （这正是加条码时挖出来的**老 bug**）。
  - `priority-engine`：引擎「被盖住就不编」的守卫失效 → 被图片盖住的条码照样编出来。

**13 条全部让探针变红，还原后复验回到全绿。**

### 2. TS 镜像补齐（`mirror-check.py` 从 3 处漂移 → 0）

`openprint/src/report/grid-report.ts` 补 `CellBarcode`（4 字段）+ `ResolvedBarcode`（3 字段）
+ `BarcodeSymbology` 类型别名，以及 `CellModel` / `CellTpl` / `GridCell` 三个槽。
`mirror-check.py` 的 PAIRS 加两组 → **21 个类型全部一致**。

文档里顺带写清**能力边界**（免得被当成缺口反复提）：QR 只做字节模式 + ECC M + v1~10；
Code128 不做中途切码集；没有 HRI 人可读文字；EAN/UPC/Code39 明确拒绝。

### 3. 回归（全部绿）

- Rust：**423 passed / 0 failed / 14 ignored**；强制重编 **0 告警**。
- 前端全量 vitest：**101 文件 / 1068 用例全过**（与上次基线数字**完全一致**，无回归）。
- `tsc -p designer-react/tsconfig.json`（经 alias 连带 openprint）：**exit 0，零错误**。
- `scripts/ts-check.sh`：22 个文件，**无类型错误**。
- `scripts/ts-test.sh`：`grid-report.spec.ts` **143 passed**。
- `scripts/verify-barcode.py`（编码器级，zxing 解回原文）：10 样本全过。
- `scripts/verify-xlsx-barcode.py`：6 张去重位图 + 12 个内联 SVG 全部解回原文；
  新增优先级段通过。
- 无回归：`verify-xlsx-chart.py` ✅ / `verify-xlsx-image.py`（6 媒体 6 锚点）✅ /
  `verify-xlsx-export.py`（边框 + 3 页各 2 行重复表头）✅。

## 三个「差点误判」的教训

1. **BSD grep 的 `\|` 又踩一次**（这是第六、七次了）。
   我用 `grep -n "清掉最后一项\|老报表无 params\|先 grep 源码" MEMORY.md` 复核，
   **返回空** → 我据此判断「改动已生效」。其实 BSD grep 在 BRE 下把 `\|` 当字面量，
   三个词一个都没匹配。**判据：grep 说「没有」时先怀疑 grep。** 代码/文本搜索一律用
   Grep 工具或 `grep -E`。
2. **Edit 报成功 ≠ 文件真的改了**。同一批里有两个 Edit 返回 success，
   但事后逐字节核对发现**旧串还在文件里**（`清掉最后一项…`、`老报表无 params…`）。
   教训叠加了上面那条，导致连续两次误判。
   → **改完一批 Edit 要用「应有 / 应删」子串断言复核最终内容**，别信 success 消息。
   最后 `MEMORY.md` 改成**整文件 Write**，再用脚本逐条断言（应有 5 条 / 应删 4 条）
   并核对字节数（12199 < 12288）。
3. **⚠️ 把「沙箱 broker 拒绝」误判成「这个测试套件就是很慢」**。
   全量 vitest 跑了 **38 分钟无输出**，我先归因成「约 1900 用例 + happy-dom 慢」，
   还写进了日志。换成引擎配置又跑了 **30 分钟**，结束时是
   **64 个文件全部 `CODEBUDDY_BROKER_DENY` / `Sensitive content approval timed out`**
   —— 每个文件在等 broker 审批超时，**根本没跑测试**。
   挂上 `NODE_OPTIONS="--require scripts/vite-safe-delete-bypass.cjs --require
   scripts/broker-mkdir-throttle.cjs"` 之后：引擎 **4.15 秒 / 779 用例**，
   全量 **13.8 秒 / 1068 用例**。
   **判据：vite 工具链（vite / vitest / vue-tsc / tsc -p）在沙箱里一律要挂 preload；
   几分钟没输出先怀疑 preload，别怀疑套件慢。** 这条 `sandbox-broker-workarounds`
   里本来就有（trigger 就写着 `CODEBUDDY_BROKER_DENY`），**该先读技能再动手**。
   顺带：`vitest run <路径过滤>` 带位置参数也会被拒，要收窄范围用 `-c 配置文件`。

## 一个既有问题（本次未动，先记着）

`openprint` 自己的 `npm run type-check`（`vue-tsc --build`）**报 126 条错**
（`noUncheckedIndexedAccess: true` + `tsconfig.app.json` 把 `*.spec.ts` 也 include 了，
下标访问全是 TS2532）。
**已证明与本次改动无关**：把 `grid-report.ts` 换回上一版重跑，
归一化（去行号列号）后**错误集合逐条一致**（15 种 / 各 126 处），还原后 `git status` 干净。
项目实际用的类型检查门是 `scripts/ts-check.sh`（`--noResolve` + 噪声过滤）→ 通过。
要不要清这 126 条是独立一件事。

## 数字

- Rust 350 → **423**（+73），0 告警。
- 前端全量 vitest **1068 用例 / 101 文件**（与上次基线一致）；挂 preload 后 **13.8 秒**。
- 探针：`verify-barcode.py` / `verify-xlsx-barcode.py` / `fault-inject-barcode-probe.py`（13 条）。
- 新增文件：`barcode.rs` / `barcode_svg.rs` / `png.rs`（Rust）+ 3 个探针脚本。
- 本次改动：11 个文件 +864 / -72，另有 6 个新文件。
- 提交 `1e390e2`（18 文件 / +4144 -72），已 push，`git status -sb` 复核在同步。

## 没做 / 待定

- 设计器面板还没给条码加 UI 入口（目前只有模板 / JSON 层可用，`CellBarcode` 的
  TS 类型已就位，面板接上即可）。
- 差距分析里还剩：**B3 条件格式 / B4 超链接·钻取 / B5 子报表 / B6 Word 导出 / C 文件·API 数据集**。
- 用户待定：客户端 `niceMax` 是否改成文档里写的 80/300 行为。
- `openprint` 的 126 条既有类型错（见上）没清。

---

# 2026-09-22（下午）· 补设计器面板：条码 + 图表

上一段收口了 B2 的**引擎**能力，但设计器里**只有「图片」段** —— 条码和图表都只能手改 JSON，
等于「能力做出来了、UI 上摸不到」。本轮把这两段面板补上（用户选定）。

## 做了什么

### 1. 引擎层的设计期判据（`openprint/src/report/grid-report.ts`）

加了一批**纯函数**，只吃声明不吃数据，供面板在**发出请求之前**就提示：

- `BARCODE_SYMBOLOGIES` / `BARCODE_MAX_BYTES`（`{qr:213, code128:48}`）/ `normaliseSymbology`
  / `isGs1Relevant` / `utf8ByteLength` / `isValueBarcode` / `barcodeProblem`
- `CHART_KINDS` / `chartProblem`

三条刻意的设计：
- **`barcodeProblem` 不收 `payload` 参数**：内容就是声明里的 `value`，让调用方另传一份迟早会传岔。
- **`from: 'value'` 直接返回 `null`**：内容运行期才从数据里来，设计期无从判断，硬报就是假红。
- **只提示不拦**：服务端失败粒度是**一格**（`[条码: 原因]` + 告警），设计器拦成「整表不让存」
  比服务端更严 = 擅自加规则。
- `utf8ByteLength` 要**认代理对**：`'𝒜'.length === 2` 但 UTF-8 是 4 字节，直接 `s.length` 会算少。

### 2. `GridReportModal.tsx` 的 `CellModelEditor` 加两段

照「图片」段的写法。另有**优先级提示**（同时设了 ≥2 种图形时说明只有第一个会出）。
`gs1` 只对 Code128 显示，且切回二维码时**把已有 `gs1` 摘掉**（否则静默留在模板里，设了没反应）。

### 3. 用例 35 条 + 反证 10 条

- `grid-report-cell-barcode.spec.tsx`（18）/ `grid-report-cell-chart.spec.tsx`（17）。
  断言一律看 `onChange` 收到的 **model**，不断言「没报错」。
- `scripts/fault-inject-ui-panel.py`：**10 条注入全部让用例变红**，还原后复验全绿。

## 本轮两个新教训

1. **⚠️ 同一个文件的两个 `Edit` 放在同一条消息里会互相覆盖（静默）。**
   我给 `GridReportModal.tsx` 并行发了两个 Edit，两个都回 success，
   但事后核对发现**第一个没落地**（`const ch = m?.chart` 那行还是原样）。
   两个 Edit 各自「读文件 → 改 → 写回」，并发时后写的把先写的覆盖掉。
   → **同一文件一次只发一个 Edit，改完立刻 Read 复核**。
   （这是本项目第二次栽在「Edit 报成功 ≠ 文件真改了」，上一次是 `MEMORY.md`。）

2. **`ts-check.sh` 过了 ≠ 类型没问题 —— 它是 `--noResolve`，看不见跨文件类型错。**
   本次 `ts-check.sh` 报「无类型错误」，而 `tsc -p designer-react/tsconfig.json` 在
   **同一个 spec 文件**里抓出 **2 处真错**：
   - `symbology: 'code39'` 不满足 `BarcodeSymbology`（故意塞契约外的值，要显式 cast）；
   - `{ from: 'value', symbology: 'code128' }` 缺必填 `value`（`value: string` 是必填，
     `from: 'value'` 时它被忽略，所以填 `''`）。
   → **两个门禁都要跑**：`ts-check.sh`（快、带 `--noUnusedLocals` 抓没用的 import）
   + `tsc -p`（真解析、抓跨文件）。只跑前者就宣称「类型干净」是过度自信。
   顺带：`ts-check.sh` **真的会抓没用的 import** —— 本次它先报出
   `CHART_KINDS` / `CellChart` 两个 import 没用（因为我把下拉项硬编码了，
   没用 `CHART_KINDS.map`），改成从常量生成后才消掉。修法是**用上它**（单一事实源），
   不是把 import 删掉。

## 回归（全绿）

- 前端全量 vitest：**103 文件 / 1103 用例全过**（基线 1068 + 新增 35，数字吻合）。
- Rust：**423 passed / 0 failed / 14 ignored**。
- `tsc -p designer-react/tsconfig.json`：**exit 0，零错误**。
- `scripts/ts-check.sh`：24 个文件，**无类型错误**。
- `scripts/mirror-check.py`：**21 个类型全部一致**（含 `CellBarcode` / `ResolvedBarcode`）。
- `scripts/fault-inject-ui-panel.py`：10/10 被抓。

## 数字

- 改动：`GridReportModal.tsx` +316 / `grid-report.ts` +145，另有 2 个新 spec + 1 个注入驱动。
- 测试增量：**+35 条 UI 用例**（前端 1068 → 1103）。

---

# 2026-09-22（傍晚）· B3 条件格式 + 一次「多 agent 并行」盘点

用户要求「看下还有什么任务，多 agent 运行」。于是先把**剩余任务**盘清，再并行跑 4 个 agent。

## 剩余任务盘点（依据 `引擎差距分析-对照积木报表.md`）

B1 图表 ✅、B2 条码 ✅ 已收口。**剩下的**：

| 项 | 结论 |
| --- | --- |
| **B3 条件格式** | 本轮**已做**（见下） |
| **B6 Word 导出** | 调研完成：现实 **900~1400 行**（砍掉原生图表 ~700）。**最大风险是静默**：OOXML 少个命名空间 / `r:id` 对不上，Word 只说「文件已损坏」，不给行号 |
| **C 文件·API 数据集** | 调研完成：**600~1000 行**。最便宜的 80% **不需要动 Rust** —— `RenderRequest.datasets` / `ReportTemplate.datasets` **已存在**，设计器把上传的 CSV/JSON 内联进去即可 |
| **B5 子报表** | 调研结论：**不建**。本项目自己的理论笔记 `报表PDF资料通读笔记.md:76` 就明确**否定用子报表组合复杂报表**（子表与主表非整体、不能互相运算），改走多源分片 —— 而我们已有 `loop_field` + 多数据集 + `join_on` |
| **B4 超链接·钻取** | 打印场景用不上，未动 |
| **HTML 预览不消费 `CellStyle`** | **本轮发现的既有缺口**，见下（比 B3 本身更值得修） |

## 并行方式：按**文件归属**切，不是按任务切

4 个 agent 同时跑，但**只有 1 个允许写代码**：

| agent | 类型 | 能否写 | 归属 |
| --- | --- | --- | --- |
| B3 条件格式 | `general-purpose` | ✅ 唯一写者 | `model.rs` / `engine.rs` / `mod.rs` / `grid-report.ts` / `GridReportModal.tsx` |
| B6 方案 | `Explore` | ❌ 工具上就没有 Edit/Write | 只读 |
| C 方案 | `Explore` | ❌ | 只读 |
| B5 方案 | `Explore` | ❌ | 只读 |

**为什么必须这样切**：B3 / B4 / B5 **全都写 `model.rs` + `engine.rs` + `mod.rs`**（`mod.rs` 6930 行、
`engine.rs` 4930 行），两个写者并发必然互相覆盖 —— 当天上午刚被更小的同类事故咬过
（同一条消息里两个 `Edit` 打同一个文件，后写的把先写的**静默**覆盖）。
用 `Explore` 类型做调研是**工具层面的硬约束**（它没有写工具），比在提示词里写「请勿修改」可靠。

## B3 条件格式（已独立复验）

**关键洞察（省掉了整个渲染端）**：`CellStyle` 的链路 `CellModel.style` → `CellInst.style`
→ `GridCell.style` → xlsx `with_style()` **本来就通**。所以条件格式**不需要任何新渲染代码**，
只要在展开期决定「用哪份样式」塞进同一个槽。结果：**`xlsx.rs` 改动 0 行**。

- 声明：`CellModel.conditional?: CellConditional[]`（**只挂 model** —— 它比的是本格算出来的值，
  而值只可能来自 `model`，与同样只挂 model 的 `style` / `format` 一致）。
- `when` 用 `String` 不是 enum：与 `CellChart::kind` 同一套约定 —— enum 会让**整份模板**解析失败，
  字符串只坏这一条规则。
- **第一条命中生效**（顺序是语义，设计器给序号 + ↑↓）。
- **空样式的规则在编译期被刷掉**：留着它命中后会把后面真正想生效的规则**挡掉**，比报错难查。
- **非数值 / 空值 / 布尔 → 一条都不命中**（不把文本猜成数字；「空着」和「就是 0」是两回事）。
- 上下界写反 → 告警并**自动交换**（否则区间恒空 = 「规则配了、导出什么都没变」）。
- 坏规则 → `warnings`，表照常出（服务端失败粒度是一格）。

**复验数字**：Rust **443 passed / 0 failed**（423 → +20）；vitest **104 文件 / 1125 用例**
（103/1103 → +22）；`ts-check.sh` 0 错；`tsc -p` exit 0；`mirror-check.py` 一致（22 类型）；
`fault-inject-conditional.py` **15/15 被抓**，还原后全绿。

## ⚠️ 本轮最值得记的一条：HTML 预览根本不画样式

做 B3 时顺着查「样式到底谁在消费」，发现：

- 全项目 **唯一**读 `GridCell.style` 的生产代码是 **`xlsx.rs:565`**。
- **`to_html`（`mod.rs:630`，73 行）完全不消费它** —— 只吐 `<table border="1">`，
  函数体里 `background` / `color` / `font-weight` **零命中**。

后果：作者设的字色 / 底色 / 粗斜 / 对齐 / 字号，**以及刚做完的条件格式**，
在 **HTML 预览里完全看不到**（设计器预览走的就是服务端 HTML），只在导出的 xlsx 里出现。

**这是既有缺口，不是 B3 引入的**（`CellStyle` 从做出来那天起就只对 xlsx 生效）——
之前没人发现，是因为没人同时看「预览」和「导出」两边。已立为独立待办（#74）。

教训：**「某个字段有人写」不等于「有人读」**。`CellStyle` 有 7 个字段、有 UI、有单测、
有 xlsx 落地探针，但**没有一个探针验过 HTML 侧**。判断一条数据链路是否真的通了，
要**从产出物往回查消费点**，不能只看赋值点。
