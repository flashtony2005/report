# 2026-09-27

## mirror-check.py 加「语义层」（第 2 档）—— 规则类事实的跨语言闸

**动机**：`mirror-check.py` 以前只对**字段名形状**（24 组）。于是「TS 侧单独改一个字节上限
/ 别名 / 白名单」**没有任何闸会红** —— Rust 侧改了有内容钉子，TS 侧改了无人拦。
属本项目最怕的「同一份事实声明两遍后漂移」。

### 改了什么

`scripts/mirror-check.py` 从单层变**两层**：

```
A. 形状层   24 组 (Rust 文件, Rust 类型, TS 类型) —— 只比字段名（行为不变）
B. 语义层    8 条规则类事实 —— 比内容，每条自带方向
```

新增 `dataclass Fact{label, rust, rust_path, ts, mode, note}`，`mode` 三种：

| mode | 含义 | 用在 |
| --- | --- | --- |
| `equal` | **顺序 + 内容**都要一致 | 会被 TS 拿去拼提示文案的清单（`PAPER_NAMES.join(' / ')`）—— 顺序变了用户看到的话就变了 |
| `set_equal` | 集合相等，**顺序不算** | 只做「认不认识」判断的清单（`IssueCode`） |
| `ts_subset` | **TS 不允许比 Rust 宽**，允许更窄 | 别名表（TS 少认一个别名是合法收窄） |

8 条事实与锚点：

| 事实 | mode | Rust | TS |
| --- | --- | --- | --- |
| 纸张名清单 | `equal` | `model.rs` `PAPERS` | `grid-report.ts:677` |
| 图表类型 | `equal` | `chart.rs:30` `KINDS` | `:301` `CHART_KINDS` |
| 码制白名单 | `equal` | `barcode.rs:40` `SYMBOLOGIES` | `:188` `BARCODE_SYMBOLOGIES` |
| 字节上限 | `equal` | `barcode.rs:52,59`（213 / 48） | `:197` `BARCODE_MAX_BYTES` |
| 条件运算符 | `equal` | `model.rs:163` `CondOp::NAMES`（8） | `:417` `CONDITION_OPS` |
| 码制别名 | **`ts_subset`** | `barcode.rs:186` `normalise_symbology` | `:211` `normaliseSymbology` |
| 页脚字符上限 | `equal` | `model.rs:1028` `escaped_len + 2 > 255` | `:748` `escaped + 2 > 255` |
| 诊断 code 词表 | `set_equal` | `issue.rs:139-149` `CODE_*`（6） | `:799` `IssueCode`（6） |

实测：`python3 scripts/mirror-check.py` → 24 形状 `OK` + 8 语义 `OK`，
末行 `结果： 镜像一致（形状 + 语义）`，退出码 0。

### 关键设计：**抽取失败必须红，不能静默跳过**

抽取函数返回 `Optional[list[str]]` —— **`None` 不是「空」，是「抽取失败」，计为红**。
理由（写进文件里了）：静默跳过会让这条检查退化成「永远绿、但根本没在检查」——
**比红闸更坏，因为它产出的是空头的信心**。

配套 `_one(pattern, text)` 要求锚点**恰好匹配 1 处**：
匹配 **0 处**（常量被改名）和 **≥2 处**（正则写松了）**都算失败**。

**「≥2 处」不是假想的**：`engine.rs` 在 4 个测试函数内各自定义了局部 `const KINDS`
（`:4559 :4803 :4852 :4921`）。抽取器若图省事做**全仓搜索**，「取第一个」会
**静默取到测试里的那张表**。`_one()` 把这种情况变成红。

### 另一个自己踩出来的坑：字符串里的 `{`

取函数体靠数花括号（`_fn_body`），而 `normalise_symbology` 里有 `format!("…{raw}…")`
和 `"{}"` —— **字符串字面量里的 `{` 会让计数错位**，切出错误的函数体、可能给出**假 OK**。
修法：先 `_blank_strings()` 把字符串**内容**抹成**等长空格**（长度不变所以索引不漂）再数括号。
三种引号都要处理（Rust/TS 双引号、TS 单引号、TS 模板串）。

### 故障注入：9 条，9/9（`scripts/fault-inject-mirror-semantics.py`）

| # | 注入 | 期望 |
| --- | --- | --- |
| 1~6 | TS 侧改图表类型 / 字节上限 / 条件运算符 / 纸张名 / `IssueCode` / 页脚上限 | 红，且报出**是哪一条事实** |
| 7 | TS 别名**更宽** | 红（`ts_subset` 上界在管） |
| 8 | TS 别名**更窄** | **仍然绿**（对照组 —— 证明方向没写反） |
| 9 | Rust `KINDS` → `KINDS_RUST`（**只改锚点、不改事实**） | 红，报「**抽取失败**」 |

还原后 sha256 逐字节一致，基线回到 `exit=0` 全绿。

**第 8、9 条是最重要的两条**，因为它们证明的**不是「改了会红」**：
- 第 8 条 = **对照组**：证明「合法的更窄不会被误报」。没有它，前 7 条全绿也可能只是「碰巧都改宽了」。
- 第 9 条 = **元性质**：**没有改任何事实**，只改锚点。没有它，前 7 条全绿也可能只是
  「恰好锚点还在、正则还松」—— **闸根本没在检查**。

### 本层**没有**覆盖的（边界，别当成已覆盖）

- **表头行数判据**（`model.rs:1071` ↔ `grid-report.ts:1543`）—— 它不是「一张表」而是一段**判据**，
  抽成事实要跑两边的代码，正则做不到。仍只有 Rust 侧内容钉子，**只改 TS 一侧仍无人拦**。
- **样式颜色 `#RRGGBB`** —— 两边形态不同（Rust 在 `html_style_attr` 校验，TS 是一组正则），
  且是「两端点都报 400」那种，抽成「清单」不自然。
- **只覆盖「声明」，不覆盖「用法」** —— 闸能证明两边声明了同一张表，
  不能证明两边用同一套逻辑去用它（判据顺序、trim / 大小写敏感度）。

### 顺带修的数字漂移

- `scripts/check-all.sh`：注释里的 `15 个 fault-inject` → **16**（两处：header + 末行 printf）。
  同时把「27 个 .py」那句改成「**写这个脚本时**，27 个 .py」，避免历史陈述被读成现状。
- `check-all.sh` 的闸 1 标签与 `--list` 描述补上「形状 24 组 + 语义 8 条」。
- `报表引擎详解-功能与算法.md` §15.1 的 fault-inject 数量 `15` → **16**，并记明它**漂过两次**。
  **判据：注释里的数字是最容易漂的东西 —— 改完脚本记得 `ls scripts/fault-inject-*.py | wc -l` 核一遍。**

### 文档

- `架构体检-不足与改进方案.md`：§2.2 表格加「现状」列 + 现状横幅；§4 第 2 档标 ✅ 已做
  （8 条事实表 + 三种方向的判据）；§7.5 更正；**新增 §8 第 2 档实施记录**（8.1~8.6）。
- `报表引擎详解-功能与算法.md`：§14.5 重写（8 条已覆盖 + 3 条仍未覆盖）；
  §15.1 数量更正；**新增 §15.7**（15.7 方向表 / 15.7b 抽取失败必须红 / 15.7c 9 条注入）。

### 提交与闸

- `ef3ff81` feat(scripts): mirror-check 加语义层（8 条规则事实）+ 9 条注入证明有牙齿
- `572f6ae` docs: §2.2 那句「不覆盖规则体」改成「体检时」（别让历史判断读成现状）
- 闸：`bash scripts/check-all.sh` → **通过 7 · 失败 0 · 没跑成 0**（195s，退出码 0）；
  第 7 道 136 passed / 11 文件（140.85s）。两次 push 后都 `git ls-remote` 复核过。

### 技能更新（`~/.workbuddy-ai/skills/`，不在 git 里）

这次的两条教训**都不属于本项目**，属通用方法论，已写进技能：

- **`fault-injection-verify` 新增纪律 6「不是每条注入都期望红」**（五条 → **六条**）：
  - **6a 对照组注入**：判据带方向时（`A ⊆ B` / 允许更窄），
    「改窄了会红」与「方向写反了」**在红的输出里长得一模一样** →
    必须配一条**界内**注入（合法收窄）**期望绿**。
    **判据：凡判据带方向的，注入必须成对 —— 一条越界（红）+ 一条界内（绿）。**
  - **6b 元性质注入**：检查器若「从源码抽取事实再比对」，它有一条**读不到**的失败路径。
    必须有一条注入**只改锚点、不改事实**（`KINDS` → `KINDS_RUST`），
    期望红且报「**抽取失败**」。没有它，所有「改事实会红」都可能是假绿。
  - frontmatter description 与「## 六条纪律」同步改过（YAML 校验 OK）。
- **`silent-failure-hunt`**：§「闸只覆盖形状不覆盖语义」标**已闭合**，
  新增子节「**闭合时的教训：抽取失败必须红**」——
  指出这是 §2c「闸没有跑器」**换个器官复发**：
  §2c 是「闸没有跑器」，这里是「**闸有跑器、但读不到它要检查的东西时假装通过**」，
  病根相同 —— **绿的含义没有被验证过**。
  案例表加一行「闸读不到要检查的东西」+ 一条推广
  （「检查器有『读不到』这条路吗？有，就必须证明它不产出绿」）。

---

## 第 4 档第 1 条：保存不再毁用户的报表

**用户说「按推荐的选择处理」→ 我推荐的是这条。** 原本只是「原子写报表」（~5 行），
读代码时发现**另一半更严重**，一起做了。

### 两个缺口

| # | 缺口 | 性质 |
| --- | --- | --- |
| **A** | 手打一个**已存在**的 id 点保存 → 那份报表被**无声无息换掉** | **静默** |
| **B** | 写到一半崩 / 磁盘满 → 文件变截断 JSON，旧内容无备份、不可恢复 | 可见的数据丢失 |

**A 不在体检报告里**，是顺着 B 的代码读出来的：`store::save` 的 doc 注释写着
「静默覆盖同名文件会丢东西，**所以调用方（UI）要先经列表确认**」，
而 `GridReportModal` 的 `saveReport` **一次确认都没有**，UI 手上明明有 `savedReports`。

> **教训：读到「所以调用方要 X」的注释，去调用方看一眼 X 做了没有。**
> 这种注释比没有注释更坏 —— 它让后来的人**以为已经处理过了**。

### 改了什么

**服务端** `print-server/src/report/store.rs`：
新增 `write_atomic(path, text)` = `.bak` 备份 → 写 `.tmp` → `rename`。
与 `config.rs::ServerConfig::save` 同一套。`rename` 保「不出半成品」，
`.bak` 保「覆盖可逆」——**两件事，缺一不可**。
顺带钉住新风险：`.bak` / `.tmp` 不能进 `list()`（判据 `extension()=="json"` 天然跳过；
改成 `contains(".json")` 就会漏）。

**客户端** `designer-react/src/modals/GridReportModal.tsx`：
- 新增 `lastSavedId` / `reportsListKnown` / `pendingOverwrite` 三个 state
- `saveReport` 拆成「判据 + `doSave`」两段（确认后要能回到落盘那步，不能重建请求）
- 判据 `id !== lastSavedId && (!reportsListKnown || 列表里有这个 id)`
- 确认框写明**要换掉的是哪一份** + 上一版留在哪个 `.bak`

### 验证（都证明有牙齿）

- Rust 4 条单测：写失败时旧文件完好（**含时间戳没变**）/ 覆盖留 `.bak` /
  不残留 `.tmp` / `.bak`-`.tmp 不进列表
- **真机探针 `scripts/verify-atomic-save.py`**：单测造不出「进程写到一半死掉」，
  用 `RLIMIT_FSIZE` + SIGXFSZ 让服务必死在中途，断言目标文件**逐字节仍是上一版**。
  实测输出：`PUT 返回：（连接断了）` / `服务进程已退出 = True` /
  `与 v1 逐字节相同 = True` ✅
- `fault-inject-atomic-save.py` → **4 道门禁 4/4**（注入 A 同时挂单测与探针）
- `grid-report-save-confirm.spec.tsx` 6 条 + `fault-inject-save-confirm.py` → **6/6**

### 三个坑（都写进代码注释了）

1. **注入锚点别多包一行**。第一版把 `save()` 里整句 `write_atomic(&path, &text)?;`
   换成 `fs::write(&path, text)?` → **连 `create_dir_all` 一起删了** →
   `由配置路径推出的目录能存能列` 也红，原因是 `No such file or directory`，
   **与原子性无关**。会让人误以为「3 条用例守住了原子性」，其实只有 2 条。
2. **探针端口复用**。第一次跑出「服务进程已退出 = True」**同时**「写入成功」——
   18888 上留着上一轮（其实是**上一轮会话**留下的后台任务）的 print-server：
   我起的那个（带限额）bind 失败退出，health 与 PUT 全被旧进程答上（它没限额）。
   **症状是「服务死了但活干完了」。** 修法：起前 `pkill` 清端口，
   **并确认占端口的 PID 就是我起的那个**（只查「端口有人答话」不够）。
   `fault-injection-verify` 的反面模式里写着这条，又踩了一遍。
3. **jsdom 里 antd Modal 关了还在 DOM 里**。`confirmVisible()` 一开始用
   「有没有那个按钮」判 → 3 条用例同时红在「框该关掉」。
   根因：**rc-motion 的离场动画在 jsdom 里永远不结束**，`destroyOnHidden`
   等不到 `afterClose`，关掉的框留着 `ant-zoom-leave-active`。
   拿存在性当判据 → 「该弹」恒真、「不该弹」恒假，**两边都等于没测**。

### 顺带更正体检报告 §2.5 的一处错

初版写「`updated_at` 在写盘前赋好 → 崩了以后从列表**看不出**保存失败过，
这是**静默的那一半**」。**前半句对，推论是错的**：那个赋值改的是局部变量，
写失败时 `def` 被丢弃；失败会以 `Err` 返回（HTTP 400 / 前端弹错）→ **看得见的**。
已加更正框，并补了一条断言把「文件压根没被碰过」钉住
（诚实标注：它是「逐字节没变」的**推论**，不是独立证据）。

### 本档没做（边界）

- 服务端 **409 + `force`**：把「覆盖要确认」从客户端自觉变成服务端强制，
  才能覆盖「列表快照过期 / 别的客户端刚建了同名」。要改 API 与 UI 流程。
- UI 上没有「还原上一版」的入口；`.bak` 只有一代。
