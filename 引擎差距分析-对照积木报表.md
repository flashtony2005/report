# 引擎差距分析 —— 对照积木报表（JimuReport）

> **证据来源**
> - 积木报表一侧：其 GitHub README（`jeecgboot/JimuReport`，v2.5.3，2026-09-18）。
>   没读它的源码（LGPL，且核心不开源），所以**功能清单以它自己的声明为准**，
>   凡 README 没明写的我都标了「README 未明写」，不替它补。
> - 本项目一侧：**逐条 grep 代码**得出，不抄文档。凡本文说「有 / 无」都指
>   代码里有实现，不是「文档里提过」。
>
> 对照的是**功能面**，不是代码质量。两边的产品定位本来就不同（见下节）。

---

## 一句话结论

**不是一个物种，硬比功能清单我们会输得很惨；但输的那部分大多不是我们的赛道。**

- 积木报表是**在线报表平台**（Java/Spring Boot Web 应用）：把「业务人员在浏览器里
  做报表 / 大屏 / 填报」这条链路全包了，广度极大。
- 本项目是**打印版面引擎 + 非线性报表内核**（Rust 服务 + 客户端）：把「
  mm 精确排版 → 打印机 / 票据机」和「分组 / 交叉 / 主格关系」这两件事做深。

按 README 逐项比，我们缺 **7 个大类**（填报、大屏、AI、权限分享、移动端、
数据源广度、图表广度）。但其中**只有 2~3 类是真该补的**，其余是赛道不同或
架构性取舍（详见下节分类）。

---

## 定位差异（先看清这个，否则下面的对比会读歪）

| 维度 | 积木报表 | 本项目 |
| --- | --- | --- |
| 形态 | Spring Boot 内嵌的 Web 应用（浏览器设计 + 浏览器看） | Rust 单二进制服务 + 桌面打印客户端 |
| 谁在用 | 业务/实施人员自助拖拽 | 开发者定义模板，终端跑打印 |
| 核心资产 | 报表定义（存在它自己的库里） | **打印版面**（mm 绝对定位）+ 报表模板 JSON |
| 输出终点 | 屏幕 / 导出文件 | **纸**（激光/针式/票据机/标签机） |
| 部署依赖 | JDK17+ / MySQL / Redis(可选) | 一个可执行文件（默认构建**零原生依赖**） |
| 许可 | LGPL / GPL-3.0 双许可，**含禁止同类竞争条款** | 本项目自有 |

**最根本的一条差异**：积木报表的「打印」是**浏览器打印**的延伸；
我们的「打印」是**设备指令级**的（ESC/POS、TSPL、ZPL 字节流直发）。
这两件事的难点完全不同。

---

## 差距清单

按「该不该补」分四类。**A 类不是缺陷，是取舍；B 类才是真差距。**

### A. 架构性缺失 —— 补不了，或不该补

| 能力 | 积木报表 | 我们 | 说明 |
| --- | --- | --- | --- |
| **数据填报 / 回写** | ✅ 在线录入、校验、批量导入、写回库 | ❌ **架构上就没有** | 三个引擎全部**只读打开**（sqlite `SQLITE_OPEN_READ_ONLY` / pg `SET SESSION ... READ ONLY` / odbc 语句级）。这是**刻意的安全设计**，不是漏做。要填报等于把整个数据层反过来重做 |
| **大屏 / 仪表盘**（JimuBI） | ✅ 拖拽大屏、24 栅格仪表盘、门户 | ❌ | 我们的自由画布（mm 绝对定位）**技术上能画**，但没有大屏那套组件/主题/自适应。属另一个产品 |
| **AI 生成报表 / 大屏 / Chat2BI**（JimuChatBI） | ✅ 一句话出报表、截图还原、自动建表 | ❌ | 需要接 LLM + 一整套 schema 生成链路。与「打印引擎」正交 |
| **移动端** | ✅ 移动看报 / 填表 / 仪表盘 | ❌ | 我们连 Web 端都不是（客户端 + 服务） |
| **权限 / 分享 / 嵌入 / 水印** | ✅ 大屏密码、仪表盘保护码、分享、iframe | ❌ | 我们只有一个 `x-openprint-admin` 请求头做 CSRF 防护，**没有账号体系**。定位是「本机/内网工具」 |

> ⚠️ 这五类里，**填报**是唯一「业务上真的会有人问」的。答复口径应该是
> 「本服务按只读设计，不支持回写」——**说清是取舍，别说成「暂未实现」**
> （这俩区别本项目已经吃过一次亏，见 ODBC 那次）。

### B. 真实功能差距 —— 同赛道内该补的

这些是**我们赛道内**确实缺的，按我的优先级排：

| # | 能力 | 积木报表 | 我们 | 影响 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| B1 | **图表进服务端报表** | ✅ 28+ 种（ECharts） | ⚠️ **只有 3 种**（自研 `chartkit`：bar/line/pie），且**只在客户端画布**，服务端 `print-server` grep `chart` 零命中 | 导出 xlsx / 服务端渲染时图表出不来 | **高** |
| B2 | **条码 / 二维码进服务端报表** | ✅ 二维码/条形码报表 | ⚠️ 画布有（`PrintQrcode` / `barcode` 控件），**`CellTpl` 没有** | 标签/单据上要印二维码时只能走画布，不能走数据驱动 | **高** |
| B3 | **条件格式** | ⚠️ README 未明写，但强调「数据预警报表」 | ❌ 无。最接近的是 `format_expr`（改展示文本）和 `row_test_expr`/`col_test_expr`（整行/整列删除） | 「数值超标标红」这类需求做不了 | **中** |
| B4 | **单元格超链接 / 钻取 / 联动** | ✅ 超链接、下钻、联动 | ❌ 无（`CellModel` 21 个字段里没有链接类字段） | 交互式分析做不了；打印场景其实用不上 | 中低 |
| B5 | **子报表 / 报表内嵌报表** | ✅ 主子报表、报表调用报表 | ⚠️ 有**主格关系**（父子格 + `join_on` 跨数据集关联），语义更强，但**不是**「嵌另一个报表文件」 | 复用已有报表做不了 | 中低 |
| B6 | **Word 导出** | ✅ | ❌ 服务端只有 xlsx / csv / html（`/print` 另有 pdf）；客户端 export-engine 有 PDF/SVG/图片 | 公文类交付受限 | 中 |
| B7 | **多 sheet 报表** | ✅ | ✅ **有**（渲染支持多 sheet，导出按序） | — | 已具备 |
| B8 | **数据字典** | ✅ | ✅ **有**（`CellModel.dict`） | — | 已具备 |

> B1/B2 是**同一根因**：`CellTpl` 缺「非文本格子」的通道。图片那次（`CellTpl.image`）
> 已经把这条路走通了一遍，图表 / 条码可以照抄同一套做法
> （两个槽都认 + 只收 data URI + 取不到不静默、进 `warnings`）。

### C. 数据源广度 —— 差距很大，但大部分是「Java 生态红利」

| | 积木报表 | 我们 |
| --- | --- | --- |
| 数量 | **30+** | **3**（sqlite / postgres / odbc） |
| 信创 | 达梦、人大金仓、神通、GaussDB、虚谷、瀚高、TDengine | ❌（但 **ODBC 这条口子已经能接**） |
| NoSQL / 搜索 | Hive、HBase、CouchBase、Elasticsearch、MongoDB | ❌ |
| 云 / NewSQL | PolarDB、TiDB、Doris、ClickHouse | ❌ |
| **数据集类型** | SQL / API / JSON / 存储过程 / WebSocket / 静态 / 文件（CSV、Excel、JSON） | **只有 SQL**（`/api/data/*` 直连库） |
| 文件导入 | ✅ CSV / Excel / JSON | ⚠️ 只支持 **xlsx → 报表模板**（`/api/report/import`，反过来的方向），**没有把数据文件当数据集** |

**怎么读这个差距**：30+ 里绝大多数是 JDBC 驱动白送的，我们这边对应的是
「ODBC DSN 配一个驱动」。所以**真正的差距不是数量，是这两条**：

1. **没有「非 SQL 数据集」抽象** —— 缺 API / JSON / 文件数据集。这是**架构层**的
   差距（我们只有「连接 → 表 → 取数」一条路），比少几个驱动严重得多。
2. **没有跨数据源的联合查询**（积木报表支持一个报表连多个数据集做跨库查询）——
   我们只有**单 sheet 内跨数据集按 `join_on` 关联**（`Engine::new_multi`），
   是「主格匹配」而不是「SQL 级 join」。

### D. 平台能力

| 能力 | 积木报表 | 我们 |
| --- | --- | --- |
| 定时刷新 / 数据缓存 | ✅ | ❌ |
| 参数 / 查询表单 | ✅ 参数配置 + 条件查询 | ✅ **有**（`ReportDef.params` + `RunRequest.values`） |
| 套打 | ✅ 明列为特性 | ⚠️ **无专门模式**，但 mm 绝对定位画布 + 指定打印机/纸张，套打是其自然用法 |
| 分页打印 | ✅ | ✅ 有（分组感知分页） |
| 打印区域 / 页码 / 背景 | ✅ | ⚠️ **页码 ✅ · 纸张 / 方向 / 页边距 / 水平居中 ✅**（2026-09-23 补，`PageConfig` 的后 5 个字段）· **背景 / 水印 ❌** · 「打印区域」无框选概念，只有纸张 + 页边距 |
| 部署便捷性 | 需 JDK + MySQL(+Redis) | ✅ **一个二进制**，默认零原生依赖 |

> **2026-09-23 更正**：上一行原先写的是「`PageConfig` 3 字段：纸张/方向/边距类」——**那是错的**。
> `PageConfig` 当时那 3 个字段是**分页**（`rows_per_page` / `repeat_header_rows` /
> `repeat_footer_rows`），纸张 / 方向**从来不在它里面**，而在画布路径的 `PrintJob` 上
> （两回事：画布有纸张 ≠ 报表有纸张）。报表路径真正拿到这几项是 2026-09-23 才补的，
> `PageConfig` 现为 **8 字段**。教训：**「某个结构体有某能力」这种话，写完要能被 `grep` 验证。**

---

## 我们更强 / 它没有的地方（别只盯着缺的）

| 能力 | 说明 |
| --- | --- |
| **非线性报表内核** | 主格关系（`row_parent`/`col_parent`）、层次坐标（`D3[B3:+0]`）、跨数据集 `join_on`、`expand_expr` 固定列表展开、`expand_min/max_count`、`keep_expand_empty` —— 这是**润乾/NopReport 那一系的语义**，积木报表的 README 没到这一层 |
| **票据指令直发** | ESC/POS + TSPL + ZPL **字节流**（GBK/UTF-8 编码、点阵定位、二维码分段）。积木报表走浏览器打印，做不到 |
| **打印版面精度** | 自由画布 mm 绝对定位 + 13 类控件（text / image / table / barcode / qrcode / rect / line / richtext / chart / math / signature / zone / labelgrid） |
| **xlsx 导出的保真度** | 列宽/行高按 Excel 单位换算、重复表头、真嵌入图片、`export_formula`（导出成真公式，在 Excel 里改明细小计会重算）—— 比「导出一张表」深 |
| **可验证性** | 每个能力都有真机探针 + **故障注入反证**（本项目惯例）。这一条不是功能，是工程质量的差距 |

---

## ⚠️ 许可：这条比功能差距重要

积木报表 README 的 LICENSE 补充条款里有两条**必须知道**：

1. **禁止同类竞争** —— 不得修改、重新打包、发布或销售「功能与 JimuReport 类似」
   的产品，也不得在同一软件市场类别内竞争，违者视为侵权。
2. **必须保留版权** —— 二次开发/集成/部署时必须保留「Powered by 积木报表」、Logo
   与官方链接。

**对本项目的含义**：本项目就是报表引擎，**属于它的「同类」**。所以：

- ❌ **不能**把积木报表的代码抄进来（也抄不到，核心不开源）。
- ❌ **不能**用它的模板格式 / 设计器产物做兼容。
- ✅ **可以**读它的 README 做功能对标（本文就是这么做的）—— 对标功能不等于竞争。
- ⚠️ 如果将来想「兼容积木报表模板」，先找法务看第 1 条，别自己判断。

（顺带：它声明的免费版「代码不开源」，只有 LGPL/GPL 的声明，
所以「照着它的实现抄一遍」这条路本来也不存在。）

---

## 建议的动手顺序

只列**赛道内、投入产出比高**的。A 类（填报/大屏/AI/权限/移动端）**建议明确不做**，
对外统一口径为「按只读设计，不支持回写」而不是「暂未实现」。

| 顺序 | 做什么 | 为什么先做它 |
| --- | --- | --- |
| 1 | **`CellTpl` 加图表通道**（复用图片那套：两个槽都认 + 只收 data URI + 失败进 `warnings`） | 与图片同根因，图片刚走通一遍，边际成本最低；补上后服务端导出的报表才「完整」 |
| 2 | **`CellTpl` 加条码/二维码通道** | 同上；且客户端已有 `PrintQrcode`/`barcode` 可参照 |
| 3 | **条件格式（至少「按值改样式」）** | 需求高频。注意：Univer 的样式通道**只有底色 + 字色能画**，边框完全不渲染 —— 别设计依赖边框的条件格式（本项目已实测） |
| 4 | **Word 导出** | 公文场景刚需；服务端已有 xlsx 那套「单位换算 + 布局」的积累可复用 |
| 5 | **文件 / API 数据集** | 架构层差距，最贵。等前四条做完、且真有需求再动 |

**不建议追的**：数据源数量（信创库用 ODBC DSN 接就行，别逐个写适配）、
图表种类数量（3 种够打印用，缺的是「能进服务端」不是「有 28 种」）。
